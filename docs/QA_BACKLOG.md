# 열려 있는 QA 백로그

> **아직 끝나지 않은 일의 원본 목록입니다.** 2026.09.21 이전에는 이 목록이
> `CHANGELOG.md` 안에 `W1~W10 · U6 · U7 · U9 · U10 · P1~P3` 같은 **라벨로만** 인용되고
> 있었습니다. 라벨을 붙인 사람 말고는 무엇이 열려 있는지 알 수 없었습니다.

| 이 문서 | 아직 **안 끝난** 일 — 무엇이·왜 열려 있고·누가 정할 일인가 |
|---|---|
| [`../CHANGELOG.md`](../CHANGELOG.md) | 무엇을 언제 왜 **바꿨는가**(끝난 일) |
| [`ROADMAP.md`](ROADMAP.md) | 앞으로 **어디로 가려 하는가**(방향) |
| [`../AGENTS.md`](../AGENTS.md) §2 | 끝내기 전에 통과시켜야 하는 **검증 도구** |

⛔ 항목을 여기와 `CHANGELOG.md` 양쪽에 적지 마십시오. **원본은 이 문서**이고,
`CHANGELOG.md` 에는 **닫을 때** 무엇을 왜 바꿨는지만 적습니다.

---

## 0. 표기

| 표기 | 뜻 |
|---|---|
| 🟥 **열림** | 아직 안 했습니다 |
| 🟨 **절반** | 일부만 했습니다. 어디까지인지 함께 적습니다 |
| 🟩 **닫힘** | 끝났습니다. 닫은 날짜와 `CHANGELOG.md` 항목을 답니다 |
| ⬛ **정의 유실** | 라벨만 남고 **내용이 저장소에 없습니다**. 지어내지 않고 그대로 적습니다 |

**누가 정할 일인가**도 함께 답니다 — 이게 이 목록의 핵심입니다.
`업무` 는 소관 부서·담당자가 정할 일이고, `개발` 은 코드로 풀 수 있는 일입니다.

---

## 1. W — 업무·담당자 확인 (`업무`)

> ⚠️ **옛 `W1~W10` 라벨과 번호가 이어지지 않습니다.** 그 열 개의 **정의가 저장소에
> 남아 있지 않습니다** — `CHANGELOG.md` 6곳이 「W1~W10(담당자 확인)」이라고 인용만 하고
> 있고, 커밋 이력(`git log -S`)에도 내용이 없습니다. 아래 다섯은 「열려 있는 TODO」
> 절에 **문장으로 남아 있던 것만** 복원한 것입니다. ⬛ 나머지는 유실로 둡니다.

| | 항목 | 상태 |
|---|---|---|
| **W1** | 이혼 「당사자 신분증」 — 실제 접수에 **오는 사람의 신분증만** 필요한가 | 🟥 열림 |
| **W2** | 이혼 「협의서 원본」 — 담당자가 뜻한 **정확한 서류명** | 🟥 열림 |
| **W3** | 혼인 준비물이 **남편·아내 두 줄**로 갈라져 있다(원문은 「당사자 신분증」 한 줄) | 🟥 열림 |
| **W4** | 혼인·이혼은 제목 아래 **신고일을 인쇄하지 않는다**(출생·사망·개명은 찍는다). 의도인가 | 🟥 열림 |
| **W5** | 증명서·부동산의 준비물(`ready`) 목록이 **담당자 확정본인가** | 🟨 절반 |

- **W5 보충** — 「`ready` 를 비워 두었다」는 옛 기록은 **이미 틀렸습니다.**
  지금은 채워져 있습니다(`forms/cert.config.js:76` · `forms/realestate.config.js:183`).
  남은 물음은 「비었는가」가 아니라 **「담당자가 확정한 목록인가」**입니다.
  ⚠️ `forms/realestate.config.js:521` 에 「담당자가 확정한 준비물이 아니다」라는 경고가
  그대로 붙어 있습니다 — 확정되면 그 주석도 함께 걷어내야 합니다.

---

## 2. U — 화면·사용성 (`개발`)

| | 항목 | 상태 |
|---|---|---|
| **U1** | 여권 첫 화면에 진행 표시가 없었다 | 🟩 닫힘 · 2026.08.29 (13) |
| **U2** | 혼인 단계 제목이 **화면에 없는 항목**을 말했다 | 🟩 닫힘 · 2026.08.29 (13) |
| **U3** | 이혼 단계 제목이 같은 문제 | 🟩 닫힘 · 2026.08.29 (13) |
| **U4** | 혼인 6단계 항목 순서가 서식 번호와 어긋났다 | 🟩 닫힘 · 2026.08.29 (13) |
| **U5** | 부동산 지번주소 안내가 두 번 나왔다 | 🟩 닫힘 · 2026.08.29 (13) |
| **U6** | **증명서 단계 분할** — 한 단계가 너무 길다 | 🟥 열림 ⬛ 세부 유실 |
| **U7** | **출생 단계 분할** — 한 단계가 너무 길다 | 🟥 열림 ⬛ 세부 유실 |
| **U8** | Review·인쇄 미리보기 단순화(공통 마감 ②) | 🟩 닫힘 · 2026.08.30 |
| **U9** | `state.unsure` / Human Handoff | 🟨 **절반** — 아래 참조 |
| **U10** | **소요시간 안내** — 「몇 분쯤 걸립니다」 | 🟥 열림 ⬛ 세부 유실 |

### U9 — 「잘 모르겠어요」와 창구로 가는 길

단추는 「이 단계는 잘 모르겠어요 · **나중에 창구에서**」라고 약속합니다.

- 🟩 **약속의 앞쪽은 닫혔습니다**(2026.09.21) — 건너뛴 단계가 **인쇄 준비 화면**의
  「창구에서 확인하실 항목」에 모여 나옵니다. 되돌아가 [다음]으로 통과하면 지워집니다.
  `tools/verify-review.py` 가 세 가지(표시·지워짐·화면에 나옴)를 8종×2예시에서 잽니다.
- 🟥 **뒤쪽은 열려 있습니다** — 화면 아래 **Human Handoff 자리(`#askFoot`)가 엔진 7종에서
  빈 `<div>` 입니다.** `engine/engine.js` 에 `askFoot` 이 한 번도 나오지 않습니다.
  여권만 채웁니다(`passport-helper-v1.html:3652`).
- 🟥 **창구 전환 사유 코드가 여권에만 있습니다** — `P-01`~`P-06`
  (`passport-helper-v1.html:2031`). 엔진 7종에는 체계가 없습니다.

> ⚠️ **무엇을 어느 화면에 둘지는 `업무` 판단입니다.** 코드가 정할 일이 아닙니다 —
> 「직원을 부른다」가 이 민원실에서 실제로 무엇을 뜻하는지(번호표·호출·안내 문구)를
> 담당자가 정해야 7종 공통으로 이식할 수 있습니다.

---

## 3. P — 색·정렬·인상 (`개발`)

| | 항목 | 상태 |
|---|---|---|
| **P1~P3** | 색 · 정렬 · 인상 | 🟥 열림 ⬛ **세부 유실** — 라벨만 남았습니다 |

⛔ **서식 하나에서 색만 손대지 마십시오.** 이것은 **Color & Visual Polish backlog** 이고
8종에 **한 번에** 적용합니다 — `AGENTS.md` §5-1 · `docs/PRODUCT_SKIN_v1.md`.

---

## 4. 코드 잔재 (`개발` · 비차단)

> 2026.09.21 에 **실제 코드와 대조해** 갱신했습니다. 옛 목록에는 **이미 해소된 항목
> 네 개**가 남아 있었습니다.

| 항목 | 상태 |
|---|---|
| `FORM.rerenderOnSet` 이 하는 일이 없다 | 🟩 닫힘 — config 에서 제거됨(`engine/engine.js` 에 경위 주석만 남음) |
| `preview.fields`·`preview.sample`(허브 카탈로그)이 죽은 코드 | 🟩 닫힘 · 2026.09.04 허브 정리(889KB→149KB) |
| `engine/base.html`(옛 껍데기)을 지워도 되는가 | 🟩 닫힘 · 2026.09.04 삭제 |
| 증명서·부동산 `ready` 가 비어 있다 | 🟩 **사실이 아니게 됨** — 채워져 있습니다 → W5 로 옮김 |
| `#askFoot` 이 엔진 7종에서 비어 있다 | 🟥 열림 → **U9** 로 옮김 |
| 여권 Review 꼬리표의 **✍ 이모지**·선택지 `.opt-check` 의 활자 `✓` | 🟥 열림 · **미관** |
| 실재 군포 도로명 목록을 받으면 작성예시 주소를 사람마다 흩을 수 있다 | 🟥 열림 · `업무` |
| 시범운영 중이라 **운영문서 PDF·Electron 설치본 재생성을 멈춰 두었다** | 🟨 의도된 보류 |

⛔ 여권의 ✍·✓ 는 **미관 사유이므로 여권을 다시 열지 않습니다**(`AGENTS.md` §5-1).
`check-icons.py` 도 「한쪽에만 있는 이름」은 문제로 보지 않습니다.

---

## 5. 사람이 기기 앞에서 확인할 것

⛔ **코드로 닫을 수 없습니다.** 실제 키오스크에서 사람이 봐야 합니다.

| | 항목 | 상태 |
|---|---|---|
| **K1** | 부동산 「받는 곳」 **실물 인쇄** — 덮어 찍기가 종이에서 제대로 보이는가 | 🟥 열림 |
| **K2** | 환경설정 **통계 탭** — 현장에서 값이 쌓이는가 | 🟥 열림 |
| **K3** | **3분 유휴** 중도 이탈 — 초기화가 실제로 도는가 | 🟥 열림 |

⚠️ 이 셋은 **설치본 1.5.0 기준**입니다. 2026.09.21 의 U9 변경은 **설치본에 들어 있지
않습니다** — 시범운영 중 파생 산출물 재생성을 멈춰 두었기 때문입니다(`AGENTS.md` §3).
다시 구울 때 함께 확인하십시오.
