# 변경 이력

시범운영 중 들어온 의견과 그 반영을 여기에 기록한다.
8종에 흩어져 반영되는 변경이 많아, **무엇을·왜 바꿨는지**를 한 곳에 남긴다.

기록 형식

```
## 2026.MM.DD
### [반영] 한 줄 요약  · 의견 출처(민원실/담당자/시민)
- 무엇을: 바뀐 동작
- 왜: 요청·문제 상황
- 어디를: 고친 파일 (엔진이면 config, 공통이면 8종 전부)
- 확인: 인쇄 픽셀 비교 결과 등
```

---

## 2026.08.30 (6)

### [확인] **키오스크 실기 확인** — 공통 마감 ① 을 브라우저가 아니라 Electron 에서  · 실사용

지금까지의 확인은 전부 **브라우저 헤드리스**였다. 이번에 바꾼 것들(Idle 1분 · 서식→메인
복귀 경로 · 전환 · 준비물 2종 · 주민등록번호 선택)을 **실제 키오스크 앱으로 띄워** 확인했다.

- `--selfcheck`(창을 띄우지 않는 점검) — 렌더러 보안(sandbox·contextIsolation·devTools 차단·
  인쇄 보호) **전부 양호** · 지정 프린터 「삼성 흑백 프린터」 Normal · 대기 0건 ·
  개인정보 저장소가 임시 경로 **양호** · 시계가 오늘과 일치(하드보안관 원복 없음) ·
  Electron 33.4.11 / Chromium 130.
- 대화식 — 첫 화면 · Idle 순회 · 만졌을 때 멎고 첫 누름이 먹히는지 · 주민등록번호를 비운 채
  완주 · 인쇄 · 인쇄 후 복귀 · 무동작 복귀까지 **이상 없음**(2026.08.30 실사용 확인).
- ⚠️ `?from=idle` 은 키오스크의 이동 차단(`isAllowedUrl`)에 걸리지 않는다 —
  그 검사는 `pathname` 만 보고 쿼리는 `search` 라 통과한다(코드 확인 + 실기 확인).

- 📌 **운영 설정 두 가지가 [확인] 로 남았다**(코드 문제가 아니라 그 PC 의 설정이다):
  ① **관리자 PIN 미설정** → PIN 없이 종료 가능.
  ② **「파일로 저장하는 프린터」가 있다**(XPS · Print to PDF) → 인쇄물이 파일로 남을 수 있다.
  둘 다 `kiosk-app/admin/관리자_개인정보보호_설정.bat` 과 환경설정 창의 몫이다.
  ⛔ 그 배치를 적용·되돌리기 모드로 실행하지 마라(실제로 프린터가 삭제된다 — AGENTS.md §3).

---

## 2026.08.30 (5)

### [반영] Idle 전환이 **어색하던 것** 두 가지  · 실사용 지적

「좌측 카드가 아래로 내려가는데 가운데가 이상하게 바뀐다 · 앞 화면이 흐려지면서 넘어가는 게
어색하다」는 지적. **원인이 둘이었고 둘 다 고쳤다.**

1. **다 사라지기 전에 갈아 끼우고 있었다.** 퇴장 200ms 중 **110ms** 에 내용을 바꿔,
   옛 화면이 **45% 쯤 남아 있는 상태에서 툭 바뀌었다.** 겹쳐 보이라고 한 것이 오히려
   「흐려지다 끊기는」 것으로 보였다. → 퇴장 **160ms**, 갈아 끼우기 **170ms**(다 사라진 뒤),
   등장 **280ms**.
2. **가운데가 위로 올라와 왼쪽과 싸우고 있었다.** 왼쪽 선택 블록은 **아래로** 내려가는데
   가운데 컨테이너는 `translateY(15px)→0`, 즉 **위로** 올라왔다. → 컨테이너의 세로 움직임을
   **없앴다.** 세로 움직임은 항목별 stagger(9px)가 그대로 맡는다.

- 🔬 **「진짜 두 겹으로 겹치기」는 해 보고 되돌렸다.** 옛 화면을 복제해 얹고 서로 반대로
  흐리게 하는 방식(crossfade)을 실제로 구현해 갈무리로 확인했다 — **더 나빴다.**
  민원마다 준비물이 2~4장이라 가운데 덩어리의 **높이가 다르고**, 가운데 정렬이라
  두 겹의 제목·카드·단추가 **서로 어긋난 채 겹쳐 이중 노출**처럼 보였다.
  ⛔ 가운데 덩어리의 높이를 고정하지 않는 한 겹침은 답이 아니다. 다시 시도하지 마라.
- 어디를: `index.html`(전환 CSS·`demoSelect`) · `kiosk-app/app/index.html`.
- 확인: 전환 실측 **0ms 퇴장 시작 → 180ms 에 새 내용**(복제본 0개) · 멈춘 뒤 class 정리 ·
  **전환 한가운데에서 눌러도 첫 누름 삼켜짐 없음**(LEFT·CENTER 일치, [작성 시작하기] 동작) ·
  10초 뒤 화면 그대로(opacity 1) · `verify-print` 19쪽 0px · `measure-screen` 15 ·
  블록 9화면 동일 · `verify-site-config` 6 · `sync-kiosk --check` 통과.

---

## 2026.08.30 (4)

### [반영] 증명서·부동산 **준비물** — 담당자 지시로 채웠다  · 담당자

두 서식은 **받은 것이 없어 비워 두고 있었다**(그 구역을 아예 내지 않았다). 이제 들어왔다.

| 서식 | 준비물 |
|---|---|
| 가족관계증명서 발급받기 | **신분증** · **대리 신청 시 관련 서류**(대리 신청인 경우 위임 등 관련 서류가 필요할 수 있습니다) |
| 부동산 거래 신고하기 | **부동산 거래계약서 사본** · **신고인 신분증** · **거래 유형에 따른 관련 서류**(거래 내용에 따라 추가 서류가 필요할 수 있습니다) |

- ⛔ **개별 서류명을 열거하지 않았다.** 증명서의 「위임장·대리인 신분증」, 부동산의
  「자금조달계획서·법인 관련 서류·토지거래허가 서류」 — 거래·신청 형태마다 달라지고
  무엇이 필요한지는 창구가 정한다. 마지막 한 줄이 그 모두를 덮는다.
- ⚠️ **허브 카탈로그(`index.html`)와 서식 config 양쪽에 같은 값**을 넣었다. 두 곳이 갈라지면
  Main 에서 본 준비물과 서식에서 본 준비물이 달라진다(실측으로 대조해 확인).
- 「신분증」의 보조설명은 **나머지 6종이 이미 쓰는 문장**을 그대로 썼다(새로 짓지 않았다).
  「부동산 거래계약서 사본」은 담당자가 준 보조설명이 없어 **비워 두었다** — 지어내지 않는다.
- 어디를: `forms/cert.config.js` · `forms/realestate.config.js` · `index.html`(카탈로그) ·
  2종 재빌드 · `kiosk-app/app/`.
- 확인: 서식 RAIL 과 허브 Main/Idle 이 **글자까지 동일**(2줄·3줄) ·
  허브에서 가로 넘침 0(Normal·크게 보기 둘 다) · 인쇄물 19쪽 0px.

### [확인] 주민등록번호 선택화 — **전수 재검사**  · 담당자

앞선 반영(⑩)이 빠짐없이 걸렸는지 **대표 화면이 아니라 8종의 모든 단계를 돌며** 확인했다.

- 정적 대조: 라벨에 「주민등록번호」가 있는데 `type:"jumin"` 이 아닌 칸 **0** ·
  `type:"jumin"` 인데 주민등록번호가 아닌 칸 **0** → 공통 처리의 **부작용 없음**.
  생년월일은 `type:"date"`, 개업공인중개사 등록번호(`*_regno`)는 일반 text 라 손대지 않았다.
- 실행 대조: 8종의 모든 단계를 열어 실제로 그려지는 `data-t="jumin"` 입력을 전부 모았다 —
  **41칸**(출생 5 · 혼인 11 · 이혼 9 · 사망 3 · 개명 3 · 증명서 3 · 부동산 4 · 여권 3).
  **필수 배지 0 · 진행 막힘 0 · 안내 문구 누락 0.**
- A(공란): 7종 모두 필수 미충족 **없음** → Review 표시 → 인쇄 준비 화면(총 장수 정상) →
  인쇄란 **전부 공란**.
- B(입력): 종전 좌표에 그대로 출력(혼인 16칸 · 이혼 14칸 · 증명서 6칸 · 출생 6칸 ·
  사망 4 · 개명 4 · 부동산 3). 여권은 적다 만 번호를 **여전히 막는다**.
- 다른 필수항목은 그대로다(성명을 비우면 막힌다).

---

## 2026.08.30 (3)

### [반영] 가족관계등록 5종 — **담당자 2차 검토사항**  · 담당자

⛔ **담당자 원문이 원본이다.** 기존 구현·문구와 부딪히면 담당자 쪽을 따랐다.
⛔ 담당자가 말하지 않은 것은 손대지 않았다 — 업무규칙 추가해석·서류 추가·새 조건부 질문·
   Main·Product UI·색·아이콘·모션·Review/미리보기 구조·다른 서식으로의 확대 적용 **전부 없음**.

**① 준비물** — 5종 모두 담당자 표기와 맞췄다.
- **혼인만 달랐다**: 「남편(부) 신분증 / 아내(처) 신분증 / 도장」 3줄 → **「당사자 신분증 / 도장」**
  2줄(이혼과 같은 표기). ⛔ 「당사자가 둘이니 두 줄이 낫다」는 판단으로 되돌리지 마라.
- 출생·사망·개명·이혼은 **이미 일치**해 그대로 두었다.

**② 출생 — 출생 장소는 주소까지만**(병원명 X)
- 도움말: 「태어난 곳의 주소. 병원이면 병원 이름까지 적으면 좋습니다.」
  → **「출생한 곳의 주소까지만 적어 주세요. 병원명은 적지 않습니다.」**
- **작성예시도 고쳤다**: `경기도 군포시 산본로 000 △△여성병원` → `경기도 군포시 산본로 000`.
  ⚠️ 예시가 곧 본보기다 — 안내문으로 아무리 막아도 예시에 시설명이 있으면 따라 적는다.
- ⛔ 데이터 구조·좌표는 그대로다. 「장소 구분(자택·병원·기타)」 영표도 그대로다.

**③ 사망 — 사망 장소(상세)는 주소까지만**(병원명·건물명 X)
- 도움말: 「돌아가신 곳의 이름이나 주소.」
  → **「돌아가신 곳의 주소까지만 적어 주세요. 병원명이나 건물명은 적지 않습니다.」**
- **작성예시도 고쳤다**: `○○대학교병원` → `경기도 군포시 산본로 000`.
  ⚠️ 「의료기관」은 장소 **구분**(영표)이라 그대로 뒀다 — 주소칸과 다른 것이다.

**④ 혼인 — ④ 성·본의 협의 설명**
- 「자녀의 성·본을 어머니의 성·본으로 하기로 협의했나요? **해당 없으면 ‘아니요’**.」
  → 「… 협의했나요? **어머니의 성·본으로 하는 경우 ‘예’, 아버지의 성·본으로 하는 경우
  ‘아니요’를 선택해 주세요.**」 종전 문구는 보통의 경우(부의 성·본)를 「해당 없음」처럼 읽혔다.
- ⛔ 서식 표현 「④ 성·본의 협의」는 **그대로 뒀다** — 없애라는 뜻이 아니었다.
- **매핑 확인**: 서식 원문이 「자녀의 성·본을 **모**의 성·본으로 하는 협의를 하였습니까?
  예□ 아니요□」이고 영표 좌표도 예=437.0 / 아니요=489.0 이다 →
  **예 = 모의 성·본 / 아니요 = 부의 성·본**이 정확히 유지된다(무변경).

**⑤ 개명** — 준비물이 이미 담당자 표기와 같아 **아무것도 바꾸지 않았다.**

**⑥ 이혼 5-1 — 법원명에 지원까지**
- 종전 안내가 지원을 **막고 있었다**: 「서식에 ‘법원’이 이미 인쇄돼 있으니 **앞부분만**
  적습니다. 예: 수원가정」 → **「지원까지 있으면 함께 적습니다. 예: 수원가정법원 안양지원」**
- 작성예시(재판상)도 `수원가정` → `수원가정법원 안양지원`.
- 확인: 길이 제한은 **원래 없다**(엔진 입력칸에 `maxlength` 없음). 인쇄도 칸을 넘지 않는다.
- ⛔ 지원 입력칸을 따로 만들지 않았다(한 칸에 전체 법원명).

**⑦ 이혼 5-2 — 친권자 지정 효력발생일을 다시 묻지 않는다**
- 종전에는 자녀마다 「효력 발생일」을 **직접 입력**받았다(필수). 이미 아는 날짜를 두 번 적게
  해, 한 장에 서로 어긋난 날짜가 찍힐 수 있었다.
- 이제 이혼 종류 하나에서 **그대로 나온다**(`effDateOf()`):
  **협의이혼 → 협의이혼 신고일(오늘) · 재판상 이혼 → ④ 재판확정일.**
- 화면에서는 묻지 않고 **무엇이 들어가는지 알려만 준다**(값과 근거 한 줄).
- `c*_effDate` 상태키·작성예시 값도 함께 걷어냈다 — 읽는 곳이 없어 남겨 두면 오해를 부른다.
- Review 에는 원래 날짜가 없어 **모순 표시 없음**(이혼 종류·자녀 수·친권자·출석만).

- 어디를: `forms/birth.config.js` · `forms/marriage.config.js` · `forms/death.config.js` ·
  `forms/divorce.config.js` · 해당 4종 재빌드 · `kiosk-app/app/`.
  ⛔ `naming` 은 손대지 않았다. ⛔ 엔진·좌표·다른 3종은 손대지 않았다.

- 확인(대표 경로만): 준비물 **5종 전부 일치** · 출생·사망 화면 안내가 「주소까지만」이고
  **시설명 유도 없음** · 혼인 설명 교체 + 매핑 무변경 · 이혼 재판상에서 법원명
  「수원가정법원 안양지원」이 **칸 안에 인쇄**됨 · 효력발생일 **입력칸 없음**(필수 미입력 0) ·
  인쇄 효력발생일 = 재판상 **2026.05.10**(=④ 재판확정일) / 협의 **2026.08.04**(=신고일) ·
  `verify-print` **19쪽 0px**(아래 기준선 갱신 후) · `--electron` 19쪽 0px ·
  `verify-review` 16 · `measure-screen` 15 · `verify-site-config` 6 · 블록 9화면 동일 ·
  `sync-kiosk --check` 통과 · `node --check` 5종.

- 📌 **인쇄 기준선 갱신** — 바뀐 쪽은 5개이고 전부 위 지시의 결과다:
  `birth-wed`·`birth-unwed`(출생장소 예시에서 병원명 삭제) ·
  `death-hospital`(사망장소 예시에서 병원명 삭제) ·
  `divorce-judicial`(법원명 전체 표기) · `divorce-consensual`(효력발생일 = 신고일).
  ⛔ 좌표는 **한 자리도 옮기지 않았다** — 값만 달라졌다.

- 📌 **보고한 세 가지 중 두 가지는 곧바로 지시가 와서 아래에 반영했다**(⑧⑨).
  나머지 하나(**엔진이 `placeholder` 를 화면에 내지 않는다** — `ph:` 가 7종에서 쓰이지 않고
  여권만 손작성이라 2개 있다)는 **확인만 받았고 고치지 않았다.** 시민에게 보이는 안내는
  **도움말과 작성예시**뿐이라는 뜻이다 — 이번에 `ph` 도 같이 고쳤지만 화면에는 드러나지 않는다.

**⑧ 이혼 — 인쇄된 「법원」 두 글자를 가린다**(담당자 지시)
- 서식은 「법원명 [____] **법원**」이라 앞부분만 적는 것을 전제한다. 그런데 실제 작성 형태가
  「수원가정법원 안양지원」이라, 그대로 두면 인쇄물이 「… 안양지원 **법원**」이 됐다.
- 껍데기에 이미 있던 `.cover`(원본 인쇄글자 가림)를 **엔진이 쓸 수 있게 통로만 열었다**
  (`FORM.covers(v,state)` → 흰 상자). 여권은 손작성본이라 같은 class 를 예전부터 쓰고 있었다.
- ⛔ **적은 값이 있을 때만** 가린다 — 비어 있는 협의이혼에서는 원본을 건드리지 않는다.
- ⚠️ 좌표는 원본 PDF 에서 **잉크를 실측**했다(글자 x 485.12~501.31 · y 376.44~384.81 /
  셀 안쪽 x 402~551 · y 373~391). ⛔ 넓히지 마라 — 격자선이나 옆 글자가 지워진다.
- ⛔ 관공서 서식의 인쇄물을 덮는 일이다. **담당자 요청이 있을 때만** 이 통로를 쓴다.

**⑨ 이혼·혼인 — 신고일 칸을 찍는다**(담당자 지시)
- 제목 아래 「( 년 월 일 )」 = 신고일. **8종 중 이 둘만 비워 둔 채 인쇄**되고 있었다.
- 협의이혼은 이 날짜가 곧 ⑤ 친권자 지정의 효력발생일이라(⑦), 한쪽만 찍히면 같은 날을
  뜻하는 두 칸이 종이 위에서 어긋나 보였다. 이제 **8종이 모두 신고일을 찍는다.**
- 좌표는 두 서식 원본에서 **잉크를 실측**했다 —
  이혼 「(」101.72·「년」143.50·「월」174.69·「일」206.38, 세로중앙 89.47 →
  `today:{y:89.5, yx:122.6, mx:159.1, dx:190.5}`
  혼인 「(」76.59·「년」127.22·「월」171.06·「일」215.19, 세로중앙 84.0 →
  `today:{y:84.0, yx:101.9, mx:149.1, dx:193.1}`
  (값은 글자 사이 빈 곳의 한가운데 — `.ov` 는 좌표를 **중심**으로 잡는다.)
- 확인: 두 서식 모두 「( 2026 년 8 월 4 일)」로 칸에 맞게 찍힌다.

**⑩ 8종 공통 — 주민등록번호는 「선택」이다**(담당자)
- 여기는 **공용 키오스크**다. 지나가는 사람이 보는 화면에 주민등록번호를 적기 꺼려지는 것은
  당연하고, 그 사람에게 길이 없으면 도우미를 통째로 포기하게 된다. 여권이 이미 주던 선택권
  (「인쇄한 뒤 직접 쓰겠습니다」)을 **8종 전부**로 넓혔다.
- 화면: 엔진의 `inputHtml` 한 곳에서 `type:"jumin"` 이면 **언제나 「선택」 배지**를 달고
  아래 한 줄을 붙인다 — 「공용 화면에 적기 불편하시면 비워 두세요. 그 칸은 빈 채로 인쇄되고,
  인쇄한 뒤 직접 적으셔도 됩니다.」 ⛔ 서식마다 다른 말을 짓지 마라. 문구는 `JUMIN_HINT` 하나다.
  ⛔ config 가 `req:true` 라 적어 두어도 엔진이 **선택으로 낸다**(서른 몇 군데가 제각각
  되돌아가지 않게 한 곳에서 막았다).
- 진행: 각 단계 `required()` 에서 주민등록번호를 뺐다 — **15줄**(출생 3 · 사망 2 · 이혼 4 ·
  혼인 4 · 개명 2). 뺀 자리마다 「다시 넣지 마라」 표시를 남겼다.
- 여권: 본인 주민등록번호를 `req:true` → **`req:false`**. 종전에는 카드에서 「인쇄한 뒤 직접
  쓰겠습니다」를 골라야만 빈 채로 넘어갔다. 이제 「여기에 입력하겠습니다」인 채로 비워 두어도
  막지 않는다 — 카드는 **안내**로 남는다. (법정대리인·공동친권자 번호는 원래 선택이었다.)
- ⛔ **바꾸지 않은 것**: 인쇄 좌표·마스킹·출력 방식 · 형식검증(여권은 적다 만 번호 1~12자리를
  **여전히 막는다**) · 주민등록번호 **외** 다른 필수항목 · 생년월일·법인/사업자등록번호 ·
  대신할 새 입력항목(만들지 않았다).
- 확인: 작성예시를 채운 뒤 **주민등록번호 계열 값을 전부 지우고** 모든 단계의 `required()` 를
  돌렸다 — 8종 10경로에서 **막히는 곳 0** · 인쇄값 **전부 공란** · 안내 문구 표시 확인.
  여권도 두 경로에서 막힘 0 · 인쇄 공란. 값을 넣으면 종전 좌표에 그대로 찍힌다(혼인 16칸·
  사망 4칸). 성명을 비우면 **여전히 막힌다**(다른 필수는 영향 없음).
- 📌 **한 곳만 예외로 남았다** — 증명서 ② 의 「**등록기준지 또는 주민등록번호**」.
  이것은 주민등록번호를 요구하는 규칙이 아니라 **등록기준지의 대체 수단**이라, 주민등록번호만
  비우면 막히지 않는다(실측 확인). 여기서 주민등록번호를 빼면 **등록기준지가 필수가 되어**
  「다른 필수항목의 규칙은 바꾸지 않는다」와 어긋나므로 **그대로 두었다.**
  둘 다 비면 종전과 똑같이 막힌다(규칙 무변경).
- 어디를: `engine/engine.js`(`JUMIN_HINT`·`inputHtml`) · `forms/birth·death·divorce·
  marriage·naming.config.js`(필수 목록) · `passport-helper-v1.html` · 7종 재빌드 · `kiosk-app/app/`.
- 인쇄물 **19쪽 0px**(화면만 바뀌었다).

- 📌 **인쇄 기준선 2차 갱신** — ⑧⑨ 로 4쪽이 더 바뀌었다:
  `marriage-adult`·`marriage-minor`(신고일) · `divorce-consensual`(신고일) ·
  `divorce-judicial`(신고일 + 「법원」 가림). 나머지 15쪽은 **0px** —
  엔진에 `covers` 통로를 열었지만 그것을 쓰는 서식은 이혼뿐이다.

---

## 2026.08.30 (2)

### [반영] **Idle Motion v2** — 대기 중인 Main 이 스스로 제품을 설명한다  · 이식 지시서

⛔ **Main 의 구조·레이아웃·색·타이포는 한 글자도 바꾸지 않았다.** 이번 작업은 **모션과
리듬**뿐이고, 새로 넣은 규칙은 **전부 `html.demo` 안에서만** 산다 — 사람이 화면을 만지는
순간 그 class 가 떨어져 실제 작성 화면은 종전 그대로 조용하다.

**바뀐 값**

| 무엇 | 종전 | 지금 |
|---|---|---|
| 시연으로 돌아가는 대기 | 3분(환경설정 `idleMs` 따라감) | **1분**(고정 · 환경설정과 분리) |
| 한 민원에 머무는 시간 | 8000ms | **4500ms** (8종 한 바퀴 36초) |
| LEFT 선택 표시 | 카드마다 켜고 끔(점멸) | **블록 하나가 다음 카드로 미끄러짐** 240ms |
| 전환 | 모두 동시 | LEFT 240ms → CENTER 퇴장 160ms → **다 사라진 뒤 170ms 에** 갈아 끼움 → 등장 280ms |
| CENTER 내부 | 통째로 0.2초 페이드 | 제목 40 → 준비물 120 → 신청서 200 → 시작하기 280 → 안내 340ms (각 340ms) |
| CTA | 없음 | 620ms 에 한 번만 (600ms · scale 1.02 + ring) |
| 소개 | 없음 | Idle 진입 때 **한 번만** 1400ms |

- **소개**(진입 시 1회) — 「복잡한 민원서식, 질문에 답하면 자동으로 작성해 드립니다.」
  ⛔ 별도 대기화면이 아니다. **CENTER 위에만** 덮이고 LEFT·RIGHT 는 그대로 보인다.
  ⛔ 순환마다 되풀이하지 않는다(실측: 40초 동안 **1회**).
  기관 이름은 BAR 의 글자를 그대로 가져다 쓴다 — 지어내지 않는다.
- **각 민원의 한 줄 설명**(`IDLE_TAGLINE`) — 「무엇을 해 드리는가」를 Idle 에서만 말한다.
  ⛔ **새 행정정보·준비물·업무요건을 만들지 않았다.** 카탈로그의 `desc`·`ready` 가 이미
  말하는 것을 홍보 문장으로 다시 쓴 것뿐이고, 표에 없는 서식은 종전 문구가 그대로 쓰인다.
- **준비물은 검증된 `ready` 그대로.** 증명서·부동산은 비어 있으므로 그 격자를 내지 않는다
  (칸을 채우려고 지어내지 않는다). 체크 배지 같은 새 요소는 **넣지 않았다** — 지시서에서
  「허용」이지 필수가 아니었고, 넣으려면 CENTER 마크업이 늘어난다.
- ⛔ carousel · bounce · 무한 pulse · flash · gradient · 떠다니는 아이콘 · RIGHT 애니메이션
  **없음.** 모든 연출은 한 번 일어나고 멎는다(`both`). 등장이 0.68초에 끝나 **4.5초 중
  3.8초는 정지 상태**다 — 읽을 시간이 남는다.

- 🐞 **LEFT 선택이 「옮겨 가지」 않고 점멸하고 있었다.** `selectForm()` 이 고를 때마다
  `renderCards()` 로 목록을 통째로 다시 그려, **새로 만든 카드에는 전환이 걸리지 않았다**
  (게다가 4.5초마다 카드 여덟 벌을 버렸다). `markSelected()` 가 class 만 바꾸도록 갈랐다 —
  실측 **목록 DOM 재생성 0회**. 사람이 누르는 길은 그대로 즉시다(모션 없음).
- ⚠️ **`#ready > *` 가 stagger 를 통째로 이기고 있었다.** 위쪽에 `#ready > *{animation:
  m-ready-in …}` 이 있는데 **ID 선택자라 class 만으로는 못 이긴다** — 규칙은 멀쩡한데
  연출이 하나도 걸리지 않았다. 새 규칙에도 `#ready` 를 붙여 바로잡았다.
- ⚠️ **`stopDemo()` 에서 CENTER 를 다시 그리면 사용자의 첫 누름이 삼켜진다.** 이 함수는
  `pointerdown` 에 실려 도는데, 그 순간 눌린 단추가 DOM 에서 사라지면 브라우저는
  mousedown·mouseup 의 **공통 조상**에 `click` 을 던져 `closest("[data-go]")` 가 빈손이 된다.
  대신 **LEFT 를 되돌려** 맞춘다(`markSelected()` 는 class 만 바꾼다).
  실측: 전환 한가운데에서 눌러도 **[작성 시작하기]가 그대로 동작**했다(삼켜짐 없음).

- 어디를: `index.html` (Idle 모션 CSS 블록 · `markSelected`·`demoSelect`·`IDLE_TAGLINE` ·
  소개 덮개 마크업) · `kiosk-app/app/index.html`.
  ⛔ 서식 8종·엔진·좌표는 건드리지 않았다.

- 확인(지시서 §13 그대로): 순환 **1490 → 5990 → 10490 → 14990ms**(간격 4500ms 정확) ·
  LEFT 가 먼저 움직이고 CENTER 가 **110ms 뒤** 따라옴 · 목록 DOM 재생성 **0회** ·
  stagger·CTA 실측(40/120/200/280/340ms · CTA 620ms) · 준비물 없는 서식(증명서)에서도
  순서 정상 · 소개 **1회** · **첫 입력 삼켜짐 없음** · 멈춘 뒤 화면·시계 정리
  (살아 있는 시계 **0**, 정상 상태 1개·전환 창 순간만 2개, `startDemo()` 를 세 번 겹쳐
  불러도 하나) · `prefers-reduced-motion` 에서 **순회 자체가 시작되지 않음**(CENTER 무변화) ·
  `verify-print` **19쪽 0px** · `verify-review` 16 · `measure-screen` 15(가로 넘침 0) ·
  `verify-site-config` 6 · 블록 9화면 동일 · `sync-kiosk --check` 통과.
- 📌 사람이 만진 직후에도 **Idle 한 줄 설명이 CENTER 에 잠깐 남는다**(다음 선택에서 원래
  문구로 돌아온다). 그 자리에서 다시 그리면 위의 「첫 누름 삼킴」이 생기므로 일부러 두었다.
- **서식에서 무동작으로 되돌아오면 허브는 기다리지 않는다.** 서식이 `index.html?from=idle`
  로 보내고, 허브가 그것을 보면 **도착 즉시** 시연을 시작한다.
  ⚠️ 두 시계가 **줄줄이 붙어 있었다** — 서식 3분(무입력 2:30 + 경고 30초) 뒤 허브에 도착해
  거기서 또 1분. 사람이 떠난 뒤 키오스크가 **도합 4분**을 멈춰 있었다. 서식에서 30초 경고까지
  지나쳤다면 떠난 것이 확실하므로 더 기다릴 이유가 없다.
  ⛔ **인쇄 후 복귀(`goHome(false)`)에는 붙이지 않는다** — 그 사람은 아직 종이를 집으며
  화면 앞에 서 있다. ⛔ 사람이 직접 누르는 [메인으로](`href="index.html"`)에도 붙지 않는다 —
  방금 누른 화면이 갑자기 움직이면 안 된다.
  확인: `?from=idle` → **0.1초**에 시연 시작 · 표시 없이 열면 **60.1초** ·
  8종 전부 무동작 복귀는 `goHome(true)`, 인쇄 후 복귀는 `goHome(false)` 로 갈라져 있다.

- **대기 1분** — 아무 조작이 없으면 **1분** 뒤 시연으로 돌아간다(종전 3분).
  ⚠️ 종전에는 키오스크 환경설정의 `idleMs` 를 그대로 따라갔는데, **갈라 두었다.**
  두 시계는 하는 일이 다르다 — `idleMs` 는 **보호**(서식 8종에서 반쯤 적다 만 개인정보를
  지운다. 짧게 잡으면 생각하는 사이에 적은 것이 날아간다), 이쪽은 **대기로 돌아가기**
  (허브에는 지울 개인정보가 없고, 만지는 순간 멎으며 첫 누름도 삼키지 않는다).
  ⛔ 환경설정 「무동작이 이만큼 지나면」은 **그대로 3분**이다. 그것을 1분으로 줄이지 마라.
  확인: 가만히 두면 **60.2초**에 시작 · 누르면 멎고 **60초** 뒤 다시 시작 ·
  `idleMs=10분`을 줘도 허브는 1분(분리 확인) · 서식 8종은 종전대로 `idleMs`(기본 3분)를 쓴다.

- **LEFT 이동 블록** — 선택 표시를 카드에서 떼어 **움직이는 블록 하나**에 맡겼다.
  ⚠️ 블록은 카드 **위**에 얹는다(`z-index:2`). 아래에 두면 지나가는 길의 카드들이 불투명한
  `#fafafa` 라 **블록이 중간에 사라졌다 나타난다.** 6% 투명이라 지나가는 글자는 그대로 읽힌다.
  ⚠️ 고른 카드는 제 배경·테두리를 **블록에 넘긴다**(투명). 두 겹으로 짙어지지 않게 —
  결과 색은 종전 「고른 카드」와 같다. ⛔ **고르지 않은 카드는 손대지 않았다.**
  ⚠️ 자리·높이는 JS 가 `--sel-y`·`--sel-h` 로 넘긴다. 카드 높이는 글자 길이와 「크게 보기」에
  따라 달라지므로 **잴 때마다 다시 읽는다**(크게 보기·창 크기 변화에서도 다시 맞춘다).
  ⛔ 기본은 `display:none` — 사람이 쓰는 화면에는 없는 것과 같다. `pointer-events:none` 이라
  카드 누름을 가로채지 않는다.
  확인: 8칸 전부에서 블록 자리가 카드 자리와 **정확히 일치**(sel-y = wrapTop:
  0·75·151·226·301px) · 데모/일반의 선택 행 픽셀 비교에서 다른 곳은 **의도한 아이콘 1.04배와
  6% 베일뿐**(468×70 중 439px, 1.3%) · 멈추면 블록이 사라지고 카드가 제 배경을 되찾는다.

---

## 2026.08.30

### [반영] 공통 마감 ② — **Review 정리 · 미리보기를 「인쇄 준비 화면」으로**  · 실사용 QA(U8)

8종을 **한 번에** 손봤다(AGENTS.md §5-1: 서식 하나에서 고치면 다시 갈라진다).
⛔ 업무규칙·인쇄 좌표·질문 흐름은 **한 글자도 건드리지 않았다** — 인쇄물 19쪽 0px.

**① Review(마지막 확인)는 「고르신 것」만 보여 준다.**
종전 요약은 이름·주민등록번호·주소·전화까지 전부 되풀이해 스크롤이 필요했다.
그 값들은 **가운데 신청서가 이미 실시간으로 보여 준다** — 같은 것을 두 번 읽힐 이유가 없다.
이제 시민이 **고른 것**(성별·이혼 종류·사망 장소 구분·증명서 종류 …)만 남기고,
빈 값은 **줄 자체를 내지 않는다**(엔진에 `sumRowIf` 추가).
- 실측: 여권 최종확인 화면의 세로 넘침이 **646px → 0** (크게 보기 928 → 83,
  미성년 2쪽 1233 → 0). 스크롤 없이 한 눈에 들어온다.
- 어디를 보면 전체가 있는지 한 줄로 알려 준다(「적으신 값 전체는 옆의 신청서에 …」).
  ⛔ 서식마다 다른 말을 짓지 않았다 — 엔진이 `kind:"summary"` 에 한 문장을 낸다.
- 각 줄에 **[수정]** — 그 선택을 한 단계로 되돌아간다(여권이 쓰던 문법을 엔진에도).
  ⛔ 모든 줄에 기계적으로 달지 않았다. **시민이 고른 값**에만 붙인다 —
  생년월일에서 저절로 갈리는 값(혼인 ⑧ 동의 필요)은 되돌아갈 선택이 없다.

**② 미리보기 = 인쇄 준비 화면.**
종전에는 서식을 그냥 크게 띄우기만 해서 **몇 장이 나오는지 알 수 없었다**(부동산은 2장이다).
이제 `총 N장 → 축소 썸네일 → 인쇄 후 할 일 → 참고 → [N장 인쇄하기]` 순이다.
- 장수는 **재서 센다** — 별지는 내용이 늘면 두 장이 되므로 서식별로 적어 두지 않는다.
- 썸네일은 **실제 인쇄에 나가는 그 DOM 을 복제**한다(미리보기 전용 렌더러를 만들지 않았다).
- 「인쇄한 뒤에 하실 일」은 **새로 지은 문구가 아니다** — 완료 화면에 있던 확정 문구를
  `FORM.afterPrint`/`refInfo` 로 옮겼을 뿐이다. 없는 서식은 그 구역이 비어 사라진다.
- 모달 배경을 `.6` → `.94` 로. 종전엔 뒤 화면이 비쳐 이 글이 읽히지 않았다.

- 어디를: `engine/engine.js`(`sumRowIf`·`sumRow(step)`·`renderPreview`) ·
  `engine/base-product.html`(미리보기 조형) · `forms/*.config.js` **7종** ·
  `passport-helper-v1.html`(손작성본에 같은 내용) · 엔진 7종 재빌드 · `kiosk-app/app/`.

- ⚠️ **함정 하나** — `.sum-row` 골격을 아래쪽에서 2열로 덮고 있어 세 번째 칸([수정])이
  **다음 줄로 떨어졌다.** 위쪽(여권에서 뜬 Product UI 값)에 `150px minmax(0,1fr) auto`
  가 이미 있다. 아래쪽에는 엔진 전용 `.val` 색만 남겼다.

- ⚠️ **혼인만 [수정]이 0개였다**(8종 일관성 누락). ④성·본·⑤근친혼 → 6단계,
  ⑨출석 → 8단계로 붙여 8종을 같게 맞췄다. 인쇄물 무변경.

- 🐞 **「총 N장」이 거짓말을 하고 있었다 — 고쳤다**(`docs/GOTCHAS.md ㉟`).
  장수를 **화면의 종이**에서 재고 있었는데, 화면의 종이는 ① 창에 맞춰 줄어 있고
  (1366px 창에서 720px, 210mm=794px 가 아니다) ② 별지 여백이 인쇄와 다르다
  (화면 `14mm 12mm` · 인쇄 `12mm 10mm`). 좁은 폭에서는 글이 더 접혀 세로가 길어진다.
  **실측**: 부동산 별지 최대(4명/건)에서 화면 **1.134쪽 → 2장**, 실제 인쇄 **1장** —
  미리보기가 「총 3장」·[3장 인쇄하기]라 해 놓고 **2장이 나왔다.**
  겉으로 드러난 유일한 증상은 **세 번째 썸네일이 백지**였던 것뿐이다.
  → 화면 밖 `.pv-measure`(**폭 210mm · 인쇄 여백**)에 복제해 펴 놓고 잰다.
    인쇄 여백은 `--xpad-print` **한 곳에만** 적어 두 벌이 되지 않게 했다.
  → 확인: 별지 추가 **0~4 전부** 미리보기 = 실제 인쇄(1·2·2·2·2장). 종전엔 4명/건에서
    3 ≠ 2 로 어긋났다. 인쇄물 19쪽 0px **무변경**(재는 방법만 바뀌었다).
  → 엔진과 여권 **양쪽에** 넣었다(㉒).

- 확인: `verify-print` **19쪽 0px** · `--electron` **19쪽 0px**(용지 210.23×297.35mm) ·
  **인쇄 기준선 무변경** · 8종×2예시 **16건**에서 미리보기 실측(총 장수가 인쇄 기준선 쪽수와
  **16건 전부 일치** · 썸네일 개수·내용 채움·버튼 라벨) · Review [수정] 단추 **16건 전부**
  의도한 단계로 이동(죽은 단추 0) · `measure-screen` 15화면 통과(가로 넘침 0 · 종이 유지) ·
  `verify-site-config` 6 · 블록 9화면 동일 · `sync-kiosk --check` 통과 · `node --check` 8파일.
- 📌 `tests/screen-baseline.json` **갱신** — 위 여권 최종확인 3화면의 세로 넘침이
  사라진 것이 유일한 차이다(가로 넘침·종이 크기는 그대로).
- 📌 남은 QA 항목은 그대로다 — W1~W10(업무·담당자 확인) · U6 증명서 단계 분할 ·
  U7 출생 단계 분할 · U9 `state.unsure`/Handoff · U10 소요시간 · P1~P3(색·정렬·인상).
  공통 마감 ③ 색 · ④ 아이콘 · ⑤ 모션이 다음이다.

### [추가] `tools/verify-review.py` — Review·인쇄 준비 화면의 **사각지대**를 메운다

위 두 화면은 **어떤 도구도 보지 않았다.** 인쇄 CSS 가 `.modal-back` 을 숨기므로
`verify-print.py` 는 미리보기를 못 보고, `measure-screen.py` 는 모달을 열지도
[수정]을 누르지도 않는다 — **화면이 통째로 죽어도 19쪽 0px 가 그대로 통과한다.**

- 무엇을: 8종×2예시(16건)에서 ① 「총 N장」이 인쇄 기준선 쪽수와 맞는지(썸네일 개수·
  내용 채움·단추 라벨 포함) ② Review 의 [수정]을 **실제로 눌러** 그 단계로 가는지 잰다.
- 왜: `gotoStep(n)` 은 `n < state.step && stepActive(n)` 일 때만 움직인다. 번호를 잘못 달면
  **눈에는 멀쩡한 죽은 단추**가 된다. 위 혼인 누락이 바로 이 도구로 잡혔다.
- 확인: 8종 16건 통과 · **자체 시험** — 혼인 ⑨출석의 목표 단계를 일부러 10(Review 자신)으로
  바꿔 두 건 모두 「죽은 [수정] 1개 (⑨ 출석)」로 잡히는 것을 보고 되돌렸다.
- 어디를: `tools/verify-review.py`(신규) · `AGENTS.md` §2 검증 목록에 등록 ·
  `docs/GOTCHAS.md ㉟`(장수를 화면에서 재면 안 되는 이유).

---

## 2026.08.29 (13)

### [반영] 실사용 QA 1차 — **명백한 UX 문제 정리**(업무판단 없음)  · 실사용 QA

8종을 빈 상태로 직접 조작해 나온 문제 중 **업무판단이 필요 없는 다섯 가지**만 고쳤다.
⛔ 업무내용(W1~W10)·Review·`state.unsure`·단계 분할·색/아이콘/모션은 손대지 않았다.

- **U1 여권 — 첫 화면에도 진행 표시가 나온다.** 안내 기둥이 경로(본인·미성년·대리)를
  고르기 **전에는 통째로 비어** 있어, 나머지 7종과 달리 여권만 어디쯤 왔는지 알 수 없었다.
  이제 같은 진행 표시를 첫 화면부터 낸다.
  ⛔ **분모를 지어내지 않았다** — 경로에 따라 15~16 화면으로 갈리므로 고르기 전에는
  `/ N` 과 막대 채움을 내지 않는다(`flow()` 가 그때 길이 1 이라 그대로 쓰면
  「1 / 1 · 100%」로 **다 끝난 것처럼** 보인다). 읽어 주는 말도 분모를 말하지 않는다.
  ⛔ `flow()`·`stages()`·`gotoStep` 계산은 손대지 않았다. 표시만 바뀐다
  (실측: 첫 화면 `1 · 0%` → 성인 4 `4/15 · 27%` → 미성년 11 `11/16 · 69%` → 대리 16 `16/16 · 100%`).

- **U2 혼인 — 단계 제목이 화면에 없는 항목을 말하지 않는다.** 종전 「⑨⑩⑧ 출석·제출·동의」는
  번호가 뒤섞였고 **미성년이 아니면 ⑧ 동의가 화면에 없는데** 제목에는 남아 있었다.
  이제 「출석 여부 · 제출인」/「… · 미성년자 동의」로 갈린다. 번호는 각 항목 라벨이 이미 달고 있다.

- **U3 이혼 — 같은 문제를 같은 방법으로.** 「③④ 이혼 종류·재판확정일자·기타사항」이
  협의이혼에서도 ④를 말하고 있었다 → 「이혼 종류 · 기타사항」/「… · 재판확정일자 · …」.

- ⚠️ U2·U3 을 위해 **엔진이 `title` 을 함수로도 받게 했다**(한 줄).
  ⛔ `short`(안내 기둥의 묶음 이름)는 고정이다 — 진행 표시가 흔들리면 안 된다.

- **U4 혼인 — 6단계 항목 순서를 서식 번호 순(③④⑤⑥)으로** 바로잡았다.
  종전 ④ → ⑤ → ③ → ⑥ 이라 종이와 눈이 어긋났다.
  ⛔ **순서만** 바꿨다 — state·검증·인쇄 좌표는 그대로다(인쇄물 0px).

- **U5 부동산 — 지번주소 안내가 두 번 나오던 것을 한 번으로.** 경고 상자와 ⑥ 소재지 칸의
  도움말이 같은 말을 하고 있었다. 경고는 상자에서만 하고, 도움말에는 상자가 말하지 않는 것
  (①② 주소란과 다르다는 점)만 남겼다. ⛔ 업무 내용은 그대로다.

- 어디를: `passport-helper-v1.html` · `engine/engine.js`(제목 함수 지원) ·
  `forms/marriage.config.js` · `forms/divorce.config.js` · `forms/realestate.config.js` ·
  엔진 **7종 재빌드** · `kiosk-app/app/`.

- 확인: `verify-print` **19쪽 0px** · `--electron` **19쪽 0px**(용지 210.23×297.35mm) ·
  **기준선 무변경** · 엔진 7종 **대표 흐름 14건 완주**(각 2예시) · 여권 4개 상태 진행 표시 실측 ·
  Normal/Big 확인(제목 두 줄 접힘 · 어절 중간 줄바꿈 0 · 가로 넘침 0) ·
  `measure-screen` 15 · `verify-site-config` 6 · 블록 9화면 동일 · `sync-kiosk --check` 통과.

- 📌 **남은 QA 항목은 그대로 열려 있다** — W1~W10(업무·담당자 확인) · U6 증명서 단계 분할 ·
  U7 출생 단계 분할 · U8 Review 정리 · U9 `state.unsure`/Handoff · U10 소요시간 ·
  P1~P3(색·정렬·인상). 공통 마감 단계에서 처리한다.

---

## 2026.08.29 (12)

### 📌 **기준점 — Product UI v1 · 8종 이식 완료** (커밋 `d4c5960`)

허브 · 여권 · 출생 · 혼인 · 이혼 · 사망 · 개명 · 증명서 · 부동산 **전부** Product UI v1 이다.
손작성본은 **여권 하나**뿐이고, 엔진 7종은 모두 `engine/base-product.html` 을 쓴다
(`engine/base.html` 은 **쓰는 서식이 없다** — 대조용).

**이 상태에서 전체 검증을 한 번에 돌려 확인했다**(2026.08.29)

| 검사 | 결과 |
|---|---|
| `verify-print.py` | **19쪽 0px** |
| `verify-print.py --electron` | **19쪽 0px** · 용지 210.23 × 297.35 mm |
| 재빌드 드리프트 | 엔진 7종 재빌드 결과가 **커밋본과 동일** |
| `sync-kiosk.sh --check` | 9개 파일 동기화됨 |
| `check-site-block.py` · `check-design-tokens.py` | 9개 화면 동일 |
| `verify-site-config.py` | 시나리오 6 통과 |
| `measure-screen.py` | 15화면 · 가로 넘침 0 · 종이 794×1122 |
| `make-form-previews.py --check` | 미리보기 8종 |
| `node --check` | 엔진·config 7·`build-form`·kiosk-app 3 |

⛔ **여기서부터는 개별 서식을 다시 열지 않는다.** 남은 일은 **공통 마감 단계**이고,
한 서식이 아니라 **8종에 걸쳐 한 번에** 처리한다.

| 공통 마감 단계 | 무엇 |
|---|---|
| ① 실사용 QA | 키오스크에서 8종을 실제로 돌려 본다(3분 유휴·인쇄 후 초기화·지정 프린터 포함) |
| ② Review / 인쇄 미리보기 단순화 | 마지막 확인 화면과 미리보기 구조를 8종 공통으로 |
| ③ Color & Visual Polish | BAR tint · primary/secondary 위계 · 미리보기 강조도 · 색 농도 |
| ④ Icon Polish | 목록·RAIL·준비물 아이콘 한 벌 |
| ⑤ Motion | 전환·눌림 반응 |

📌 비차단 잔재는 **지금 고치지 않고 TODO 로만** 남긴다.
⚠️ 예외는 **실제 구현 결함**뿐이다 — 값이 인쇄에 안 실림 · 필수 우회 · 조건 변경 뒤 잔상 ·
JS 오류 · 인쇄 좌표/쪽수 · 화면 넘침 · 개인정보 초기화 · 다른 서식 회귀.

### 📌 열려 있는 TODO (지금 고치지 않는다)

**담당자 확인 대기**
- 이혼 「당사자 신분증」 — 실제 접수에 **오는 사람의 신분증만** 필요한지
- 이혼 「협의서 원본」 — 담당자가 뜻한 **정확한 서류명**
- 혼인 준비물이 **남편·아내 두 줄**로 갈라져 있다(원문은 「당사자 신분증」 한 줄)
- 혼인·이혼은 제목 아래 **신고일을 인쇄하지 않는다**(출생·사망·개명은 찍는다)
- 증명서·부동산은 담당자가 준 준비물 목록이 없어 `ready` 를 **비워 두었다**

**코드 잔재(비차단)**
- `#askFoot`(Human Handoff 자리)이 **비어 있다** — 무엇을 어느 화면에 둘지는 업무 판단이다
- `state.unsure`(「이 단계는 잘 모르겠어요」)는 **적히기만 하고 아무도 읽지 않는다** —
  창구로 전달되지 않으므로 이 기능은 완성된 것이 아니다
- `FORM.rerenderOnSet` 은 **하는 일이 없다**(엔진이 언제나 `renderAll()` 한다).
  5종 config 에 남아 있어 지우려면 함께 봐야 한다
- `preview.fields`·`preview.sample`(허브 카탈로그)은 **Main 이 쓰지 않는다** — 종이 미리보기
  시연 전용이었고 그 코드는 사용 중단 상태다
- `engine/base.html` 은 **쓰는 서식이 없다.** 지워도 되는지는 공통 마감 단계에서 판단한다
- 시범운영 중이라 **운영문서 PDF·Electron 설치본은 재생성을 멈춰 두었다**

---

## 2026.08.29 (11)

### [완료] 부동산 거래신고 — **Product UI v1 이식 · 별지 조판 누락 수정** (8종 이식 완료)  · 사용자 지시

- 무엇을: Product UI v1 껍데기를 옮겼다. **이로써 8종 전부 Product UI v1 이 됐다.**
  ⚠️ **전수분석은 하지 않았다.**

- **서식은 그대로다** — `서식원본/부동산거래계약 신고서.pdf` 가 현행이고 개정본이 없다.
  좌표를 한 자리도 건드리지 않았고 **인쇄물도 무변경**이다(기준선 갱신 없음).

- 🐞 **별지(2쪽) 조판이 어긋난 것을 잡았다.** 새 껍데기(`base-product.html`)에
  **`.xpaper` 규칙이 통째로 빠져 있었다** — 여백(화면 14mm/12mm · 인쇄 12mm/10mm)과
  쪽 나눔, 화면의 점선 구분선이 옛 껍데기에만 있었다. 별지를 쓰는 서식이 부동산 하나뿐이라
  여섯 서식을 옮기는 동안 드러나지 않았다.
  ⚠️ **화면은 멀쩡하고 별지 2쪽만** 밀렸다(`verify-print` 가 194,677px 로 잡았다).
  옛 껍데기의 규칙을 **글자까지 같게** 옮겨 **19쪽 0px** 로 되돌렸다.

- ⛔ **가족관계등록 신고서의 공통 필수/선택 원칙을 적용하지 않았다** — 이 서식은 신고서가
  아니다. 필수 조건은 **기존 업무규칙 그대로**(매도인·매수인 성명/주소 · 거래대상 종류 ·
  소재지 · 총 실제거래가격 · 계약 체결일)이고 새로 보태지 않았다.
- ⛔ **준비물도 비워 두었다** — 담당자가 준 목록이 없다.
  ⚠️ 다만 종전 **시작 화면**이 안내하던 「거래계약서·신분증·계약금 지급 확인 서류·
  인터넷 신고(rtms.molit.go.kr)」 문장은 **없애지 않고 ① 매도인 단계의 안내 상자로 옮겼다** —
  시작 화면을 숨기면 그 안내를 아무도 보지 못하기 때문이다.
  ⛔ 그 문장을 안내 기둥의 `ready` 로 승격하지 마라. 화면 안내이지 확정된 준비물이 아니다.
  30일·과태료 경고도 같은 이유로 ① 단계의 `why` 로 옮겼다.

- 📌 **`engine/base.html` 은 이제 쓰는 서식이 없다.** 옛 껍데기이고 대조용으로만 남는다 —
  그것을 고쳐도 배포물은 바뀌지 않는다(문서 세 곳에 적어 두었다).

- 어디를: `forms/realestate.config.js` · **`engine/base-product.html`(별지 조판 복구)** ·
  엔진 **7종 재빌드** · `kiosk-app/app/` · `AGENTS.md`·`engine/README.md`·
  `docs/UIUX_HANDOFF.md`·`tools/design-shots.py`.

- 확인: `verify-print` **19쪽 0px** · `--electron` **19쪽 0px** · **기준선 무변경** ·
  아파트·입주권 두 예시 **11단계 완주**(2→12) · **별지 2쪽 유지** · 빈 상태 필수 차단 5단계 ·
  준비물 구역 안 나옴 · Normal/Big 가로 넘침 0 · state **245개** ·
  `measure-screen` 15 · `verify-site-config` 6 · 블록 9화면 동일 · `sync-kiosk --check` 통과.

---

## 2026.08.29 (10)

### [완료] 증명서 발급신청 — **Product UI v1 이식**(좌표·업무규칙 무변경)  · 사용자 지시

- 무엇을: Product UI v1 껍데기만 옮겼다. ⚠️ **전수분석은 하지 않았다.**

- **서식은 그대로다** — `서식원본/[별지 제11호 서식]…pdf` 가 현행이고 개정본이 없다.
  그래서 **좌표를 한 자리도 건드리지 않았고 인쇄물도 무변경**이다(`verify-print` 2쪽 0px).

- ⛔ **가족관계등록 신고서의 공통 필수/선택 원칙을 적용하지 않았다.**
  「모든 사항 필수 · 등록기준지·본·한자만 선택 · 신고인란 필수」는 담당자가
  **신고서에 한정해** 준 요구사항이다. 이 서식은 신고서가 아니므로 **기존 업무규칙 그대로** 두고
  새 필수 조건을 추정해 보태지 않았다(필수는 종전과 같이 대상자 성명 ·
  「등록기준지 또는 주민등록번호」 · 신청인 성명 셋뿐이다).

- ⛔ **준비물도 비워 두었다.** 담당자가 준 목록이 없다 — 없으면 그 구역을 아예 내지 않는다.
  「신분증이 필요할 것이다」 같은 짐작으로 채우지 않았다.

- **Product UI v1 이식** — `shell:"product"` · ASK 26 / PAPER 56 / RAIL 18 · BAR 62px ·
  RAIL(진행 → 안심 → 이용 안내) · Normal/Big. 시작 화면은 `when` 으로 숨겼다.

- 어디를: `forms/cert.config.js` · `cert-helper-v1.html`(재빌드) · `kiosk-app/app/` ·
  `AGENTS.md`·`engine/README.md`·`docs/UIUX_HANDOFF.md`·`tools/design-shots.py`.

- 확인: `verify-print` **19쪽 0px** · `--electron` **19쪽 0px** · **기준선 무변경** ·
  본인·대리 두 예시 **6단계 완주**(2→7) · 빈 상태 필수 차단(2/1건) · 준비물 구역 안 나옴 ·
  가장 빽빽한 4단계를 Normal/Big 으로 확인(가로 넘침 0) ·
  `measure-screen` 15 · `verify-site-config` 6 · 블록 9화면 동일 · `sync-kiosk --check` 통과.

- 📌 남은 Product UI 이식은 **부동산** 하나다.

---

## 2026.08.29 (9)

### [완료] 개명신고 — **신서식 교체 · 좌표 전면 재확립 · Product UI v1 이식**  · 담당자 요구사항 확정본

- 무엇을: 확립한 양산 방식 그대로. ⚠️ **전수분석은 하지 않았다.**

- **서식 원본을 `서식원본/개명신고서(20260828).pdf` 로 교체**했다.
  ⚠️ **성명칸의 짜임이 바뀌었다** — 구본은 「(성) / (명)」 두 칸이었는데
  신본은 **한글 한 칸 · 한자 한 칸**이다. 그래서 화면은 성·이름을 따로 묻되
  인쇄할 때 **붙여서 한 번에** 찍는다(`buildVals` 의 `befKor`·`befHan`·`aftKor`·`aftHan`).
  ⛔ 구본 `개명신고서.pdf` 는 대조용이다. 그것으로 배경을 다시 만들지 마라.
  📌 이 서식은 텍스트층이 살아 있어 `get_text("words")`·`rawdict` 로 라벨을 바로 쟀다
  (다른 신서식들은 글자가 곡선으로 변환돼 있어 잉크 덩어리를 실측해야 했다).

- **준비물 — 담당자 원문 그대로 둘뿐이다**: **신고인 신분증 · 법원 결정문 원본**.
  ⛔ 종전 화면이 쓰던 「개명허가결정등본」 같은 다른 이름을 섞지 않았다.

- **공통 필수/선택 원칙 적용** — **등록기준지·본·한자만 선택**(+`optHint`),
  **이메일만 예외 문구**. ⚠️ 종전에는 **등록기준지가 필수**였는데 원칙에 따라 선택으로 바꿨고,
  대신 신고인 **주민등록번호·주소**가 새로 필수가 됐다.
  **자격(기타) 상세**는 「기타」를 골랐을 때만 묻고 그때는 필수이며,
  다른 자격으로 되돌리면 **인쇄되지 않는다.**

- **Product UI v1 일괄 이식** — ASK 26 / PAPER 56 / RAIL 18 · BAR 62px ·
  RAIL(진행 → 준비물 → 안심 → 이용 안내) · Normal/Big. 시작 화면은 `when` 으로 숨겼다.

- 어디를: `forms/naming.config.js` · `engine/assets/naming.b64`(새 배경) ·
  `naming-helper-v1.html`(재빌드) · `index.html`(카탈로그 준비물 · 미리보기 그림) ·
  `kiosk-app/app/` · `tests/baseline/naming-*.png` · `AGENTS.md`·`engine/README.md`·
  `docs/UIUX_HANDOFF.md`·`tools/design-shots.py`.

- 확인: `verify-print` **19쪽 0px** · `--electron` **19쪽 0px** · **baseline 은 naming 2장만** ·
  본인·법정대리인 두 예시 **8단계 완주**(2→9) · 빈 상태에서 필수가 막음(2/2/2/2/5건) ·
  「기타」 되돌림에 인쇄 잔상 0 · 성명 합침 확인 · state **24개** ·
  `measure-screen` 15 · `verify-site-config` 6 · 블록 9화면 동일 · `sync-kiosk --check` 통과.

- 📌 남은 Product UI 이식은 **증명서·부동산** 2종이다.

---

## 2026.08.29 (8)

### [정정] 준비물을 **담당자 원문**으로 되돌렸다 — 사망 도장 삭제 · 이혼 서류명  · 담당자 요구사항 확정본

- 무엇을: 담당자 요구사항 확정본이 **업무요구사항의 원본(Source of Truth)** 이 됐다.
  기존 화면·과거 코드·일반 업무지식·추론과 충돌하면 **담당자 원문이 이긴다.**
  이미 마감한 이혼·사망에서 **원문과 어긋난 준비물만** 정리했다.
  ⛔ 엔진·좌표·UI·조건분기는 다시 열지 않았다.

- **사망 — 도장을 뺐다.** 담당자가 준 준비사항은 **신고인 신분증 · 사망진단서(또는
  시체검안서) 원본** 둘뿐이다. 도장은 「다른 신고서에 있으니 여기도」로 넣었던 것이고,
  ⛔ **「과거 화면에 있었다」는 근거가 되지 않는다.** 서류명도 원문대로 「원본」을 붙였다.

- **이혼 — 서류명을 원문으로 되돌렸다.** 담당자 원문은
  **당사자 신분증 · 도장 · 협의서 원본 또는 법원 판결문 원본** 이다.
  종전에 「협의이혼의사확인서 등본」·「판결 등본 및 확정증명서」로 **구체화**하고
  협의/재판 갈래로 나눠 보여주던 것을 걷었다 — 원문에 없는 서류명이라
  시민이 **다른 서류를 떼러 갈** 수 있다. 화면 안내문(⑥ 단계 두 곳·완료 화면)의 서류명도
  같이 맞췄다. ⚠️ 협의/재판 **분기 자체와 조건부 입력·필수검증은 그대로**다.

- ⚠️ **엔진의 `ready` 함수 지원을 되돌렸다.** 이혼의 갈래별 준비물을 위해 하루 전에 넣은
  한 줄인데, 이제 쓰는 곳이 없다. 쓰지 않는 갈래를 남겨 두지 않는다(고정 배열로 복귀).

- 📌 **담당자 확인 TODO(임의로 결론 내리지 않는다)**
  · 이혼 「당사자 신분증」 — 실제 접수에 **오는 사람의 신분증만** 필요한 것인지
  · 이혼 「협의서 원본」 — 담당자가 뜻한 **정확한 서류명**
  · 혼인 「당사자 신분증」 — 지금 화면은 **남편·아내 두 줄**로 갈라 놓았다.
    원문은 한 줄이므로 두 사람 각각이 맞는지 확인이 필요하다(이번에는 건드리지 않았다).

- 어디를: `forms/divorce.config.js` · `forms/death.config.js` · `engine/engine.js`(되돌림) ·
  `index.html`(카탈로그 준비물 두 곳) · 엔진 **7종 재빌드** · `kiosk-app/app/`.

- 확인: `verify-print` **19쪽 0px** · `--electron` **19쪽 0px** · **기준선 무변경** ·
  Main 이혼 3장 / 사망 2장 · 작성화면 기둥이 Main 과 **같은 목록** · 어절 중간 줄바꿈 0 ·
  `measure-screen` 15 · `verify-site-config` 6 · 블록 9화면 동일 · `sync-kiosk --check` 통과.

---

## 2026.08.29 (7)

### [완료] 사망신고 — **신서식 교체 · Product UI v1 이식 · 인쇄 누락 1건 수정**  · 사용자 지시

- 무엇을: 출생·혼인·이혼에서 확립한 양산 방식을 사망신고에 그대로 적용했다.
  ⚠️ **전수분석은 하지 않았다** — 신서식·좌표·필수 검증·UI 이식·준비물만 했다.
  사망은 이미 엔진 서식이었으므로 **포크 전환은 없다**(껍데기만 `shell:"product"` 로 바꿨다).

- **서식 원본을 `서식원본/사망신고서(20260828).pdf` 로 교체**했다(개정본 · 통계청 →
  국가데이터처 · **2쪽 → 1쪽**). 판면이 재조정돼 **좌표 34개를 다시 잡았다.**
  격자선(`get_drawings`)으로 셀을 뽑고, 인쇄된 안내글자(「(성)」·「년/월/일/시/분」·「의」·
  번호 네모 ①②③)는 **잉크 덩어리를 실측**해 그 옆·그 위에 얹었다.
  ⚠️ 특히 **사망일시의 년·월·일·시·분 간격**과 **인구동향 번호칸**이 구본보다 오른쪽으로
  밀려 있었다(㉮ 중학교 272 → 280 · 고등학교 325 → 344 · 대학(교) 388 → 414 · 대학원 448 → 480).
  ⛔ 구본 `사망신고서.pdf` 는 대조용이다. 그것으로 배경을 다시 만들지 마라.

- 🐞 **인쇄 누락을 하나 고쳤다** — `r_addr`(신고인 주소)가 `buildVals` 에 **빠져 있었다.**
  좌표는 있는데 값을 만들지 않아 **화면에는 보이고 인쇄물에는 빈칸**으로 나갔다.
  ⚠️ 창구가 되돌려보내는 자리다. 종전 기준선에도 그 빈칸이 그대로 박혀 있었다.

- **필수 검증을 채웠다**(담당자 원칙 — 「화면에 제시되고 해당 신고에 적용되는 사항은
  원칙적으로 필수」). **등록기준지·한자만 선택**(+`optHint`), **이메일만 예외 문구**
  (「없으면 비워 두셔도 됩니다」 — 창구가 대신 만들어 줄 수 없다. 출생과 같다).
  성별·세대주 성명/관계·사망 시각(시·분)·사망 장소 상세·구분·신고인 주민등록번호/자격/주소·
  최종 졸업학교·혼인 상태가 새로 필수가 됐다.
  · **사망 시각에 범위 검증**을 붙였다(시 0~23 · 분 0~59) — `invalid()` 로 `required()` 와 나눴다.
  · **「기타」 상세는 화면에 나타났을 때만 필수**이고, 다른 장소로 되돌리면 **인쇄되지 않는다.**

- **준비물** — 신고인 신분증 · **사망진단서 또는 시체검안서**(의사가 작성한 것) · 도장.
  ⛔ 지어낸 것이 없다. 사망진단서는 이 서식이 요구하는 첨부서류이고 **종전 화면에도
  같은 문장이 있었다.** 허브 카탈로그(`ready`)에도 같은 셋을 넣었다.

- **Product UI v1 일괄 이식** — ASK 26 / PAPER 56 / RAIL 18 · BAR 62px ·
  RAIL(진행 → 준비물 → 안심 → 이용 안내) · Normal/Big · 작성 → 미리보기 → 인쇄.
  시작 화면은 `when` 으로 숨겼다. ⚠️ 「시」의 도움말은 출생과 **글자까지 같게** 뒀다 —
  길어지면 반 칸에서 두 줄이 돼 옆 칸과 짝이 어긋난다.

- 어디를: `forms/death.config.js` · `engine/assets/death.b64`(새 배경) ·
  `death-helper-v1.html`(재빌드) · `index.html`(카탈로그 준비물 · 미리보기 그림) ·
  `kiosk-app/app/` · `tests/baseline/death-*.png` · `AGENTS.md`·`engine/README.md`·
  `docs/UIUX_HANDOFF.md`·`tools/design-shots.py`(껍데기 목록에 사망 추가).

- 확인(정상 경로·대표 분기·Normal/Big·인쇄): `verify-print` **19쪽 0px** ·
  `--electron` **19쪽 0px** · **baseline 은 death 2장만** 변경 ·
  의료기관·자택 두 예시 **7단계 완주**(2→8) · 빈 상태에서 필수가 막음(7/5/6/2건) ·
  시각 범위 오류 2건 잡힘 · 「기타」 되돌림에 인쇄 잔상 0 · 신고인 주소 인쇄됨 ·
  state **28개** · `measure-screen` 15화면 · `verify-site-config` 6 · 블록 검사 9화면 동일.

- 📌 TODO(비차단): 남은 Product UI 이식은 **개명·증명서·부동산** 3종이다.
  `#askFoot`(Handoff)은 비어 있다.

---

## 2026.08.29 (6)

### [완료] 이혼신고 — **신서식 교체 · 엔진 전환 · Product UI v1 이식**  · 사용자 지시

- 무엇을: 출생(`75a2582`)·혼인(`923ecff`)에서 확립한 양산 방식을 이혼신고에 그대로 적용했다.
  ⚠️ **전수분석은 하지 않았다**(사용자 지시) — 신서식·좌표·필수 검증·UI 이식만 했다.

- ⚠️ **손작성 포크를 엔진으로 되돌렸다.** `divorce-helper-v1.html`(464KB)은 혼인과 같은
  **옛 엔진 복제 포크**였고(`CO`·`STEPS`·`STEP_HL`·`renderForm` 인라인),
  갈라진 탓에 **필수 검증(`required`)이 한 곳도 없었다.**
  이제 `forms/divorce.config.js` + `shell:"product"` 다. **이로써 손작성본은 여권 하나만 남았다.**

- **서식 원본을 `서식원본/이혼신고서(20260828).pdf` 로 교체**했다(개정본 · 통계청 →
  국가데이터처 · 「㊞ 또는 서명」 → 「(서명 또는 날인)」 · **2쪽 → 1쪽**).
  ⚠️ 판면이 구본과 달라 **좌표 90개를 다시 잡았다.** 눈대중으로 옮기지 않았다 —
  격자선(`get_drawings`)으로 셀 경계를 뽑고, 인쇄된 안내글자(「(성)」·「년」·「명」·
  번호 네모 ①②③)는 **잉크 덩어리를 실측**해 그 옆·그 위에 얹었다.
  ⛔ 구본 `이혼(친권자 지정)신고서(서식).pdf` 는 대조용이다. 그것으로 배경을 다시 만들지 마라.

- **업무 갈래를 화면에 세웠다** — 종전에는 「재판상 이혼이면 적으세요」라는 **안내문뿐**이라
  협의/재판을 시스템이 알지 못했고, 그래서 아무것도 필수로 만들 수 없었다.
  · **⑥ 이혼 종류**(협의이혼 / 재판상 이혼)를 물어 ④ 재판확정일자·법원명을 **조건부 필수**로 했다.
    협의이혼으로 되돌리면 그 값은 **인쇄되지 않는다**(`buildVals` 에서 끊는다) —
    남아 있으면 창구가 되돌려보낸다.
  · **⑦ 미성년 자녀 수**(없음~4명)를 물어 자녀 블록을 그만큼만 낸다.
    서식의 ⑤ 칸이 **네 자리**라 상한이 4다. 고르지 않은 자리에는 영표가 남지 않는다(`checkVisible`).
  ⚠️ 이 둘은 서식에 없는 칸이다 — **질문일 뿐 인쇄되지 않는다.**

- **필수 검증을 채웠다**(2026.08.29 담당자 원칙 — 「화면에 제시되고 해당 신고에 적용되는
  사항은 원칙적으로 필수」). **등록기준지·본·한자만 선택**이고 공통 안내(`optHint`)를 붙였다.
  당사자·부모·이혼 종류·친권자·출석·인구동향 항목이 전부 필수가 됐다(종전에는 검증이 아예 없었다).

- **준비물을 「갈래에 따라 달라지는 것」으로 만들었다.** 혼인의 목록을 그대로 옮기지 않았다 —
  이혼은 확인을 받은 뒤 부부 중 **한 사람만 와도** 신고할 수 있으므로 「신분증 두 장」이 아니다.
  · 공통 — **신고인 신분증 · 도장**
  · 협의이혼 — **협의이혼의사확인서 등본**
  · 재판이혼 — **판결 등본 및 확정증명서**(조정·화해로 이혼한 경우에는 그 재판서류)
  ⚠️ **엔진이 `ready` 를 함수로도 받게 했다**(`engine.js` 한 줄). 종전에는 고정 배열뿐이라
  경로에 따라 갈리는 준비물을 낼 수 없었다. ⛔ **두 갈래를 동시에 보여주지 않는다** —
  고른 뒤에는 그 갈래만 낸다. 시민이 **없는 서류를 떼러 가지 않게** 하는 것이 요점이다.
  ⛔ 친권자 지정 관련 서류는 넣지 않았다 — 업무규칙을 추정하지 않는다.
  **허브(Main)** 는 아직 고르기 전이므로 네 장을 내되 「협의이혼일 때 · 재판이혼일 때」로
  **조건을 부제에 적었다.** 카드 조형·정보밀도는 그대로다.
  ⚠️ 그 과정에서 **허브 준비물 카드에 한국어 조판 규칙이 빠져 있던 것**을 찾았다 —
  「협의이혼의사확인서 등본」이 「등/본」으로 갈렸다. `.rc-name`·`.rc-sub` 에
  `word-break:keep-all` 한 줄을 넣었다(⛔ `<br>` 로 막지 않았다). 준비물 이름이 짧던
  일곱 서식에서는 드러나지 않던 자리다.

- **Product UI v1 일괄 이식** — ASK 26 / PAPER 56 / RAIL 18 · BAR 62px ·
  RAIL(진행 → 준비물 → 안심 → 이용 안내) · Normal/Big · 작성 → 미리보기 → 인쇄.
  시작 화면은 `when` 으로 숨겼다.

- ⚠️ **도구 목록이 혼인 전환 때부터 갈라져 있던 것을 함께 바로잡았다.** `embed-logo.py` ·
  `check-site-block.py` · `check-design-tokens.py` · `make-form-previews.py` 가 혼인·이혼을
  아직 **손작성**으로 알고 있었다. 그대로 두면 `--fix` 와 로고 심기가 **재빌드에 덮일 파일**을
  고쳐 놓고 고쳤다고 말한다. 아울러 `embed-logo.py` 는 새 껍데기 `engine/base-product.html` 을
  **보지 않고 있었다** — 로고를 바꾸면 출생·혼인·이혼 셋만 조용히 옛 로고로 남는다.

- 어디를: `forms/divorce.config.js`(신설) · `engine/assets/divorce.b64`(새 배경) ·
  `engine/engine.js`(`ready` 를 함수로도 받음 → **엔진 7종 재빌드**) ·
  `divorce-helper-v1.html` · `index.html`(카탈로그 준비물 · 미리보기 그림 · 조판 한 줄) · `kiosk-app/app/` ·
  `tests/baseline/divorce-*.png` · 도구 4종 · `AGENTS.md`·`README.md`·`engine/README.md`·
  `docs/UIUX_HANDOFF.md`(엔진 7종 / 손작성 여권 하나로 정정).

- 확인(정상 경로·대표 분기·Normal/Big·인쇄): `verify-print` **19쪽 0px** ·
  `--electron` **19쪽 0px**(용지 210.23×297.35mm) · **baseline 은 divorce 2장만** 변경 ·
  협의·재판 두 예시 **9단계 완주**(2→10) · 빈 상태에서 단계마다 필수 검증이 막음(6/6/4/4/1/1/1/7건) ·
  자녀 수 밖 잔상 0 · 협의↔재판 왕복에 인쇄 잔상 0(재판확정일자·법원명) ·
  준비물이 갈래대로만 나옴(고르기 전 4 · 협의 3 · 재판 3) · 허브 카드 어절 중간 줄바꿈 0 ·
  여권 허브 카드 무변경 · state **64개** ·
  `measure-screen` 15화면 · `verify-site-config` 6 · 블록 검사 9화면 동일 · `sync-kiosk --check` 통과.

- 📌 TODO(비차단): 혼인과 같이 **신고일(제목 아래 「 년 월 일」)을 인쇄하지 않는다**(출생은 찍는다) —
  둘을 함께 담당자에게 확인한다. `#askFoot`(Handoff)은 비어 있다.
  남은 Product UI 이식은 **사망·개명·증명서·부동산** 4종이다.

---

## 2026.08.29 (5)

### [완료] 혼인신고 — **신서식 교체 · 엔진 전환 · Product UI v1 이식**  · 담당자 요구 + 사용자 지시

- 무엇을: 출생신고(`75a2582`)에서 확립한 양산 방식을 혼인신고에 그대로 적용했다.
  ⚠️ **전수분석은 하지 않았다**(사용자 지시) — 신서식·좌표·담당자 요구·UI 이식만 했다.

- ⚠️ **손작성 포크를 엔진으로 되돌렸다.** `marriage-helper-v1.html`(508KB)은 **옛 엔진을
  통째로 복제해 손으로 유지해 온 포크**였다(`CO`·`STEPS`·`STEP_HL`·`renderForm` 인라인).
  갈라진 탓에 **필수 검증(`required`)이 한 곳도 없었다.** 이제 `forms/marriage.config.js` +
  `shell:"product"` 이고, 출생에서 만든 껍데기·RAIL·검증 훅을 그대로 쓴다.

- **서식 원본을 `서식원본/혼인신고서(20260828).pdf` 로 교체**했다(개정본 · 통계청 →
  국가데이터처 · 「㊞ 또는 서명」 → 「(서명 또는 날인)」 · 2쪽 → 1쪽).
  ⚠️ 출생과 같이 **판면이 재조정**돼 있어 좌표 **110개를 셀 대 셀로 옮겼다.**
  본문 ①~⑩ 은 행 띠 **0~28 이 1:1** 로 맞았고, 그 아래 「인구동향조사」만 구본에 얇은 틈이
  더 있어 띠 번호를 이어 줬다(36→32, 38→33, 39→34, 40→35, 41→36).
  ⚠️ 병합 칸 **49곳은 라벨을 실측**해 따로 잡았다 — ④⑤ 예/아니요 4 · ⑨ 출석 2 ·
  ㉮ 동거 시작일 3 · ㉯㉰㉱ 네모 40. 격자만으로는 영표가 표 밖으로 나갔다.
  ⛔ 구본 `혼인신고서(서식).pdf` 는 대조용이다. 그것으로 배경을 다시 만들지 마라.

- **담당자 요구 반영**
  · 준비물 — **남편(부) 신분증 · 아내(처) 신분증 · 도장**. ⚠️ 혼인은 당사자가 둘이라
    신분증도 둘이다 — 한 줄로 묶으면 한 사람만 와도 되는 것처럼 읽힌다.
  · **④ 성·본의 협의를 필수로** 바꿨다(종전 선택).
  · **⑦ 증인** 화면에 「인쇄 후 직접 서명 또는 날인을 합니다. (대리서명 금지)」를 넣었다.
    ⛔ 부드럽게 고쳐 쓰지 마라 — 대리서명은 신고 자체가 무효가 될 수 있는 자리다.
  · 「화면에 제시되고 해당 신고에 적용되는 사항은 원칙적으로 필수」를 적용하고,
    **등록기준지·본·한자**만 선택으로 두고 공통 안내(`optHint`)를 붙였다.
    당사자·부모·증인·인구동향 항목이 전부 필수가 됐다(종전에는 검증이 아예 없었다).

- **Product UI v1 일괄 이식** — ASK 26 / PAPER 56 / RAIL 18(495/1066/343) · BAR 62px ·
  RAIL(진행 → 준비물 → 안심 → 이용 안내) · Normal/Big · 선 아이콘(도장 아이콘 신설) ·
  작성 → 미리보기 → 인쇄. 시작 화면은 `when` 으로 숨겼다(허브에서 이미 골랐다).

- 어디를: `forms/marriage.config.js`(신설) · `engine/assets/marriage.b64`(새 배경) ·
  `engine/engine.js`(도장 아이콘) · `marriage-helper-v1.html`(재빌드) ·
  `index.html`(허브 준비물·미리보기 그림) · `kiosk-app/app/` · `tests/baseline/marriage-*.png`.

- 확인(정상 경로·대표 분기·Normal/Big·인쇄): `verify-print` **19쪽 0px** ·
  `--electron` **19쪽 0px** · **baseline 은 marriage 2장만** 변경 · 성인·미성년 **9단계 완주** ·
  **RAIL 넘침 0**(6단계 × 일반·크게) · 가로 넘침 0 · 필수 검증 표본 통과 ·
  `measure-screen` 15화면 · `verify-site-config` 6 · 블록 9화면 동일.

- 📌 TODO(비차단): 혼인은 **신고일(제목 아래 「 년 월 일」)을 인쇄하지 않는다** — 출생은 찍는다.
  `#askFoot`(Handoff)은 비어 있다. 이혼 도우미도 같은 손작성 포크라 같은 전환이 필요하다.

---

## 2026.08.29 (4)

### [완료] 출생신고에 **Product UI v1** 을 이식했다  · 사용자 지시(Phase 3)

- 무엇을: 여권에서 동결한 Product UI v1(`1d735fc`)을 출생신고에 **재설계 없이** 옮겼다.
  `.phone` 2단 구조를 걷고 **ASK 26 / PAPER 56 / RAIL 18**(495/1066/343) · BAR **62px** ·
  Product Base Topbar · Normal/Big · 한국어 조판 · Button Role 4종 · 선 아이콘 · RAIL 을 갖췄다.
- ⚠️ **껍데기를 갈랐다** — `engine/base-product.html` **신설**. 엔진 서식 5종이 HTML 껍데기를
  공유해서, 한 서식만 옮기려면 껍데기가 둘이어야 한다. 출생만 `shell:"product"` 를 쓰고
  **사망·개명·증명서·부동산은 옛 껍데기 그대로**다. 새 껍데기는 `passport-helper-v1.html` 의
  머리·꼬리를 **글자 그대로 뜬 것**이라 조형이 갈릴 여지가 없다.
- **RAIL 신설** — 진행 → 준비물 → 안심 → 이용 안내. 준비물은 허브 카탈로그와 같은 두 가지
  (신고인 신분증·출생증명서 원본)뿐이다. ⛔ 지어내지 않았다.
  이용 안내를 5줄 → 2줄로 줄였다(지운 셋은 안심 문단·준비물·마지막 단계로 **옮겨져 있다**).
  줄이지 않으면 크게 보기에서 8px 넘쳤다.
- **Intro 단계를 숨겼다**(`when:function(){return false;}`). 허브에서 이미 출생신고를 고르고
  들어왔는데 또 「시작하기」를 누르게 할 이유가 없다. ⛔ **번호를 당기지 않았다** —
  `STEP_HL`·`applySample`·`required` 가 절대 번호를 쓴다.
- ⚠️ **인쇄가 한 번 어긋났다가 복구됐다.** 여권 CSS 에는 `.ov.r`(우측정렬)·`.ov.o`(영표 ○)·
  `.ov.o.blk` 가 **없다**(여권이 쓰지 않는 규칙). 그대로 두니 「세대주 및 관계」의 이름이
  우측정렬을 잃고 밀렸다 — **화면은 멀쩡하고 인쇄만 어긋나는** 그 종류다.
  `engine/base.html` 과 글자까지 같게 이어 붙여 **19쪽 0px** 로 되돌렸다. baseline 은 무변경.
- ⚠️ 보정 CSS 를 `SITE-CONFIG` 블록 **안**에 끼웠다가 9화면 동일 검사가 깨졌다 — 블록 밖으로 옮겼다.
- 반 칸 입력이 세로로 서던 것을 고쳤다 — `inline-block + 인접 선택자`로는 셋째부터 짝이
  어긋난다. 질문면을 flex-wrap 으로 두어 브라우저가 두 개씩 접게 했다.
- 크게 보기에서 「출생 시각 — 시(時)」의 필수 배지가 접혀 라벨을 **「시」·「분」**으로 줄였다.
  바로 위가 「출생 연월일」이고 아래 설명이 24시각제를 말하므로 뜻은 그대로다.
- 어디를: `engine/base-product.html`(신설) · `engine/engine.js`(+133줄 · 전부 새 껍데기의
  DOM 을 찾았을 때만 도는 코드) · `tools/build-form.js`(껍데기 고르기) ·
  `forms/birth.config.js` · 엔진 5종 재빌드 · `kiosk-app/app/`.
- 확인: `verify-print` **19쪽 0px** · `--electron` **19쪽 0px** · **baseline 한 장도 갱신 안 함** ·
  4종 인쇄물 무변경 · `measure-screen` 15화면 · `verify-site-config` 6 ·
  블록 검사 9화면 동일 · 검증 케이스 18건 + 필수 우회 차단 1건 · state **44개** ·
  혼인 중 9단계 / 혼인 외 8단계 완주 · **RAIL 넘침 0**(5단계 × 일반·크게).
- 📌 TODO(비차단): `#askFoot`(Human Handoff 자리)은 **비어 있다** — 무엇을 어느 화면에 둘지는
  업무 판단이라 담당자 확인 뒤에 채운다. `state.unsure` 는 아직 읽는 곳이 없다.
  나머지 4종의 Product UI 이식은 다음 차례다.

---

## 2026.08.29 (3)

### [반영] 출생신고 **필수 입력·출생장소·검증 기준**을 담당자 요구에 맞춰 정리  · 가족관계등록 담당자

- 무엇을: 「화면에 제시되고 해당 신고에 적용되는 사항은 **원칙적으로 필수**」라는 업무 원칙을
  44개 항목에 전부 적용했다. 다만 **등록기준지·본·한자·이메일**은 선택으로 남긴다.

  | 갈래 | 개수 | 내용 |
  |---|---|---|
  | 필수 | 23 | 성별·혼인 중/외·출생 시·출생 분·출생장소 종류·**출생장소 주소**·세대주 성명/관계·신고인 주민등록번호/주소/자격·학력 2 등을 새로 필수로 |
  | 선택 | 12 | 한자 4 · 본 3 · 등록기준지 4 · 이메일 1 |
  | 조건부 필수 | 2 | 출생장소(기타) 상세 · 기타 자격 상세 — **화면에 나타났으면 필수** |
  | 해당 없으면 안 물음 | 7 | 복수국적 · ③폐쇄등록부 3 · ④기타사항 · ⑥제출인 2 |

- **출생장소 주소를 별도 state 로 신설**(`child_birthPlaceAddr`). ⛔ 종류(`child_birthPlace`)·
  주소·「기타」 상세(`child_birthPlaceEtc`) 셋의 **의미를 섞지 않는다.** 화면에서 세 칸으로 가르고,
  서식의 그 줄에는 넓은 칸이 하나뿐이라(284.6~550.8pt) 파생 키 `birthPlaceCell` 이
  「기타 상세 · 주소」를 한 줄로 합쳐 찍는다.
  ⛔ **좌표는 손대지 않았다** — Phase 1 의 `x:297.0, y:193.8, w:241.0` 그대로이고 키 이름만 바뀌었다.

- **출생 시각에 범위 검증**을 붙였다(시 0~23 · 분 0~59). ⚠️ 「비었다」와 「값이 틀렸다」는
  안내 문장이 달라야 해서 엔진에 `invalid()` 훅을 따로 만들었다 — `required()` 와 나눠 둔 이유다.

- ⛔ **「이 단계는 잘 모르겠어요」로 필수를 우회할 수 없게 막았다.** 종전에는 이 단추가
  `required` 를 통째로 넘겨서 **반드시 있어야 할 값이 빈 채로 인쇄까지** 갔다. 창구에서
  되돌아오면 시민이 처음부터 다시 적는다. 이제 선택 항목만 건너뛴다.
  📌 `state.unsure` 는 여전히 **적히기만 하고 아무도 읽지 않는다** — 창구로 전달되지 않으므로
     이 기능은 완성된 것이 아니다. Human Handoff 로 옮기는 것이 Phase 3 과제다.

- **선택사항 안내 문구를 한 곳으로 모았다** — 엔진의 `OPT_HINT`
  「모를 경우 비워두세요. 접수 시 알려드리겠습니다.」 하나뿐이고 서식은 `{optHint:true}` 로 부른다.
  ⛔ 화면마다 비슷한 말을 새로 짓지 마라. 말이 조금씩 달라지면 시민은 「이 칸은 좀 다른가」로 읽는다.
  ⚠️ 이메일만 예외로 「없으면 비워 두셔도 됩니다」를 쓴다 — 창구가 대신 만들어 줄 수 없어
     「접수 시 알려드리겠습니다」가 성립하지 않는다(담당자 승인).

- 9단계 문구를 **「국가데이터처 인구동향조사」**로. ⛔ 사망·혼인·이혼은 서식 원본이 아직
  통계청 표기(각 4회)라 건드리지 않았다.

- ⚠️ **엔진 공용 변경 3건** — 조건부 단계(`when`) · 범위 검증(`invalid`) · 필수 우회 차단.
  앞의 둘은 훅이 없는 서식에 아무 영향이 없고, 셋째는 5종 전부에 걸린다(각 서식이 스스로 정한
  `required` 를 존중하는 것이라 어디서나 옳은 방향이다). 5종 전부 재빌드하고 흐름을 실주행했다.

- 어디를: `forms/birth.config.js` · `engine/engine.js` · `engine/base.html`(`.opt-hint` 한 줄) ·
  엔진 5종 재빌드 · `kiosk-app/app/` · `tests/baseline/birth-*.png`.

- 확인: 검증 케이스 **22건 전부 통과**(시·분 없음/범위 밖·출생장소·주소·기타 상세·신고인 3종·
  자격 기타·선택 4종·성별·혼인 구분·「잘 모르겠어요」 차단/통과) ·
  wed 10단계·unwed 9단계 완주 · `verify-print` **19쪽 0px** · `--electron` **19쪽 0px** ·
  **다른 17쪽 baseline 무변경** · 4종 인쇄물 무변경 · `measure-screen` 15화면 ·
  `verify-site-config` 6 · 블록 9화면 동일 · **선언 44 = 인쇄 도달 44 · 고아 0**.

- 📌 **담당자 추가확인 항목**(현재 동작을 보존한 채 남긴다): 혼인 외 부 단계 생략의 업무상 확정 ·
  혼인 중 부 정보의 실제 적용조건 · 성·본 협의 질문 조건 · 신고인 자격별 추가서류 ·
  출생장소 기타 상세와 주소를 한 인쇄칸에 함께 찍는 방식 · 세대주 성명/관계 필수 여부 ·
  학력 필수 여부 · 복수국적 입력 조건.

- 📌 **Main `scale`** — 실제 단계 수가 **혼인 중 10 / 혼인 외 9** 로 갈리므로 고정 숫자를
  표시하지 않는다. `steps`·`minutes` 둘 다 `null` 로 두고, 경로가 정해진 뒤 RAIL 진행이
  그 경로의 정확한 수를 보여 준다.

---

## 2026.08.29 (2)

### [반영] 출생신고서 **서식 원본을 개정본으로 교체**하고 좌표를 전면 재산출  · 담당자 제공

- 무엇을: 배경 서식을 `서식원본/출생신고서.pdf` → **`서식원본/출생신고서(20260828).pdf`** 로 바꾸고,
  좌표 **69개를 전부 다시 잡았다**.
- 왜: 개정본이다. 본문의 **「통계청」이 「국가데이터처」로** 바뀌었다(3곳). 옛 배경을 그대로 두면
  시민이 **현행이 아닌 서식**을 인쇄해 창구에 낸다.
- ⚠️ **판면이 재조정됐다.** 개정본은 표 전체가 **왼쪽으로 28.5pt 넓어지고 오른쪽으로 27.9pt** 늘었고,
  ③~⑥ 구역의 행이 몇 pt 씩 오르내린다. 옛 좌표를 그대로 쓰면 **값이 엉뚱한 칸에 찍힌다** —
  실제로 겹쳐 그려 확인했다(성별·학력 동그라미가 라벨 사이에, 출생일시 값이 「일·시·분」 위에).
  ⛔ **화면은 멀쩡하고 인쇄만 어긋나는** 종류의 사고다. 배경을 갈면 좌표를 반드시 다시 잡아라.
- 어떻게 다시 잡았나 — 두 단계다. 손으로 수치를 밀지 않았다.
  1. **격자 대응** : 두 PDF 에서 표 규칙선을 뽑았더니 **가로 34줄 · 행 띠 32개**가 그대로였고,
     **32개 띠 전부 세로선 개수가 같았다.** 셀 대 셀로 짝이 지어지므로, 각 좌표를
     「어느 칸의 어디쯤」이라는 **상대 위치**로 옮겼다(줄바꿈 폭 `w` 도 그 띠의 가로 배율로).
  2. **라벨 실측** : 규칙선이 없는 병합 칸 **31개**는 격자만으로 맞지 않는다
     (「①②③ 네모」·「년 월 일 시 분」처럼 **인쇄된 글자**가 자리를 정하는 곳). 두 PDF 의 같은 줄에서
     잉크 구간을 뽑아 **같은 글자끼리 짝지어** 읽었다 — 출생일시 5개 · 성별 2 · 혼인 2 ·
     출생장소 3 · 성·본 협의 2 · 신고인 자격 4 + 기타(자격:) 1 · 학력 12.
     ⚠️ 자동 보정도 만들어 봤으나 **빈 칸을 맞추려 해 엉뚱한 글자를 집었다**(상관도 0.2대).
        같은 이름의 체크 라벨이 두 줄에 겹치는 문제도 있었다. 그래서 쓰지 않았다.
- 확인: 작성예시 **혼인 중·혼인 외** 두 벌을 2배 해상도로 찍어 **칸마다 눈으로 대조**했다 —
  성명·한자·본·성별·혼인 구분·출생일시·출생장소·등록기준지·주소·세대주 관계·부모 인적사항·
  주민번호 앞뒤·성·본 협의·신고인 성명/자격/전화/이메일·서명 하이라이트·학력 12칸 **전부 제자리**.
  `verify-print --update` 로 기준선 갱신(**`birth-wed-p1`·`birth-unwed-p1` 두 장만** 바뀌었다) →
  이후 `verify-print` **19쪽 0px** · `--electron` **19쪽 0px** · `measure-screen` 15화면 ·
  `verify-site-config` 6 · 블록 검사 9화면 동일 · `sync-kiosk --check`.
- 어디를: `forms/birth.config.js`(좌표 69개 · 재생성 명령 머리말) · `engine/assets/birth.b64`(새 배경) ·
  `birth-helper-v1.html`(재빌드) · `index.html`(허브 미리보기 그림) · `kiosk-app/app/`(동기화) ·
  `tests/baseline/birth-*.png` · `docs/디자인검토/shots/`.
  ⛔ 다른 7종·엔진·공통 블록 **무변경**.
- 📌 옛 `서식원본/출생신고서.pdf` 는 **남겨 둔다**(대조용). ⛔ 다시 그것으로 배경을 만들지 마라 —
  `forms/birth.config.js` 머리말의 재생성 명령이 새 파일을 가리킨다.
- 📌 출생신고서 화면은 **아직 Product UI v1 이전**이다(상단 바·패널이 옛 조형). 이식은 다음 차례다.

---

### [반영] 허브에 **출생신고 준비물**을 넣었다  · 현장 확인

- 무엇을: 「아이 출생 신고하기」의 `ready` 가 비어 있어 준비물 구역이 나오지 않았다.
  **신고인 신분증**(`id-card`)·**출생증명서 원본**(`cert`) 두 가지를 넣었다.
- 왜: 시민이 준비물을 모른 채 작성을 시작하면 창구에서 되돌아간다.
- 근거: `서식원본/출생신고서(20260828).pdf` 의 **첨부서류** 항목. 서식이 드는 여섯 항 중
  2~5항은 조건부이고 「가족관계등록관서에서 전산으로 확인되면 첨부를 생략한다」고 서식이
  스스로 밝힌다 — **누구에게나 필요한 1항(출생증명서)과 6항(신분확인)만** 허브에 둔다.
  ⛔ 지어낸 것이 없다. 혼인 외 출생·외국인 부·복수국적처럼 **경로가 갈릴 때** 더 필요한 서류는
  허브가 아니라 그 질문에 도달한 서식 화면이 맡는다(여권과 같은 규칙).
- 어디를: `index.html` 의 `FORM-CATALOG` · `kiosk-app/app/index.html`.
- 📌 `scale`(약 N가지·M분)은 **아직 비어 있다.** 질문 흐름을 정할 때 채운다.

---

## 2026.08.29

### [반영] 여권 상세의 **「← 메인으로」가 눈에 안 걸리던 것**  · 현장 의견

- 무엇을: 「← 메인으로」를 **Role ④ Compact-Tertiary → Role ② Body Button** 으로 올렸다.
  회색 1px 테두리 · 잉크 글자(13.5px) → **대표색 1.5px 테두리 · 대표색 글자 · 흰 면(15px)**.
  BAR 의 [미리보기]와 **같은 family 의 테두리 변형**이다.
  실측 104×33 → **115×38**(크게 보기 114×37 → 133×44).
- 왜: 「미리보기처럼 좀 눈에 띄면 좋겠다」는 의견. 종전 값은 초기화와 같은 계층이었는데,
  **뜻이 다르다** — 메인으로는 되돌릴 수 있는 이동이고 초기화는 되돌릴 수 없는 파기다.
  계층이 같으면 그 차이가 보이지 않는다.
- ⛔ **새 수치를 만들지 않았다.** 이미 있던 `.btn`(Role ② 보조 버튼) 규격을 그대로 썼다.
- ⛔ **채운 대표색(solid)으로 하지 않았다.** 이 화면에서 앞으로 가는 길은 [다음]이고,
  위아래에 채운 파란 단추가 둘이면 어느 쪽이 진행인지 흐려진다.
- ⛔ **「처음부터 다시 작성」은 그대로 두었다**(Role ④). 되돌릴 수 없는 행동이라 가장 조용해야 한다.
- 어디를: `passport-helper-v1.html`(`.nav-back`·`.reset-btn` 분리) · `kiosk-app/app/`(동기화) ·
  `tests/screen-baseline.json` · `docs/디자인검토/shots/`(재생성) ·
  `docs/PRODUCT_SKIN_v1.md` §3-1·§3-3 · `docs/UIUX_HANDOFF.md` §4-2-3·§4-2-4.
- 확인: `verify-print` 19쪽 0px · `--electron` 19쪽 0px · `measure-screen` 15화면
  (가로 넘침 0 · 종이 794×1122) · `verify-site-config` 6 ·
  `check-design-tokens`·`check-site-block` 9화면 동일 · `sync-kiosk --check` ·
  질문칸 높이 643→636(일반)·613→604(크게) · RAIL 넘침 0.
- 📌 나머지 색 문제(BAR tint · [미리보기] 강조도 · Handoff 두 variant 의 톤)는 여전히
  **Color & Visual Polish** backlog 다.

---

## 2026.08.28 (3)

### [완료] **Passport / Product UI v1** — Main 의 디자인 언어를 여권에 그대로 옮기고 값을 동결했다  · 사용자 지시

세 묶음이 한 기준점이다 — **Visual Migration v1**(단계 0~8) · **Product Shell v1**(상단 프레임) ·
**Product Typography v1 + ASK Component System v1**(글자·컴포넌트 정규화).

- 무엇을: 허브 Main 에서 확정한 시각 언어를 **여권 작성 화면 전체**에 이식했다.
  기능은 한 줄도 바꾸지 않았다 — **인쇄·흐름·검증·분기·개인정보 처리 무변경**이다.

  | | 이전 | 지금 |
  |---|---|---|
  | 골격 | BAR 74px · 열 2:3:1(크게 5:5:2) | **BAR 62px · 26/56/18 고정**(크게 보기에서도) |
  | 종이 | 크게 보기에서 794→**757** 로 줄었다 | **794×1122 유지**(일반·크게) |
  | 면·잉크 | 따뜻한 회색 · 잉크 2단 | Main 중립 회색 · **잉크 5단 역할별 하한선** |
  | 장식 | 대표색 그라데이션 띠·색 막대·모서리 선 | **없음**(면과 여백으로 가른다) |
  | 컨트롤 | 흰 면 위 흰 입력칸 · 반경 12 · 그라데이션 버튼 | `#fafafa` 면 · 반경 8/10/6 · **단색 버튼** · 대표색 3px 포커스 링 |
  | 아이콘 | 이모지 20여 종 | **선 아이콘 한 벌**(24격자·1.7·currentColor) |
  | RAIL | 진행+준비물+안심+이용안내 6줄 · **상시 스크롤**(성인 143 · 미성년 270px) | compact 준비물(≈35px) · 이용안내 2줄 · **넘침 0**(최악 조건 포함) |
  | Human Handoff | 질문 본문 끝 — 로마자에서 **279px 아래로 밀림** | **ASK 바닥 조건부 고정** · 없으면 0px · 항상 접근 가능 |
  | 크게 보기 | 열 비율이 바뀌고, 하드코딩 때문에 **절반은 안 커짐** | 프레임 고정 · **정보 중요도 기반 차등 확대**(결정 ⑳) |

- 왜: 허브와 작성 화면이 **다른 제품처럼 보였다.** 시민은 같은 키오스크에서 한 번에
  두 화면을 지나가는데 면색·버튼·아이콘·기둥 문법이 전부 달랐다. 또 크게 보기는
  「글자를 키우는 기능」인데 실제로는 **열 비율을 흔들고 종이를 줄이면서** 정작 Handoff 문구는
  키우지 못하고 있었다(실측으로 드러났다).

- ⚠️ 도중에 안전망 두 개를 고쳤다
  - `tools/measure-screen.py` **신설** — `verify-print.py` 는 인쇄만 보므로 화면이 망가져도
    19쪽 0px 는 통과한다. 15화면의 영역·넘침·종이를 재고 **나빠진 것만** 실패로 본다
  - `tools/design-shots.py` 에 **시계 고정** — 서식이 오늘 날짜를 찍어 **코드를 안 고쳐도
    갈무리 33장이 매일 diff** 였다. 이제 diff = 진짜 화면 변화다

- 어디를: `passport-helper-v1.html`(전면) · `kiosk-app/app/`(동기화) ·
  `tools/measure-screen.py`(신설) · `tools/design-shots.py` · `tests/screen-baseline.json`(신설) ·
  `docs/PRODUCT_SKIN_v1.md`(신설) · `docs/UX_DECISIONS.md`(결정 ⑳) · `docs/UIUX_HANDOFF.md`(§2·§4 갱신) ·
  `docs/GOTCHAS.md`(㉛ 보강). **`engine/`·7종·`DESIGN-TOKENS`·인쇄 CSS·좌표맵 무변경.**

- 확인(마지막 상태): `verify-print.py` **19쪽 0px** · `--electron` **19쪽 0px** ·
  `verify-site-config.py` 6 · `check-design-tokens`·`check-site-block` 9화면 동일 ·
  `make-form-previews --check` · `sync-kiosk --check` · `measure-screen` **15화면**
  (가로 넘침 0 · 종이 794×1122) · `node --check`.
  실측 최종값 — ASK/PAPER/RAIL **495/1066/343** · BAR **62** · 종이 **794×1122** ·
  최악 RAIL(준비물 6개) **넘침 0**(일반·크게) · Handoff 없는 화면 하단 **0px** ·
  **한국어 어절 중간 줄바꿈 0**(일반·크게) · `[이전]/[다음]` **201×56 동일** ·
  「처음부터 다시 작성」 확인창 정상 · 「메인으로」 → `index.html` · **JS 오류 0**.

- **Product Shell v1(상단 프레임 일원화)** — Main 을 기준으로 상세페이지를 맞췄다.
  · BAR 구조를 `IDENTITY │ FLEX SPACE │ UTILITY │ DATE` 로 통일(⛔ 3등분·정중앙 배치 폐기)
  · 여권 identity 를 **두 줄 → Main 과 같은 한 줄 lockup**(기관명 │ 민원 서식 작성 도우미)
  · **BAR 의 업무명(「여권 신청」) 제거** — 작성 영역이 이미 말한다
  · `[← 메인]`을 identity 오른쪽으로(공통 navigation · 「서식 목록」에서 이름을 바꿨다)
  · UTILITY 는 **DETAIL TOOLS → COMMON TOOLS** 순 · 「테스트용」 배지를 시민 화면에서 뺐다.
    ⛔ **COMMON 의 끝 `[크게 보기][DATE]` 사이에는 아무것도 넣지 않는다** — 거기가 오른쪽
    기준선이고, 서식마다 다른 단추가 끼면 화면을 옮길 때마다 그 자리가 흔들린다.
    최종 순서 `[작성예시(성인)][작성예시(미성년)][미리보기]` │ `[크게 보기][DATE]`
  · **BAR 직접 인쇄 제거** — 인쇄는 **작성 → 미리보기 → 인쇄** 하나로. ⛔ 인쇄 기능·DOM·
    좌표·`afterprint` 덮개는 그대로다(19쪽 0px 로 확인)
  · **초기화를 ASK 바닥으로** 옮기고 「처음부터 다시 작성」으로. Handoff(지금 질문을 푸는 도움)와
    **시각 계층을 분리**했다. 확인 절차(`askConfirm` → `resetAll`)는 그대로다
  · **날짜를 Main 과 같은 규격으로** — 13.5px/400/`#9ca3af`/`tnum`, 형식 `2026년 08월 28일 (금)`,
    최우측 고정. ⚠️ Shell 로 옮기며 `.today` 규칙이 통째로 빠져 본문 크기로 나오고 있었다
  · 실측 대조: BAR 높이·padding·gap·정렬·면·경계선 + 로고·기관명·구분선·제품 이름·날짜의
    크기·굵기·행간·색·자간까지 **Main 과 전부 동일**
  📌 **색은 이번 범위가 아니다.** BAR signature tint(대표색 3%/5%)·버튼 강조도·전체 색 농도는
     **Color & Visual Polish** 후속 작업으로 넘겼다(`docs/PRODUCT_SKIN_v1.md`).
- **Product Typography v1 + ASK Component System v1(마지막 정규화)** — 값을 동결했다.

  · **Normal 글자를 허브 Main 의 밀도로 내렸다.** 여권의 눈금은 **크게 보기가 없던 시절**
    (2026.07 「어르신 가독성 강화」)에 만들어져 **Normal 이 큰 글씨 몫까지** 지고 있었다.
    그래서 목록에서 작성으로 넘어오면 글자가 갑자기 커졌다 — 크게 보기가 따로 있으므로
    Normal 이 그 몫을 질 이유가 없다.
    질문 34→**28** · 「왜 묻는지」 17→**15** · 라벨 18→**16** · 입력 21→**18**(칸 높이 64→**56**) ·
    카드 20/15→**18/14** · 본문 17→**15.5** · 요약 16.5→**15** · 보조 13.5→**13** ·
    CTA 18/62px→**17/56px** · 카드 표적 132→**116**. 토큰을 되올리던 하드코딩 **22곳**도 함께.
    ⛔ **크게 보기 값은 그대로다**(로마자 예외 제거분 2건 제외 · 아래).
    ⛔ 손끝 표적은 `--touch-min` **48px** 아래로 내리지 않았다.
    📌 근거: RAIL(15/13.5/12.5)은 이미 Main 과 같았다 — **ASK 만 혼자 큰 눈금**을 쓰고 있었다.

  · ⚠️ **공통 `DESIGN-TOKENS` 를 한 번 건드렸다가 되돌렸다.** 그 블록은 9개 화면에 글자까지
    같아야 하고 `check-design-tokens.py` 가 바로 잡았다. 여권 축소값은 **그 파일의 지역
    `:root`**(`MAIN-SKIN` 뒤)로 옮겼다 — 일반 보기는 뒤에 와서 이기고, `html.big` 은
    선택자 우선순위가 높아 **크게 보기가 그대로 남는다.** ⛔ 7종 이식 때도 지역 scope 를 쓴다.

  · **한국어 조판 — 어절을 쪼개지 않는다.** `.task-col,.rail-col{word-break:keep-all;
    overflow-wrap:anywhere}` 한 줄. 글자별 위치를 재서 검출한 결과
    **일반 15건·크게 23건 → 둘 다 0건**(「신청하시나 / 요?」 「신청합니 / 다」 따위).
    ⛔ 화면마다 `<br>` 을 박지 않는다 — 문구·기관명이 바뀌면 어긋난다(`rebrand.py`).
    ⛔ 선택자를 나열하지 않는다 — 나열식으로 해 보니 `.sug-note` 하나가 빠져 거기서만 쪼개졌다.

  · **Button Role 4종으로 정규화** — ① CTA(56/72px · 17→21 · r6) · ② Body Button ·
    ③ BAR Utility(38/33 · 14/13 · 두 보기 같음) · ④ Compact-Tertiary(33 · 13.5). 반경은 **전부 6**.
    ⛔ 같은 role 에서 새 수치를 만들지 않는다. BAR 의 ghost/primary 높이 불일치(42 vs 40)도
    **38/38** 로 맞췄고, `.sug-btn` 에 빠져 있던 굵기(400→700)를 채웠다.

  · **[이전]/[다음]을 같은 크기의 한 쌍으로** — 종전 `150px 1fr` 이라 [다음]이 세 배 넓었다.
    지금 **201×56 으로 완전히 같고**(반경 6 · 아이콘 16 · 가운데 정렬) 다른 것은 **색과 면**뿐이다.
    ⛔ 크기로 위계를 만들지 않는다. 좁은 화면 예외(`1fr 1.4fr`)도 걷었다.

  · **ASK Navigation Zone** — `[← 이전][다음 →]` 한 쌍 아래에 tertiary 하나. 규칙 하나가 자리를 정한다.

  · **두 tertiary control 을 보이는 단추로.** 「← 메인으로」와 「처음부터 다시 작성」이
    종전에는 **글자만 떠 있어** hover 없는 터치 화면에서 누를 수 있는 것인지 알 길이 없었다.
    흰 면 + 1px 테두리 + 반경 6 + 아이콘 15(되돌리기 아이콘 신설).
    뜻은 문서에 갈라 적었다 — **메인으로 = 업무 이탈** · **처음부터 = 현재 업무 초기화**
    (뒤엣것만 `askConfirm` 확인 절차가 있고, 그 절차는 그대로다).

  · **Human Handoff = 하나의 Family, 두 Variant.** Panel(`.ask-box`)은 제목+설명+행동이
    모두 필요한 안내형, Inline(`.card-sub`)은 한 줄 자체가 행동인 compact 형이다.
    **반경 12 · 아이콘 18 · 눌림 반응**을 공유하고, 내용 구조와 weight 차이만 허용한다.
    ⚠️ Panel 을 **가로 한 줄 → 2단 격자**로 바꿨다 — ASK 가 495px 인데 아이콘·글·단추를 한 줄에
    놓으니 글에 220px 밖에 안 남아 제목이 세 줄로 접혔다. 이제 위는 `[아이콘][제목·설명]`,
    아래는 폭을 다 쓰는 단추다. 덕분에 로마자 크게 보기 스크롤이 **649→632** 로 줄었다.
    ⛔ 의미·조건·`toCounter` 는 건드리지 않았다.

  · **로마자 화면의 입력·라벨 예외를 걷었다**(58/20px · 라벨 16 · 크게 보기 예외 2줄).
    두 칸이던 시절 폭이 좁아 깎아 둔 값인데 1열이 된 뒤로 이유가 없어졌다 — 한 입력 체계다.
    ⚠️ 그 결과 **크게 보기의 로마자만** 라벨 18.5→21 · 입력 23→24 로 올라간다. 나머지 전 화면
    Big 은 0건 변화이고, BAR compact 13.5→13 은 BAR 가 두 보기에서 같은 값이라 따라온 것이다.

  · 📌 **크게 보기의 세로 스크롤 증가는 받아들였다**(요약 930→975 · 경로선택 92→103 ·
    법정대리인 530→543). Big 은 정보밀도보다 **가독성이 먼저**다. 조건은 가로 넘침 0 ·
    잘림 없음 · PAPER/RAIL 침범 없음뿐이고, ⛔ 이것을 줄이려고 글자를 다시 깎지 않는다.
    일반 보기는 반대로 줄었다 — 로마자 456→**421** · 요약 794→**695** · 법정대리인 331→**244**.

  · 어디를: `passport-helper-v1.html`(지역 `:root`·ASK 컨트롤·Handoff) ·
    `kiosk-app/app/`(동기화) · `tests/screen-baseline.json`(갱신) ·
    `docs/디자인검토/shots/`(33장 재생성) · `docs/PRODUCT_SKIN_v1.md`(§1-2·§3-1~3-3·§6 신설/개정) ·
    `docs/UIUX_HANDOFF.md`(§4-1·4-2·4-2-1~4-2-4·4-9) · `docs/EXPANSION_PLAYBOOK.md`(동결 표·⑪).
    ⛔ `DESIGN-TOKENS`·`SITE-CONFIG`·인쇄 CSS·좌표맵·`SCREENS`·`FLOW`·`engine/`·7종 **무변경**.

- 📌 **여기까지가 v1 이다.** 이후 여권 화면은 **기능 오류 · 민원 업무 내용 오류 · 인쇄 오류 ·
  개인정보/보안 · 명백한 접근성 문제**일 때만 고친다. 단순 미관 개선은 Phase 2 로 넘긴다.
- 남은 일(Phase 2 · v1 의 미완성이 아니다): Main 신청서 카드 → Writing PAPER **shared-element
  전환** · **PAPER Focus**(부속서식) · 필요 시 ASK Dock · `MAIN-SKIN`/Product Skin 의 **실제 공통
  CSS 승격** · 나머지 **7종 이식**(`docs/PRODUCT_SKIN_v1.md` 를 한 번에 적용)과 `.phone` 정리 ·
  사용 중단된 종이 시연 코드·`FORM_IMAGES`(706KB) 정리.

---

## 2026.08.28 (2)

### [수정] 다른 기관에서 **서식이 많으면 직원 호출 단추가 화면 밖으로 밀리던 것**  · 확산 점검(사용자 지시)

- 무엇을: LEFT 기둥에서 **목록만 구르게** 하고, [어느 것인지 모르겠어요 — 직원에게
  여쭤볼게요]를 기둥 바닥에 **고정**했다(`.ask-foot` 신설). 목록이 구를 때만 그 위에
  실선 한 줄을 긋는다(`.task-col.scrolls` — JS 가 실제 넘침을 재서 켠다).
  목록 아이콘이 없는 `key` 는 **기본 서류 아이콘**으로 물러난다(빈 칸이 되지 않는다).
- 왜: 군포는 8종이라 안 걸리지만, **10종부터 크게 보기에서 단추가 화면 밖으로 밀렸다**
  (실측: 10종 1075px·12종 1245px·16종 1585px — 화면은 985px). 스크롤을 모르는 분에게는
  그 단추가 **없는 것과 같고**, 그러면 시민이 갇힌다(철학 §8). 취급 서식 수는 기관마다
  다르므로 이것은 군포의 문제가 아니라 **확산의 문제**다.
- 어디를: `index.html`(LEFT 마크업·CSS·`syncScrollHint()`·`iconHtml()` 대체 아이콘) ·
  `kiosk-app/app/index.html` · `docs/EXPANSION_PLAYBOOK.md` ⑨⑭(새 서식이 채울 필드를
  재설계 기준으로 고치고, `preview.*` 는 이제 안 쓴다는 것과 서식이 많을 때의 확인 항목 추가).
- 확인: **4·8·12·16종 × 일반/크게** 실측 — 단추 위치가 918~969px 로 **모두 같고**
  기둥 안에 남는다(종전 10종부터 밀림). 12종·크게에서 스크롤 표시가 켜지고 8종에서는 꺼진다.
  다른 기관 설정(안양시 · 대표색 `#0f6b4f` · 여권·부동산 끔)으로 갈무리해 **대표색이 배지·
  카드·CTA·아이콘까지 따라가는 것**을 확인했다(하드코딩된 파랑 없음).
  `verify-print.py` 19쪽 0px · `--electron` 19쪽 0px · `verify-site-config.py` 6개 ·
  `check-design-tokens`·`check-site-block` 9개 화면 동일 · `sync-kiosk --check` ·
  Idle 회귀(첫 누름·카드·CTA·직원 단추) 재확인.

---

## 2026.08.28

### [재설계] Main 을 Figma 시안 **원형 그대로** 이식 — 준비물 CENTER · 26/56/18 · 크게 보기 · Idle  · 사용자 요청(Figma Make 핸드오프)

- 무엇을: 허브(Main) 한 화면을 통째로 다시 만들었다. **인쇄물·8종 서식·공통 토큰은 무변경**이다.

  | | 종전 | 지금 |
  |---|---|---|
  | CENTER | A4 종이 자리(빈 윤곽→실루엣→실제 서식) | **준비물 + 작성할 신청서 + [작성 시작하기]** |
  | 열 비율 | 일반 2:3:1 · 크게 5:5:2 | **26 / 56 / 18 고정** — 크게 보기에서도 안 바뀐다 |
  | BAR | 74px + 대표색 3px 띠 | **62px** + 1px 경계선 |
  | 색 | 따뜻한 회색(공통 토큰) | Figma 중립 회색 — **Main 전용 스킨**(`MAIN-SKIN v1`) |
  | 카드 누름 | 그 자리에서 펼침판이 열림 | 가운데가 그 민원으로 바뀜(펼침판 없음) |
  | 대기 | 종이에 값이 한 줄씩 그어지는 시연(2.8초) | **실제 선택을 8초마다 옮기는 순회** |

  - 기관 대표색은 그대로 `--blue`(SITE-CONFIG 주입)이고, Figma 의 `accentAt()` 알파 파생을
    `color-mix(in srgb, var(--blue) N%, transparent)` 로 옮겼다. ⛔ `#2563EB` 를 박지 않았다.
  - 잉크 계층을 **역할별 하한선**으로 다시 정했다(`--m-ink-read` #4b5563 신설, 7.6:1).
    ⛔ 미선택 항목의 글자를 흐리게 해 선택을 표현하지 않는다 — 목록 8개 중 7개가 늘 미선택이다.
  - Main 전용 타이포 눈금 22개 + `html.big`. 읽는 글자 **+17~21%**, metadata **+4%**.
  - 미니 그래픽 5종은 Figma 원형을 그대로 옮겼고(색만 변수화), 「기존 여권」 한 장만 같은
    문법으로 새로 그렸다. 목록 아이콘은 **기존 한 벌을 지켰다**(사망·이혼 상징을 뒤집지 않으려고).

- 왜:
  - **CENTER 가 답해야 하는 질문이 바뀌었다.** 「어떤 종이인가」가 아니라 **「무엇을 준비하고
    무엇을 작성하는가」**다. 종이 미리보기는 고르기 전에는 읽을 것이 없었다.
  - 공공 민원실은 서서 읽는 자리다. Figma 의 연한 회색 위계(11.5~14px · #9ca3af)를 그대로
    쓰면 고령·저시력 이용자에게 **정보가 없는 것과 같아진다.**
  - 크게 보기는 확대가 아니라 **가독성**이다. 열 폭을 흔들면 같은 화면이 아니게 된다.
  - 대기화면은 광고판이 아니라 **사용해도 된다는 신호**다(철학 §22). 그래서 전용 화면 없이
    이 Main 이 스스로 고르고, 아무 입력에나 즉시 멈춘다.
  - **깨우는 첫 입력을 삼키지 않는다.** Idle 은 덮개가 아니라 Main 의 한 상태이므로, 첫 누름은
    「깨우기」가 아니라 그 사람의 첫 조작이어야 한다(종전에는 두 번 눌러야 했다).

- ⚠️ **없는 정보를 만들지 않았다.** Figma 시안의 `READY_DATA` 중 여권 외 7종은 샘플이라
  쓰지 않았다. 준비물이 검증된 것은 여권뿐이고, 나머지는 그 자리를 **비운 채로** 둔다.
  담당 창구 안내도 설정값이 없어 넣지 않았다 — 다음 단계에서 `ORG_KEYS` 로 붙인다.

- ⚠️ **종이 시연 코드는 지우지 않았다.** `renderPaper`·`silHtml`·`silWrite`·`showPaper`·
  `FORM_IMAGES`(706KB)·카탈로그 `preview` 는 **호출되지 않는 닫힌 섬**으로 남겨 두었다.
  실제 미사용 여부와 다른 의존성을 확인한 뒤 시연 이후 정리한다.

- 어디를: `index.html`(전면 — `MAIN-SKIN v1` 신설 · Main CSS/마크업/JS) ·
  `kiosk-app/app/index.html`(`sync-kiosk.sh`) · `tools/design-shots.py`(허브 갈무리 시나리오를
  `selectForm()` 기준으로 정합 — 종전 시나리오는 지금 없는 함수를 불러 조용히 실패했다) ·
  `docs/UIUX_HANDOFF.md`(QA 항목). **`engine/`·서식 8종·`DESIGN-TOKENS`·인쇄 CSS 무변경.**

- 확인:
  - `verify-print.py` **19쪽 0px** · `--electron` **19쪽 0px**(허브는 인쇄하지 않지만 CSS 를
    크게 건드려 두 경로 다 돌렸다)
  - `verify-site-config.py` 6개 · `check-design-tokens.py`·`check-site-block.py` 9개 화면 동일 ·
    `make-form-previews.py --check` · `sync-kiosk.sh --check` · `node --check` 4개 블록
  - **실측(헤드리스 1920×1080, 배율 1)**: 열 폭 495/1066/343 이 두 모드 동일 · 카드 8장 높이
    70→80px 균일(줄바꿈 없음) · LEFT·CENTER·RIGHT 넘침 0 · 가장 긴 민원명/서식명으로도 0
  - **Idle 실측**: 8초 순회 10걸음(한 바퀴 뒤 되돌아옴) · 3분 대기 · 입력 즉시 중단 ·
    **첫 누름이 그대로 동작**(카드 선택 / CTA 진입 / 크게 보기 / 직원 안내 각각 한 번에) ·
    키·휠·누름의 `defaultPrevented` 전부 false · reduced-motion 이면 안 돎 · `formOn` 필터 ·
    취급 서식 1종 이하면 순회 없음 · 시연 중 RIGHT 불변
- 남은 일: 담당 창구 설정 연결 · 세로(1080×1920) 전용 배치 · 카드→PAPER shared-element 전환 ·
  `docs/디자인검토/shots` 재생성(Main 이 바뀌어 낡았다) · 종이 시연 dead code 정리.

---

## 2026.08.27 (4)

### [반영] 모션 **값이 정해졌다** — 세 박자(120·200·320)로  · 외부 검토 과제 A
- 무엇을: `DESIGN-TOKENS` 의 모션 여섯 줄을 외부 검토 답으로 갈아 끼웠다.

  | 토큰 | 종전 | 지금 |
  |---|---|---|
  | `--dur-quick` | 120ms | **120ms**(그대로) |
  | `--dur-base` | 220ms | **200ms** |
  | `--dur-calm` | 340ms | **320ms** |
  | `--ease-out` | `cubic-bezier(.22,.61,.36,1)` | **`cubic-bezier(.2,0,0,1)`** |
  | `--ease-in` | `cubic-bezier(.55,.06,.68,.19)` | **`cubic-bezier(.4,0,1,1)`** |
  | `--ease-move` | `cubic-bezier(.4,0,.2,1)` | **그대로** |

- 왜: 지속시간은 20ms 씩만 움직였다 — **지금 화면이 크게 틀리지 않았다는 뜻**이고,
  실제로 바뀐 것은 **easing** 이다. 셋이 제각각이던 곡선을 한 가족으로 맞췄다
  (빠르게 나와 길게 멎는 out · 가속하며 물러나는 in · 양끝이 완만한 move).
- ⚠️ **동작 배정 12개 중 6개는 이미 제안대로였다**(카드 hover·종이 교체·진행 막대·
  선택 카드·입력 오류 없음·개인정보 덮개 없음). 밖에서 온 답이 **지금 코드를 확인해 준
  셈**이라, 채점표 §9-5(「전부 바꾸자면 재설계」)에 걸리지 않았다.
- 카드 펼침판의 **여닫는 방향에 다른 easing** 을 줬다(#2) — 펼칠 때 `--ease-out`,
  접을 때 `--ease-in`. 규칙 둘로 갈라 여는 쪽에만 out 을 적었다.
- ⚠️ **넣지 않기로 한 둘을 그대로 지켰다**(#7 입력 오류 · #9 개인정보 덮개).
  「진행 불가와 개인정보 차단은 연출보다 시점이 먼저」라는 답의 근거가 철학 §11 과 같다.
- ⚠️ **남긴 넷**은 값이 아니라 **구조를 바꿔야 하는 것**이라 이번에 넣지 않았다 —
  #8 인쇄 전 모달(`display` 로 여닫아 전환이 안 걸린다) · #10 크게 보기(가능하지만
  성능 미측정) · #11 질문 전환(`innerHTML` 재생성이라 transition 이 안 걸린다) ·
  #12 Current Focus(오버레이가 매번 재생성된다 — **인쇄 경로 옆이라 위험하다**).
- 어디를: `engine/base.html` 의 `<!--DESIGN-TOKENS v1-->` → 5종 재빌드 →
  `check-design-tokens.py --fix` 로 손작성 3종·허브 반영 → `index.html`(#2) → `sync-kiosk.sh`
- 확인: verify-print.py **19쪽 0px** · **--electron 19쪽 0px** · verify-site-config(6) ·
  check-site-block / check-design-tokens / sync-kiosk --check 통과 ·
  **갈무리 33장 전부 픽셀 동일** — 정지 화면은 한 점도 안 움직였다(순수한 모션 변경) ·
  `getAnimations()` 로 `grid-template-columns` 가 애니메이션 대상임을 확인(#10 판단 근거)

---

## 2026.08.27 (3)

### [문서] 의뢰서를 **넷으로 나눴다** — C(아이콘)·D(대기 시연) 신설  · 사용자 요청
- 무엇을: 디테일을 밖에 물을 때 **「한 벌 단위」로 묶어** 묻기로 하고, 의뢰서 둘을 신설했다.
  - `2026.08.27-의뢰서-C-아이콘-한-벌.md` — 아이콘 10개
  - `2026.08.27-의뢰서-D-대기-시연.md` — 시연의 리듬·순서·채우는 방식·한 바퀴 뒤·멈춤·감소 모션
- **형식이 다르다 — 「우리 안 + 대안 요청」이다.** 빈 종이를 주고 「그려 오세요」가 아니라
  **지금 쓰는 것을 그대로 싣고** 「이보다 나으면 그것만 주시고, 없으면 그대로 두라고 말해
  주십시오」라고 묻는다. C 에는 `path` 문자열 10개를, D 에는 초 단위 표를 그대로 넣었다.
- 왜: ⛔ **디테일 단위로 쪼개 의뢰하면 손해다.** ① 값 사이의 관계(대비·리듬)가 깨지고
  ② 한 라운드의 왕복 비용이 그 값보다 크고 ③ 9월 개시까지 라운드가 몇 번 안 남는다.
  묶는 단위의 조건은 셋이다 — **그 자체로 완결된 한 벌인가 · 합격/불합격을 판정할 수
  있는가 · 통째로 갈아 끼워도 다른 데가 안 무너지는가.**
- **「그대로 두십시오」도 정상적인 답**이라고 못 박았다. 채점표에도 넣었다
  (「다르다」는 「낫다」가 아니다).
- ⚠️ **두 번 묻지 않게 A·B 의뢰서를 정리했다** — A-2 의 #4·#13(시연 관련)과 A-3 을 빼고
  의뢰서 D 로 넘겼다. 토큰 값과 연출을 같이 물으면 답이 섞여 견줄 수 없다.
- ⚠️ C 에 **우리가 스스로 의심하는 곳 다섯**을 적어 두었다(국화가 21px 에서 뭉갠다,
  출생과 이혼의 사람 형태가 겹친다 …). 밖에서 볼 곳을 좁혀 주면 답의 질이 올라간다.
- ⚠️ D 에 **⛔ 뒤집을 수 없는 것 여덟**을 앞세웠다. 여러 번 뒤집어 본 결론이라
  되돌리는 안을 받으면 같은 논의를 처음부터 다시 하게 된다.
- 어디를: 의뢰서 C·D 신설 · `2026.08.25-의뢰서-모션과-시각.md`(A-3 이관·제목에 A·B 표기) ·
  `0-먼저-읽어-주세요.md`(§1 을 의뢰서 넷 표로, §3 파이프라인)
- 확인: `design-bundle.py` 74개 파일로 묶임 · 디자인검토 폴더 링크 전수 검사 통과 ·
  **코드 무변경**(인쇄물·화면 그대로)

---

## 2026.08.27 (2)

### [반영] 시연에서 **종이가 실제로 채워진다** — 이 화면의 한 문장을 눈앞에서  · 사용자 요청
- 무엇을: 대기 시연의 실루엣에 **그 서식이 실제로 묻는 항목**이 서고, 오른쪽 칸에
  **값이 한 줄씩 채워진다.** 왼쪽에서 오른쪽으로 드러나 손으로 적어 나가는 결이다.
  한 카드에 머무는 시간을 **1.8 → 2.8초**로 늘렸다(8종 한 바퀴 22.4초).
- 왜: 몇 시간 전에 만든 실루엣은 **추상적인 줄**만 그어졌다. 조용하기는 했지만
  「여기서 무엇을 할 수 있는가」를 말하지 못했고 지나가는 사람이 멈춰 설 이유가 없었다.
  이 제품의 한 문장 — **「질문에 답하면, 신청서가 완성됩니다」** — 이 눈앞에서 일어나는 것이
  가장 강한 신호다(철학 §22 「실제 작성 경험과 연결되는 Paper」).
- ⚠️ 값은 **전부 가상 인물**이고 종이 아래에 **「작성 예시입니다」**라고 적었다.
  앞사람 개인정보가 남아 있는 것처럼 보이면 이 제품의 가장 큰 강점이 그 자리에서 무너진다.
- ⚠️ **주민등록번호 뒷자리는 가상값이라도 가린다**(`900101-●●●●●●●`). 늘 켜져 있는 화면에
  숫자 13자리가 통째로 떠 있는 그림 자체를 만들지 않는다. 서식 안의 작성예시와는 다른 판단이다
  — 그쪽은 본인이 보는 자기 화면이고, 이쪽은 **아무나 지나가며 보는 화면**이다.
- ⛔ 폐기한 **Generic Paper**(가상의 ○○○신청서)를 되살린 것이 아니다. 실제 8종 **각각의
  항목**이고 종이는 여전히 실루엣이다 — 실제 서식 그림은 「고른 것」에만 쓴다(네 단 규칙 그대로).
- ⚠️ 줄 간격을 `4cqh → 6cqh` 로 벌렸다. 다섯 줄이 종이 위쪽에만 뭉쳐 있었다.
- 어디를: `index.html`(카탈로그 `preview.sample` 8종 · `silHtml()` · `.sil-*`) → `sync-kiosk.sh`

### [반영] 카드 아이콘을 **이모지 → 단색 선 아이콘 한 벌**로  · 사용자 요청
- 무엇을: 서식 8종 카드와 상단 바 [크게 보기]·[직원에게 여쭤볼게요]의 아이콘을
  **인라인 SVG** 로 바꿨다. 24 격자 · 굵기 1.7 · 둥근 끝 · `currentColor`.
- 왜: 이모지는 ① **OS·글꼴에 따라 모양이 달라진다** — 배포본 하나를 여러 기관이 쓰는데
  화면이 갈린다. ② **여러 색을 쓴다** — 철학 §15 는 색마다 하나의 역할을 주고 §1 은 장식
  목적의 여러 색을 금하는데, 목록 8장에 8가지 색이 박혀 있었다.
- 평소에는 **잉크색**을 따르고 hover·포커스·**고른 카드**에서만 대표색이 된다 — 그때는
  색이 「골라짐」이라는 **뜻**을 갖는다.
- ⚠️ **사망·이혼은 감정이 실리는 민원**이다. 십자가·비둘기·깨진 하트 대신
  **국화 한 송이**와 **담담한 두 사람**으로 두었다.
- ⚠️ 카탈로그의 `icon`(이모지)은 **대비책으로 남겼다** — 아이콘이 없는 서식이 생기면 그것으로 물러난다.
- ⚠️ [크게 보기]는 여권 화면에도 있다. **한쪽만 고치면 상단 바가 갈라지므로** 함께 고쳤다.
  글자만 바뀌도록 아이콘과 라벨을 갈랐다(`#btnBigLabel`).
- ⚠️ 남은 이모지는 `m-preview`(좁은 화면 전용) 하나뿐이다 — 키오스크 해상도에서는 안 보인다.
- 어디를: `index.html` · `passport-helper-v1.html` → `sync-kiosk.sh`

### [반영] 오른쪽 기둥의 **가운데 구멍**을 메웠다 — 정보를 더 넣지 않고  · 사용자 요청
- 무엇을: MAIN 의 RAIL 에서 위 묶음을 아래로 내리고(`margin-top:13vh`) 항목 간격을 벌렸다.
  구멍이 425px → 200px 남짓이 됐다.
- 왜: MAIN 의 RAIL 은 위에 **한 묶음뿐**이고 아래 안심 안내는 바닥 고정이라, 가운데가
  통째로 비어 「조용한」 것이 아니라 **「덜 만든」 것**으로 읽혔다.
- ⛔ **정보를 더 넣어 채우지 않았다**(2026.08.27 결정 ⑤ — 없으면 여백을 허용한다).
  **여백이 의도로 읽히도록 자리와 리듬만** 고쳤다.
- ⚠️ 세로 배치에서는 기둥이 가로 띠가 되므로 미디어 쿼리에서 되돌렸다.
- 어디를: `index.html`

### [문서] 위 셋을 문서·동결본에 반영
- `docs/UX_DECISIONS.md` — ③에 「그 뒤에 한 번 더」를 덧붙이고 **⑯ 아이콘** 결정을 신설
- `docs/디자인검토/2026.08.24-...구조동결-v1.md` — 시연 규격(2.6 → 2.8초 · 채워지는 종이)
- `docs/디자인검토/2026.08.25-의뢰서-모션과-시각.md` — A-2 #13 · A-3 · §6 에 **아이콘 조항** 추가
  (⛔ 이모지로 되돌리는 안·설치가 필요한 아이콘 세트는 받지 않는다)
- `docs/history/2026.08.27-Main재설계/` — After 4장을 **지금 화면으로 다시 동결**
- 확인: verify-print.py **19쪽 0px** · **--electron 19쪽 0px** · verify-site-config(6) ·
  check-site-block / check-design-tokens / sync-kiosk --check / make-form-previews --check 통과 ·
  **9개 화면 `window.onerror` 무오류** · 헤드리스 동작 11가지 확인(시연=채워지는 실루엣 ·
  항목 5개 · 예시값 · hover 는 안 채워짐 · 카드 아이콘 SVG 8개 · 단추 아이콘 2개 ·
  크게 보기 라벨 전환) · 갈무리 33장 재생성

---

## 2026.08.27

### [반영] 종이 자리를 **네 단**으로 — 실제 서식은 「고른 것」에만 쓴다  · 사용자 요청
- 무엇을: 허브 가운데 종이가 상태에 따라 **네 가지**로 갈린다.

  | 언제 | 종이 |
  |---|---|
  | 아무것도 안 고름 | 빈 윤곽 + 「서식을 골라 주세요」 |
  | **시연** | **실루엣** — 서식 이름 + 항목 줄. 줄이 **하나씩 그어진다** |
  | **hover·키보드 포커스** | **실루엣** — 정지. 줄은 비어 있다 |
  | **고름**(카드 펼침) | **실제 서식**, 흰 베일 한 겹으로 살짝 흐리게 |
  | 작성(Writing 화면) | 실제 서식, 또렷 ← 페이지가 바뀌는 순간 선명해진다 |

- 왜: 이틀 전에는 **시연·hover·고름이 셋 다 실제 서식**을 띄웠다. 그래서 ①카드를 눌러도
  화면이 그대로인 것처럼 보여 **누름이 아무 일도 안 한 것으로 읽혔고**, ②빽빽한 정부 서식이
  몇 초마다 바뀌어 대기 화면이 시끄러웠다. **선명도를 「어디까지 정해졌는가」의 눈금으로**
  삼으면 두 가지가 같이 풀린다 — 고름→작성은 기하가 같아 화면이 안 움직이는 전환인데,
  종이가 또렷해지는 것이 그 순간을 대신 말해 준다.
- ⛔ 실루엣은 **실제 서식의 칸 이름을 베끼지 않는다.** 줄 수와 길이만 쓴다(없는 것을
  지어내지 않는다). 길이는 서식 이름에서 계산해 **같은 서식이면 늘 같다** — 찍을 때마다
  갈무리가 달라지지 않는다.
- ⛔ 베일에 `filter:blur()` 를 쓰지 않았다. 700×990 그림에 blur 는 재사용 사무용 PC 에
  부담이다(철학 §19). 흰 겹 한 장이면 합성만으로 끝난다.
- ⚠️ **베일은 그림 위에만 덮는다.** 처음에는 `.paper-fit` 전체에 덮었는데 종이 둘레의
  바닥까지 밝아져 **구역 안에 밝은 사각형**이 생겼다. 그림을 `.img-hold` 로 감싸고
  실루엣과 **같은 식으로 크기를 정했다**(A4 비율 · `cqh` 로 잰다) — 그래야 고를 때
  실루엣이 실제 서식으로 **바뀌는** 것으로 읽힌다.
- ⚠️ 시연 한 바퀴를 **2.6초 → 1.8초**로 줄였다(8종이면 21초 → 14.4초). 종이가 실루엣이라
  읽을 것이 적어졌으므로 오래 머물 이유가 없다. 한 카드에 머무는 동안 줄 5개가 다 그어진다.
- 어디를: `index.html`(허브) → `bash tools/sync-kiosk.sh`
- 확인: verify-print.py **19쪽 0px** · **--electron 19쪽 0px** · verify-site-config(6) ·
  check-site-block / check-design-tokens / sync-kiosk --check / make-form-previews --check 통과 ·
  **9개 화면 `window.onerror` 무오류** · 네 단 전환 9가지를 헤드리스로 확인
  (시연=실루엣 · 시연 정지=빈 자리 · hover=줄 안 그어진 실루엣 · 고름=실제 서식+베일 ·
  베일 상자가 종이만 덮음 601px/908px · 종이 비율 0.7071 · 다시 눌러 접으면 빈 자리) ·
  갈무리 33장 재생성

### [수정] 세로 화면에서 종이가 **화면 밖으로 밀려났다**  · 위 작업 중 발견
- 무엇을: 세로 배치의 `.layout` 행을 **`auto auto 1fr`** 로 못 박았다. 남는 높이를 종이가
  가져간다.
- 왜: 세 칸이 다 내용 높이로 자라 합이 화면을 넘었다. 넘친 만큼은 `overflow:hidden` 이
  잘라 내므로 **종이가 아래가 잘린 채** 끝났다 — 키오스크에는 스크롤을 모르는 사람이 많아
  이건 보기 문제가 아니라 **기능 문제**다.
- ⚠️ 종이 크기를 `cqh`(종이 칸의 높이)로 재기 시작하면서 드러났다. 종전에는 그림이
  `max-height` 로 알아서 줄어 **잘린 것이 눈에 덜 띄었을 뿐** 배치는 그때도 넘쳐 있었다.
  실루엣은 글자를 담고 있어 **서식 이름이 세로로 한 글자씩 쪼개져** 바로 드러났다.
- 확인: 1080×1920 에서 시연·첫 화면·카드 펼침 셋 다 **스크롤 없이 종이가 통째로** 보인다
- 어디를: `index.html`

### [수정] 갈무리 도구가 새 상태를 모르고 있었다  · 위 작업 중 발견
- 무엇을: `tools/design-shots.py` 가 `renderPaper()` 에 **어느 단인지**를 넘긴다.
  시연 갈무리는 실루엣의 줄을 **마지막 한 줄만 남기고** 그어 「쓰이고 있는 중」에서 세운다.
- 왜: 인자 없이 부르면 종전 동작(실제 서식)으로 떨어져 **시연 갈무리에 실제 서식이 찍혔다** —
  코드는 실루엣인데 밖에 나가는 자료만 옛 모습이었다. 카드 펼침 갈무리도 베일이 빠졌다.
- ⚠️ 줄은 **전환을 끈 뒤에** 긋는다. 켜 둔 채로 붙이면 폭이 자라는 중에 셔터가 떨어져
  같은 파일을 두 번 찍어도 그림이 다르다(2026.08.25 의 흔들림과 같은 뿌리다).
- 어디를: `tools/design-shots.py`

### [문서] 의뢰서·안내문을 지금 화면에 맞췄다
- 무엇을: 의뢰서에 **동작 13번(줄 그어짐)**을 더하고, A-3 의 시연 리듬을 1.8초로 고쳤다.
  「⛔ 뒤집지 마십시오」에 **선명도가 곧 확정의 단계**라는 항목을 넣었다.
  `0-먼저-읽어-주세요.md` §5 는 「7종이 아직 옛 언어」라고 적혀 있던 것을 **자리는 맞췄고
  흐름만 남았다**로 고쳤다 — 2026.08.25 의 작업이 문서에 반영돼 있지 않았다.
- 어디를: `docs/디자인검토/2026.08.25-의뢰서-모션과-시각.md` ·
  `docs/디자인검토/0-먼저-읽어-주세요.md` · `docs/디자인검토/README.md`

---

## 2026.08.25

### [반영] 대기 화면을 따로 두지 않는다 — **첫 화면이 스스로 시연한다**  · 사용자 요청
- 무엇을: 아무도 안 건드리면 허브가 **왼쪽 카드를 2.6초마다 하나씩 짚으며 가운데 종이를
  그 서식의 실제 1쪽으로 바꾼다.** 누군가 쓰고 있는 것처럼 보인다.
  ⚠️ **2026.08.27 에 바뀌었다** — 시연의 종이는 이제 **실루엣**이고 간격은 1.8초다(위 참조).
  마우스가 **움직이기만 해도**(키·터치·휠도) 멈추고 첫 화면이 된다.
- 왜: 철학 §22 가 대기 화면에 요구하는 것은 둘이다 — ①「써도 된다」를 알릴 것,
  ②「실제 서비스로 넘어갈 때 다른 제품처럼 느껴지지 않을 것」.
  **②는 화면을 나누지 않으면 저절로 지켜지고**, ①은 **쓰는 모습을 그대로 보여 주는 것**보다
  잘 알리는 방법이 없다. 가상 신청서가 저절로 채워지는 것보다, 진짜 목록이 짚히고 진짜
  서식이 바뀌는 편이 「여기서 무엇을 할 수 있는가」까지 같이 말한다.
- ⛔ **하루 전에 만든 별도 IDLE 화면(배치 0·Generic Paper·깨어남)을 걷어냈다.** 화면이
  하나면 이을 자리가 없다 — 「깨어남」이라는 전환을 설계할 일 자체가 사라졌다.
- ⛔ 시연이 짚는 카드에 **「골라짐」 표시를 쓰지 않는다.** 시연이 끝난 뒤에도 시민이 고른
  것처럼 남는다. 코드에서도 `.picked` 가 아니라 `.demo-on` 이라는 다른 이름을 쓴다.
- ⛔ 시연이 **카드 펼침판까지 열지 않는다.** 여닫을 때마다 아래 카드가 밀려내려가 목록이
  출렁인다 — 대기 화면은 조용해야 한다.
- ⚠️ `prefers-reduced-motion` 이면 **시연을 아예 돌리지 않는다.** CSS 규칙은 transition 만
  끄므로 JS 에서 `matchMedia` 로 직접 갈랐다(여권 `calmScroll()` 과 같은 방식).
- ⚠️ 시연이 막 시작하면 커서 아래 요소가 바뀌어 브라우저가 `mousemove` 를 **합성한다.**
  그것에 즉시 꺼지지 않도록 시작 직후 0.4초는 움직임만 무시한다.
  `pointermove` 는 초당 수십 번 오므로 무동작 시계는 **1초에 한 번만** 되감는다.
- 다시 시연으로 돌아가는 시간은 키오스크 환경설정의 `idleMs`(기본 3분) — 서식 8종과 같은
  값이라 창구에서 두 가지를 따로 맞출 일이 없다. ⚠️ 허브에는 지울 개인정보가 없으므로
  8종과 달리 **30초 경고를 띄우지 않는다.** 잃을 것이 없는데 경고하면 소음이다.
- 어디를: `index.html`(허브) → `bash tools/sync-kiosk.sh`
- 확인: verify-print.py **19쪽 0px** · **--electron 19쪽 0px** · verify-site-config(6) ·
  check-site-block / check-design-tokens / sync-kiosk --check 통과 ·
  동작 8가지를 헤드리스로 확인(첫 화면=시연 · 카드가 넘어감 · 마우스 움직이면 끝 ·
  카드는 펼치기만 · 다시 누르면 접힘 · 다른 카드로 옮겨감 · 감소 모션에서 안 돎 ·
  무동작 뒤 재개) · [작성 시작]이 실제로 여권 화면으로 넘어가는 것까지 확인

### [반영] 첫 화면에 **아무것도 골라져 있지 않다**  · 사용자 요청
- 무엇을: 종전에는 첫 서식(여권)이 이미 골라진 채로 오른쪽에 떠 있었다. 이제 **비어 있고**
  그 자리에 종이 윤곽과 **「서식을 골라 주세요」**가 온다.
- 왜: 시민이 **고르지도 않은 것이 골라져 있는 셈**이라 「지금 무엇을 보고 있는가」가 헷갈렸다.
- ⛔ **hover 에만 의존하지 않는 원칙은 그대로다**(2026.08.24 결정 §2). 그 원칙 때문에
  첫 서식을 띄워 두었던 것인데, **빈 자리가 대신 말을 걸므로** 터치에서도 빈 화면이 아니다.
  hover·키보드 포커스는 **아무것도 안 골랐을 때만** 미리보기를 바꾼다 — 펼쳐 놓은 카드가
  있으면 그것을 이긴다(열어 둔 것을 hover 가 가려서는 안 된다).
- 어디를: `index.html`

### [반영] 카드를 눌러도 **바로 넘어가지 않는다** — 그 자리에서 펼쳐진다  · 사용자 요청
- 무엇을: 카드를 누르면 그 카드가 아래로 펼쳐져 **설명 · 몇 가지·몇 분 · 준비물 ·
  [작성 시작 →]** 을 낸다. **[작성 시작]을 눌러야** 서식으로 간다. 같은 카드를 다시 누르면
  접히고, 다른 카드를 누르면 그쪽으로 옮겨간다(한 번에 하나만 열린다).
- 왜: 누름 한 번에 페이지가 바뀌면 **잘못 누른 사람이 되돌아올 길이 없다.**
- 이것이 폐기된 START 화면(2026.08.24)이 하려던 일을 **화면을 늘리지 않고** 하는 방법이다.
  카탈로그의 `desc`·`ready`·`scale` 이 여기서 다시 쓰인다 — 하루 전 「읽는 곳이 없어졌다」고
  적어 둔 것이 해소됐다.
- ⛔ **막지 않는다.** 준비물이 없어도 [작성 시작]으로 갈 수 있다.
- ⚠️ **없는 것을 지어내지 않는다.** `ready`·`scale` 이 비면 그 줄을 아예 내지 않는다 —
  지금은 **여권만** 채워져 있고 나머지 7종은 설명 한 줄이다.
- ⚠️ 카드는 `<button>` 이라 그 **안에** [작성 시작]을 넣을 수 없다(단추 안의 단추는 HTML
  위반이고 터치에서 엉뚱한 것이 눌린다). `.card-wrap` 으로 감싸고 펼침판을 형제로 두었다.
  여는 높이는 `grid-template-rows: 0fr → 1fr` 로 잰다 — ⛔ `max-height` 추측값을 쓰면
  준비물이 긴 여권에서 내용이 잘린다.
- 어디를: `index.html`

### [조사] 부가 서식을 돌려 보여 주는 안 — **접었다**
- 무엇을: 「고른 서식의 부가 서식을 느리게 순환시키자」는 안을 실물을 세어 보고 접었다.
- 왜: 돌릴 것이 있는 서식이 **여권 하나뿐**이었다. 여권은 신청서 + 법정대리인 동의서
  (미성년일 때만, 위임장은 키오스크 경로에서 안 쓴다), **부동산 별지는 서식이 아니라 값이
  있어야 생기는 생성물**이라 빈 상태에서는 나오지 않고, 나머지 6종은 1쪽이다.
  그 한 장을 위해 빈 서식을 따로 렌더해 그림 200KB 를 더 실어야 했다 — **값이 비용을 못 넘는다.**
- ⚠️ 「종이가 심심하다」는 **시연이 카드를 짚어 갈 때 종이가 계속 바뀌는 것**으로 이미 풀린다.

### [수정] `[hidden]` 으로 감춘 것이 화면에 남아 있었다  · 위 작업 중 발견
- 무엇을: `.gp-empty`·`.gp-img` 에 `[hidden]{display:none}` 을 붙였다.
- 왜: `[hidden]` 은 브라우저 **기본 규칙**이라 클래스의 `display:flex/block` 에 **진다.**
  그래서 `hidden=true` 로 감춘 「서식을 골라 주세요」가 서식 그림 옆에 그대로 떠 있었다.
- ⚠️ `.gp-img` 쪽은 **종전부터 있던 잠재 결함**이다 — 그림이 없는 서식이 생겼을 때 빈 `img`
  가 자리를 차지했을 것이다. 지금 8종은 다 그림이 있어 드러나지 않았다.
- 어디를: `index.html`

### [반영] 7종을 여권과 **같은 결**로 — 상단 바 · 좌우 배치 · 단계 표시  · 정식개시 준비
- 무엇을: 여권 아닌 7종(엔진 5종 + 손작성 혼인·이혼)의 **자리**를 여권에 맞췄다.
  ⛔ **흐름은 그대로 두었다** — 3영역 전환·큰 카드 흐름은 별도 과제다(아래 「남긴 것」).

  | | 종전 | 지금 |
  |---|---|---|
  | 상단 바 | `flex` · 서식 이름이 왼쪽 · **작성예시가 시민 버튼 사이에** | 격자 `1fr auto 1fr` · 서식 이름 **정중앙** · 담당자 도구는 **왼쪽 묶음** |
  | 좌우 | 서식 왼쪽 / 패널 오른쪽 400px · `max-width:1420px` | **질문면 왼쪽 / 서식 오른쪽** · 화면 전폭 |
  | 단계 | 동그라미 스텝퍼 | **「진행 n / N」 + 막대 + 지나온 단계** |

- 왜: 같은 키오스크에서 **서식마다 조작법이 갈렸다**(`docs/디자인검토/README.md` §5-1·2·4).
  시민이 여권을 쓰고 나서 출생신고서를 열면 다른 프로그램처럼 보였다.
- 질문면 폭을 `1fr : 2fr` 로 둔 것은 우연이 아니다 — 1920 에서 **640px** 이 되어
  여권의 질문면(`--cols` 2:3:1 의 첫 칸)과 **똑같아진다.** 7종에는 RAIL 이 없으니 그 몫이
  서식으로 간다. 읽는 폭은 `--question-max` 로 막아 한 줄이 길어지지 않게 했다.
- ⛔ **되돌아가는 길을 없애지 않았다.** 동그라미를 지우면서 지나온 단계로 뛰는 기능을 같이
  잃으면, 요약 화면에 줄마다 「수정」이 없는 7종에서는 [이전]을 여러 번 눌러야 한다.
  지나온 단계는 **가로로 조용한 글자 링크**로 남겼다(여권의 안내 기둥 「묶음 목록」과 같은 구실).
- ⚠️ **곁들여 고친 것 셋** — 셋 다 자리를 옮기다 드러난 기존 결함이다.
  1. **[이전]·[다음]이 화면 밖으로 잘렸다.** `.wiz-body{max-height:calc(100vh - 320px)}` 로
     상단 바 높이를 숫자로 뺐던 탓이다. body 를 세로 flex 로 두어 **질문 본문만 스크롤하고
     버튼은 바닥에 남게** 했다(여권과 같은 방식). 키오스크에서 스크롤을 모르면 다음으로
     갈 수 없었으니 **기능 문제**였다.
  2. **패널 머리에 기관 로고가 또 있었다** — 상단 바에 이미 있어 한 화면에 둘이었다.
     표식(`<!--GUNPO-LOGO-->`)은 도구가 쓰므로 남기고 감췄다.
  3. **세로 화면에서 패널이 서식 위에 겹쳐** 글자가 가려졌다. 좌표가 `%`·`cqw` 기반이라
     `.form-col .paper{width:min(210mm,100%)}` 로 폭만 맞추면 비율이 유지된다(여권과 같다).
     ⛔ `@media screen` 안에 두었다 — 인쇄는 210mm 고정이어야 한다.
- ⚠️ **겪은 것**: 손작성 2종의 작성예시 버튼 class 가 `btn ghost hide-m` 이었는데 정규식이
  `btn ghost` 로만 잡아 **버튼이 통째로 빠졌고**, `addEventListener` 가 null 에서 터져
  혼인·이혼 패널이 백지가 됐다. 화면 갈무리로 잡았다. 이후 **9개 화면 전부를 열어
  `window.onerror` 로 확인**하는 검사를 돌렸다 — 수술 직후에 먼저 할 일이었다.
- 어디를: `engine/base.html` · `engine/engine.js` → 5종 재빌드 ·
  `marriage`·`divorce-helper-v1.html` 직접 수정 → `sync-kiosk.sh`
- 확인: verify-print.py **19쪽 0px** · **--electron 19쪽 0px** · verify-site-config(6) ·
  check-design-tokens / check-site-block / sync-kiosk --check / make-form-previews --check 통과 ·
  **9개 화면 `window.onerror` 무오류** · 갈무리 33장 재생성 · 1920·1080×1920 눈으로 대조

### [남긴 것] 7종의 **흐름**은 그대로다 — 3영역 전환은 별도 과제
- 여권만 `task-col`/`rail-col`/`SCREENS`(상태기계)를 갖고 있고, 7종은 **입력 폼 + 단계** 방식이다.
  여권처럼 바꾸려면 `engine/base.html` 골격 + `engine.js` + 손작성 2종 + **서식별 문항 흐름
  재설계**(config 5종)까지 가야 한다. 개시 직전에 8종의 인쇄 경로를 흔들 일이 아니다.
- 그때 함께 할 것 — 요약 줄마다 「수정」(여권이 2026.08.21 에 한 것), RAIL 신설,
  세로 화면 별도 배치(`README.md` §5-7).

### [정리] 모션 값 5가지 → 토큰 6개  · 외부 의뢰 전 준비
- 무엇을: `--dur-quick`·`--dur-base`·`--dur-calm`·`--ease-out`·`--ease-in`·`--ease-move` 를
  `DESIGN-TOKENS` 블록에 신설하고, **5원본 8곳에 박혀 있던** 지속시간을 그리로 모았다.
- 왜: 모션 토큰이 **아예 없었다.** 값이 벌써 다섯 가지였고(`.12s` `.15s` `.2s` `.22s` `.34s`)
  easing 은 대부분 **방치**돼 브라우저 기본값으로 굴러갔다 — Radius 13가지와 같은 뿌리다.
  이 상태로는 **밖에서 시안을 받아도 붙일 자리가 없다.** 이제 여섯 줄만 갈아 끼우면 된다.
- ⚠️ **값이 정해진 것이 아니다.** 지금 화면을 그대로 옮겨 둔 것이고,
  `docs/디자인검토/2026.08.25-의뢰서-모션과-시각.md` 로 밖에 물어 둔 상태다.
- 실제로 달라진 값은 둘 — 엔진 스텝 점 `150→120ms`, 여권 진행 막대 `200→220ms`.
  전환 시간이라 정지 화면에는 안 나오고, 20~30ms 는 눈으로 가려지지 않는다.
- 어디를: `engine/base.html` 의 토큰 블록 → 5종 재빌드 → `check-design-tokens.py --fix` 로
  손작성 3종·허브 반영 → 각 화면의 `transition:` 8곳을 토큰으로 교체
- 확인: verify-print.py **19쪽 0px** · verify-site-config(6) · check-design-tokens /
  check-site-block / sync-kiosk --check 통과 · **감소 모션 고정 비교에서 허브 기본·펼침
  두 상태 픽셀 동일** · 엔진 4종·여권·혼인도 HEAD 와 픽셀 동일

### [수정] 갈무리가 같은 파일을 두 번 찍어도 달라졌다  · 위 작업 중 발견
- 무엇을: `tools/design-shots.py` 가 화면을 세워 놓은 뒤 **전환을 통째로 끄고** 찍는다.
- 왜: 허브는 열자마자 시연이 돈다. 시연을 멈추는 것만으로는 **카드 강조가 풀리는 전환이
  도는 중에** 셔터가 떨어져, 같은 파일을 두 번 찍어도 그림이 달랐다(실측으로 확인).
  갈무리는 리포에 커밋되므로 **바뀐 것이 없는데 diff 가 생긴다.**
- 확인: 같은 조건으로 두 번 찍어 픽셀 동일
- 어디를: `tools/design-shots.py`

### [문서] 의뢰서 신설 — 모션·시각의 **값**을 밖에 묻는다
- 무엇을: `docs/디자인검토/2026.08.25-의뢰서-모션과-시각.md` 신설.
  과제 A(모션)·B(시각)를 갈라 놓고, **답변 형식을 표로 못 박았다.**
- 왜: 여러 곳에 같은 문서를 주고 **답을 나란히 놓고 고르기** 위해서다.
  형식이 다르면 견줄 수 없고, CSS 파일·시안 이미지로 오면 붙일 자리가 없다.
- 「⛔ 뒤집지 말 것」 7가지와 「지난 의뢰에서 겪은 함정」 4가지를 앞에 두어
  같은 실수를 다시 받지 않게 했다. 고르는 쪽이 쓸 **채점표**(§9)도 함께 넣었다.
- 어디를: 의뢰서 신설 · `0-먼저-읽어-주세요.md`(파이프라인을 ③값 단계로 갱신) ·
  `docs/디자인검토/README.md`(갈무리 목록)

### [문서] 구조 동결 v2·v3 — START 폐기와 IDLE 폐기를 문서에 반영  · 코드 변경 없음
- 무엇을: `docs/디자인검토/2026.08.24-Main-Idle-Start-구조동결-v1.md` 를 **v3** 로 올렸다.
  · **v2** ① START 상태 폐기 ② MAIN 이 배치 1 → 배치 2(세 영역) ③ MAIN 의 종이가
    Generic Paper 가 아니라 고른 서식의 **실제 1쪽**
  · **v3** ① IDLE 도 별도 화면을 두지 않음 ② **Generic Paper 폐기** ③ 첫 화면 미선택
    ④ 카드 펼침 ⑤ **배치가 하나만 남음**
- 왜: v2 에 해당하는 코드는 2026.08.24 에 이미 들어갔는데(`77c5da4`) **문서가 그 자리에
  남아 있었다.** 그 커밋이 「문서 갱신은 다음 커밋에서 한다」고 적어 두고 하지 않은 것을 갚는다.
  동결 문서와 코드가 어긋나 있으면 **동결의 뜻이 없다** — 다음 사람이 어느 쪽을 믿을지 모른다.
- **없앤 것은 왜 없앴는지를 §1 에 상자로 남겼다.** 되살리자는 말이 나올 때 무엇을 뒤집어야
  하는지 알 수 있어야 한다. 폐기된 §3-1·§3-3 의 그림도 지우지 않고 표시만 해 두었다.
- ⚠️ 파일 이름의 `Idle`·`Start` 는 그대로 뒀다 — 다른 문서 여럿이 이 이름으로 가리킨다.
- 어디를: 구조 동결 문서 · `0-먼저-읽어-주세요.md` · `2026.08.24-디자인-결정.md` §6 ·
  `tools/design-shots.py`(갈무리 3종: 시연·첫 화면·카드 펼침)

### [알려진 문제] 1366×768 에서 오른쪽 안내 기둥이 겹친다  · 이번 변경과 무관
- 무엇을: RAIL 의 「안심하고 작성하세요」와 「이용 안내」가 겹쳐 보인다.
- ⚠️ **이번 변경 전(`cfff1a4`)에도 똑같다** — 픽셀로 대조해 확인했다. 실제 키오스크는
  1920×1080 또는 1080×1920 이라 현장에서는 드러나지 않는다. 다음 차례 과제로 둔다.

---

## 2026.08.24

### [설계] Main / Idle / Start 구조 동결 v1  · 코드 변경 없음
- 무엇을: `docs/디자인검토/2026.08.24-Main-Idle-Start-구조동결-v1.md` 신설.
  **IDLE → MAIN → START → WRITING → REVIEW → PRINT → CLEAR → IDLE** 을 하나의 연속된
  경험으로 설계하고 구조·정보위계·전환 규칙을 동결했다. **구현은 아직 하지 않았다.**
- 왜: Stitch 에 **Visual Design 만** 의뢰하려면 레이아웃·위계·전환이 먼저 서 있어야 한다.
  그것이 안 서 있으면 시안을 받아도 무엇을 취할지 판단할 수 없다.
- 뼈대: 네 구역(**BAR·ASK·PAPER·RAIL**)과 세 배치. 상태가 바뀌어도 **구역이 생기거나
  사라지지 않고 자리와 내용만 바뀐다.** 배치 2(START·WRITING)는 동결된 여권의 `--cols`
  를 그대로 쓴다.
- **연속성의 축은 Paper 하나다.** 떠 있다(Idle) → 책상에 놓인다(Main) → 눈앞으로
  온다(Start) → 채워진다(Writing) → 프린터로 간다 → 치워진다 → 다시 떠오른다.
  ⛔ 카드가 날아가거나 확대되는 애니메이션은 쓰지 않는다 — **Paper 변화 하나가 담당한다.**
- Main 은 **B안 「창구 카운터」**로 확정했다. 여권의 3단을 억지로 복제하지 않고,
  위=선택(4열×2행) 아래=Paper 띠. 세로 화면과 배치가 같아진다.
- 카드는 **시민의 말이 1차, 행정명칭이 2차**다(철학 §3). 「여권 만들기 / 여권발급신청서」.
  ⚠️ 시민언어는 **Reference Text 로 동결하지 않았다** — 현장 직원 검토 대상이라
  카탈로그 한 곳에 모아 문구만 갈아 끼울 수 있게 한다. 특히 `cert` 는 가족관계·기본·
  혼인관계·입양·친양자 5종이고 **주민등록 등·초본이 아니라서** 「서류 떼기」로 쓰면 안 된다.
- Idle 시연은 **8~10초 한 주기 · 필드 3개 · 빠른 pulse 금지**로 못 박았다.
  `prefers-reduced-motion` 에서는 **시연하지 않고 정적 상태로** 대체한다 — CSS 규칙만으로는
  안 꺼지므로 JS 에서 `matchMedia` 로 분기해야 한다는 것도 적었다.
- ⛔ 여권의 `SCREENS.ready` 를 **제거하지 않는다.** Start = 경로 무관 공통 준비,
  여권 `ready` = 경로가 정해진 뒤의 추가 준비. 중복이 아니라 **책임이 다르다.**
- RAIL 의 §10 충돌을 풀었다: **하단(안심 안내)은 어떤 상태에서도 안 움직이고, 위쪽만
  상태가 바뀔 때 바뀐다.** 같은 상태 안에서 질문이 넘어갈 때는 숫자만 변한다.
- 어디를: `docs/디자인검토/` — 동결 문서 신설 · 결정 기록과 안내문에서 가리키게 함

### [수정] 기관이 대표색을 바꿔도 포인트 선만 파랑으로 남았다  · Reference UI 동결 전 정리
- 무엇을: `--hair`(서식 자리를 가리키는 모서리 선)를 SITE-CONFIG 에서 **대표색으로 계산해
  주입**한다. 종전에는 CSS 에 기본 파랑(`rgba(27,95,192,.16)`)이 박혀 있었다.
- 왜: 배포본 하나로 여러 기관이 쓴다. 초록으로 바꾼 기관에서 이 선만 파랑으로 남았다.
  7종으로 퍼지기 전에 잡는다.
- 확인: 기본(군포 파랑)은 `rgba(27,95,192,.16)` **그대로** — 화면 변화 없음.
  안양 초록(`#0f6b4f`) → `hsla(162,75%,30%,.16)`, 보라(`#6a2fa0`) → `hsla(271,55%,41%,.16)`.
  실제로 그려진 `.form-col::before` 색까지 대조했다.
- 어디를: `engine/base.html` 의 SITE-CONFIG 블록 → 9개 화면

### [정리] Radius 13가지 → 토큰 6단계  · Reference UI 동결 전 정리
- 무엇을: 여권 본 스타일의 반경 37곳을 `--r-xs·sm·md·lg·xl·pill` 로 모았다.
  실제로 값이 달라진 것은 넷뿐이다 — 6→8px(꼬리표 3곳) · 11→10px(버튼 2곳) ·
  14→12px(선택 카드) · 16→18px(창구 코드 상자).
- 왜: 새 서식을 만들 때 반경을 새로 발명하지 않게 한다(철학 §21). 지금 동결하면
  7종이 이 산만함을 그대로 물려받는다.
- ⛔ **인쇄되는 반경 둘은 토큰을 쓰지 않고 값을 박아 두었다** — `.ovhi` 4px(Print Marker),
  `.csign-hl` 3px(서명 형광). 토큰으로 바꿔 두면 나중에 토큰을 만질 때 **인쇄물이 조용히
  달라진다.** 제목 왼쪽 2px 장식 막대도 뺐다(4px 를 주면 알약이 된다).
- 확인: `verify-print.py` **19쪽 0px** · **시각 회귀** — 갈무리 29장을 전/후 픽셀 비교해
  13장 무변화, 16장이 달라졌고 **차이가 화면당 57~759px(전체의 0.04% 이하)**였다.
  확대해 보니 전부 **모서리**였고 글자·배치 이동은 없었다.

### [보완] 인쇄 후 개인정보 덮개가 다 가리지 못했다  · 갈무리 점검 중 발견
- ⚠️ **디자인 개선이 아니라 개인정보 보호 보완이다.**
- 무엇을: 인쇄 창이 닫힌 뒤 덮는 막의 불투명도를 `.985` → `1`. 종전에는 막 너머로
  **주민등록번호와 이름이 흐릿하게 읽혔다.**
- 왜: 1.5% 를 남긴 것은 「꺼진 화면이 아니라 덮인 화면」으로 보이게 하려던 것인데,
  가리는 것이 목적인 장치에서 읽히면 목적을 놓친다. 뒷사람이 앞사람 내용을 본다.
- 함께: 덮개·안내 모달이 **인쇄 CSS 에 걸려 있지 않았다.** 실수로 인쇄되면 새까만
  페이지가 나간다(`.985` 일 때도 같은 문제였고 티가 덜 났을 뿐이다).
  `@media print{#idleGuard,#printGuard,#printNotice{display:none}}` 을 공통 규칙에 넣었다.
- 어디를: 공통 블록 — `engine/base.html` · 손작성 3종 → 빌드 5종까지 8개 화면(허브는 없음)
- 확인: `reset-03-인쇄후` 갈무리를 **픽셀로 훑어** 안내 상자 밖에서 바탕색(rgb 15,23,42)을
  6단계 넘게 벗어난 픽셀 **0개**. 남은 색 차이는 상자 그림자뿐이다 · `verify-print.py` 19쪽 0px

### [반영] Validation 을 **누른 뒤에만** 알린다  · 디자인 철학 §9·§11
- 무엇을: 필수 누락 경고가 화면에 들어서는 순간 뜨던 것을 **[다음]을 눌렀을 때만** 뜨게 했다.
  종전에는 아무것도 하지 않았는데 빨간 상자로 「아직 입력하지 않은 항목이 있어요」가 떠 있었다
  (`pass-03`·`pass-09` 갈무리에 그대로 찍혀 있었다).
- 왜: **정상적인 미입력은 오류가 아니다.** 시민이 자기가 뭘 틀린 줄 안다
  (`docs/디자인검토/2026.08.24-디자인-결정.md` §3).
- 곁가지 결함도 같이 고쳤다: [다음]을 눌러 나온 친절한 문구가 **글자 한 자만 쳐도**
  막연한 문구로 덮였다(`updateWarning` 이 매번 다시 계산했다). 이제 `state.ui.warn` 에
  담아 두고, 화면을 옮기거나 다 채우면 스스로 꺼진다.
- 문구도 §9 대로 바꿨다: 「아직 입력하지 않은 항목이 있어요」 → **「「한글 성명」을 채워 주세요.」**
  (비어 있는 칸의 **이름을 불러 준다.** 여러 개면 개수도 덧붙인다)
- 어디를: `passport-helper-v1.html` — `updateWarning`·`flagWarn`·`tryAdvance`·`goNext`·[이전]·묶음 이동

### [반영] Red 를 실제 오류에만 — 개인정보·인쇄 일반안내는 Notice(갈색)로  · 디자인 철학 §11
- 무엇을: 인쇄 전 안내의 빨간 상자 2개, 인쇄 후 「출력물을 꼭 챙겨 가세요」,
  요약의 「창구에서 확인」을 **Notice(`#7a5600`/`#fff8e6`/`#ffe19a`)** 로 내렸다.
- 왜: > Red 는 실제 오류·진행불가·임박한 파괴적 상태에 제한한다.
  개인정보 안내는 중요하지만 위험이 아니고, **창구 전환은 오류가 아니라 정상 완료의
  한 종류**다(철학 §8). 붉으면 실패처럼 읽힌다.
- ⚠️ Red 를 **남긴 곳 둘**: 무동작 카운트다운 숫자(임박한 파괴적 상태) ·
  「※ ‘맞춤(Fit)’·‘축소’ 로 하면 안 됩니다」(실제 출력 실패로 이어진다 —
  Notice 상자 **안의** Warning emphasis 로 두고 상자 전체를 붉히지는 않았다).
- 색은 기존 `.note-box` 계열을 그대로 재사용했다. 최종 색감은 Visual Refinement 단계에서 본다.
- 어디를: 공통 블록(4개 원본 → 9개 화면) + `passport-helper-v1.html` 의 `.fs-counter`.
  죽은 규칙 `.sum-row .v.empty` 도 지웠다(쓰이지 않았다).

### [반영] 「잘 모르겠어요」를 20개 화면 중 18곳 → **7곳**으로  · 디자인 철학 §8
- 무엇을: `plain` 이 아닌 모든 화면에 **자동 삽입**되던 것을 없애고, 화면이 `ask`·`sub`·
  `askBox` 로 **스스로 밝힌 곳에만** 그린다.
- 왜: 어디에나 있으면 「여기서 막힐 수도 있겠구나」라는 신호가 되고, 정작 판단이 필요한
  자리에서 눈에 띄지 않는다. 기준은 **시민의 법적·행정적 판단이 실제로 필요한 화면**이다.
- 남긴 7곳: `start`(맞춤 문구) · `guardcheck`(친권 여부) · `oldpass`(기간 지난 여권 포함) ·
  `romansame`(영문 이름 변경 조건) · `roman`(askBox — 한 번 정하면 못 바꾼다) ·
  `kind`(관용·외교관·긴급 해당 여부) · `guardian2`(공동친권인지 단독친권인지)
- ⚠️ 미성년 경로에서는 **`guardian2` 하나만** 남겼다. `guardcheck` 가 「내가 법정대리인인가」를
  이미 물었고 `guardian`(법정대리인 정보)은 지금 오신 분의 **사실**을 적는 화면이다.
- 갇히지 않는지 확인: `emg`(긴급연락처)는 맞춤 출구 `sub` 를, `jumin` 은 「인쇄한 뒤 직접
  쓰겠습니다」 카드를 그대로 갖고 있다. 뺀 화면들은 본인이 아는 사실만 묻는다.
- 어디를: `passport-helper-v1.html` — `renderStep` 의 탈출구 분기 + 화면 5곳에 `ask:true`

### [반영] 「크게 보기」 — 전체 Zoom 이 아니라 Responsive Layout Mode  · 디자인 철학 §13
- 무엇을: 상단 바에 토글을 두고, `html.big` 이 **디자인 토큰만** 갈아 끼운다.
  질문면 폭·질문·설명·입력칸·카드·버튼이 커지고, **오른쪽 안내 기둥은 폭 그대로 글자만** 커진다.
- ⛔ Paper 에 고정 배율(`transform:scale`)을 주지 않았다. `width:min(210mm,100%)` 라
  **좁아진 가운데 영역에 스스로 맞춰진다**(fit-to-area). 기존 확대·스크롤은 그대로다.
- ⚠️ **인쇄물과 무관하다.** 서식 위 값은 전부 `cqw`(컨테이너 폭 기준)라 뿌리 글자 크기를
  따르지 않고, 인쇄에서 `.paper` 는 언제나 210mm 다. 크게 보기를 켠 채 인쇄해도 같은 종이가 나온다.
- 저장하지 않는다. 초기화·새로고침·무동작 복귀에서 일반 보기로 돌아간다 —
  앞사람이 켜 둔 상태를 뒷사람이 물려받지 않아야 한다.
- 컬럼 비율은 **토큰**이라 1920×1080 실물을 보고 그 값만 만지면 된다
  (`--cols`·`--cols-narrow`·`--cols-narrow-left`).
- ⚠️ 이 과정에서 한 번 깨뜨렸다: 비율을 숫자 토큰으로 빼고 `calc(var(--col-task)*1fr)` 로 썼는데
  **`fr` 은 `calc()` 안에서 쓸 수 없다.** 선언 전체가 무효가 되어 3단이 한 줄로 무너졌다.
  갈무리 용량이 절반으로 줄어 들통났고, 트랙 목록을 통째로 토큰에 담는 방식으로 고쳤다.
  경위를 토큰 블록 주석에 남겼다.
- 어디를: `engine/base.html` 토큰 블록(`html.big`) + `passport-helper-v1.html`
  (격자·타이포·터치 크기를 토큰으로 바꾸고 토글 추가)

### [수정] 상단 바 도구가 두 줄로 접혔다  · 사용자 확인
- 무엇을: 「크게 보기」를 더하면서 버튼이 일곱 개가 되어 **도구 줄이 두 단으로 접혔다.**
  도구 칸을 `1fr` → `auto`(제 너비)로 두어 **기관명·서식 이름이 먼저 줄어들게** 했다.
  서식 이름에도 말줄임을 걸고 버튼 라벨에 `nowrap` 을 주었다.
- 왜: 버튼 폭을 깎아 맞추는 식은 **기관명 길이가 바뀌면 또 접힌다** — 이 배포본은
  기관마다 이름이 다르다(`rebrand.py`). 수치가 아니라 구조로 막았다.
- 확인: 기본 기관명과 **긴 기관명**(「경상남도 창원시 의창구청 민원여권과」)·다른 대표색으로
  1920×1080 에서 각각 재어 **상단 바 74px · 도구 줄 62px — 한 줄** 유지.
- 어디를: `passport-helper-v1.html` (`.topbar`·`.doc-name`·`.topbar .btn`)

### [반영] Current Focus / Print Marker 를 갈라 부른다 · 공통 UI 규칙  · 디자인 철학 §5·§15·§17
- 무엇을: 토큰 이름을 `--hl-*` → **`--focus-*`**(화면 전용, 대표색 파생 저채도),
  `--sign-hl` → **`--marker-sign`**(⛔ 인쇄물, 손으로 적을 자리)로 갈랐다.
- 왜: 철학 §15 는 Highlighter 를 Muted Amber 로 두었는데, **그 색에는 이미 임자가 있다.**
  화면의 노랑=지금 적는 칸 / 종이의 노랑=여기 서명 으로 한 색이 두 뜻을 갖는다.
  Current Focus 는 대표색 파생을 유지한다(기관 색을 바꾸면 함께 따라오는 이점도 남는다).
- `.ovhl` 의 outline 을 1.8px → 1px 로 **약화**했다(제거는 실물을 보고 판단).
- ⛔ 인쇄되는 노란 마킹의 **색·좌표·출력 동작은 건드리지 않았다** — 이름만 토큰으로 옮겼고
  값이 같아 `verify-print.py` 19쪽 0px 그대로다.
- `prefers-reduced-motion` 을 **공통 UI 규칙**으로 토큰 블록에 넣었다(9개 화면).
  CSS 로 안 꺼지는 JS `scrollIntoView({behavior:"smooth"})` 두 곳은 `matchMedia` 로 분기했다.
- Gradient 는 **걷어내지 않았다.** §1 DON'T 는 큰 면적의 장식적 Gradient 를 금하는 것으로
  읽는다(결정 §4). 지금 쓰이는 곳은 상단 바 3px 띠뿐이라 최종 Visual 단계에서 다시 본다.

### [반영] 디자인 값을 한 곳으로 — `<!--DESIGN-TOKENS v1-->` 블록 신설  · 디자인 철학 §21
- 무엇을: 색·반경·간격·글자 크기·터치 크기·레이아웃 값을 **이름 붙인 토큰**으로 모았다.
  원본은 `engine/base.html` 의 `<!--DESIGN-TOKENS v1-->` 블록 하나이고, 9개 화면이
  **글자까지 똑같이** 갖는다(기관별 설정 SITE-CONFIG 와 같은 방식).
- 왜: `docs/디자인검토/DESIGN_PHILOSOPHY.md` §21 「새로운 서식을 만들 때 새로운 디자인을
  발명하지 않는다」. 종전에는 값이 **다섯 원본에 흩어져 하드코딩**돼 있어(base·passport·
  marriage·divorce·index) 한 곳을 고치면 나머지 넷이 조용히 갈라졌다. 밖에서 CSS 시안을
  받아도 다섯 곳에 손으로 퍼뜨려야 했다.
- ⚠️ **이번 단계에서 화면은 1px도 바뀌지 않았다.** 값을 지금 것과 똑같이 두고 이름만
  붙였다 — 아직 아무 규칙도 이 토큰을 쓰지 않는다. 하드코딩을 토큰으로 바꾸는 일은
  영역별로 나눈다(한꺼번에 바꾸면 무엇이 화면을 바꿨는지 짚을 수 없다).
- 블록 자리는 **첫 `<style>` 바로 앞**이다. 그래야 각 파일이 원래 갖고 있던 `:root` 가
  뒤에 와서 이긴다 — 토큰을 넣는 것만으로는 화면이 바뀔 수 없다는 뜻이다.
- 결정 두 가지를 블록 주석에 박아 두었다:
  - **Highlighter 는 대표색 계열을 유지한다**(§15 의 Muted Amber 미적용). 인쇄되는 서명란
    형광(`.ovhi`)이 이미 노랑이라, 화면의 노랑=지금 적는 칸 / 종이의 노랑=여기 서명 으로
    뜻이 겹친다. 기관 색을 바꾸면 하이라이트도 따라오는 이점도 그대로 남는다.
  - **터치·마우스 겸용이 전제다.** §19 가 일반 PC 를 말하지만 크기는 터치 기준을 유지한다 —
    큰 표적은 마우스에서도 문제가 없지만 그 반대는 성립하지 않는다. hover 로만 보이는
    정보는 계속 금지다.
- 알려진 결함을 주석으로 남겼다: `--hair` 에 기본 파랑이 **박혀 있어** 기관이 대표색을
  바꿔도 그 선만 파랑으로 남는다. 대표색에서 계산하도록 SITE-CONFIG 에서 함께 주입하는
  것이 옳고, 다음 단계로 미뤘다.
- 어디를: `engine/base.html`(원본) + 손작성 3종·허브 + 빌드된 5종 = 9개 화면 ·
  새 도구 `tools/check-design-tokens.py` · `AGENTS.md` §1·§2 · `README.md` 도구 표
- 확인: `verify-print.py` **19쪽 0px** · `check-design-tokens.py` 9개 화면 같음 ·
  `check-site-block.py` · `verify-site-config.py` 6개 시나리오 · `sync-kiosk.sh --check` ·
  **화면 갈무리 픽셀 비교** — 토큰 블록 넣기 전 파일과 넣은 뒤 파일을 같은 날짜에 찍어
  `realestate` 기준 **완전 동일 0px**, 여권·초기화 갈무리 12장도 0px
  (`form-*`·`hub-*` 은 08.19 에 찍은 것이라 **날짜 글자만** 달랐다 — 눈으로 대조 확인)

### [수정] 갈무리 도구가 CSS 를 반쪽만 뽑고 있었다  · 위 작업 중 발견
- 무엇을: `tools/design-shots.py` 의 `dump_css()` 가 「첫 `<style>` = 본 스타일」로 잡고
  있었는데, 토큰 블록이 그 앞에 생기면서 **토큰만 뽑아 왔다**(62KB → 31KB).
  이제 토큰 블록과 본 스타일을 나눠 **둘 다** 싣는다(72KB).
- 왜: 밖에 넘기는 `현재-스타일.css` 가 조용히 반쪽이 됐다. 받는 쪽은 그게 전부인 줄 안다.
- 어디를: `tools/design-shots.py`

### [반영] 디자인 자료 묶음을 도구로 만든다  · 외부 검토 의뢰
- 무엇을: `tools/design-bundle.py` — `docs/디자인검토/` → `_디자인검토/`(폴더 + zip).
  목적지를 통째로 비우고 다시 만들고, 나가기 전에 텍스트에서 주민번호·이메일 형태를 훑는다.
- 왜: 손으로 복사하다 **낡은 갈무리가 섞여 나갈 뻔했다** — 08.19 자 여권 갈무리가 08.21
  개편 뒤에도 묶음에 남아 있었다. 앞으로 이 왕복이 몇 번 더 있다.
- 함께: 여권 갈무리 6장을 새로 뽑고, 새 화면 둘(`pass-07`·`pass-08`)·로마자(`pass-09`)·
  **화면 초기화 3종**(`reset-01`~`reset-03`)을 추가했다. 초기화 화면은 시간이 지나야 떠서
  갈무리를 뽑을 때만 `window.__kioskCfg` 의 대기 시간을 줄이거나 늘려 띄웠다 —
  **앱 코드는 건드리지 않았다.**
- ⚠️ 그 갈무리로 알게 된 것: **인쇄 후 덮개(`.985`)가 개인정보를 다 가리지 못한다.**
  막 너머로 주민등록번호와 이름이 흐릿하게 읽힌다. 가리는 것이 목적인 장치라 목적을
  놓친 것이다. 덮개를 어떻게 보이게 할지가 마침 검토 항목이라 함께 정하기로 하고
  `docs/디자인검토/2026.08.23-…​.md` §6-4-1 에 적어 두었다.
- 어디를: `tools/design-bundle.py`(신규) · `docs/디자인검토/`(0-먼저-읽어-주세요.md 신설,
  README·현황 문서 갱신, 갈무리 25장, `시안/`) · `.gitignore`

## 2026.08.21

### [반영] 동의서에 **공동친권자**를 적어 인쇄한다 + 서명 줄에 법정대리인 이름  · 사용자 시연 확인
- ⚠️ **인쇄물이 바뀐다.** 「법정대리인 동의서」(별지 제1호의3서식) 2쪽이 대상이다.
  기준선 2장(`passport-minor-p1`·`p2`)을 `--update` 로 갱신했다. 아래가 그 이유다.
- **서명 줄에 법정대리인 1 의 성명을 넣는다.** 종전에는
  「법정대리인(부모가 공동친권자인 경우는 공동친권자 중 대표자) **(서명 날인)**」로
  이름 없이 나가서 창구에서 이름을 손으로 적고 서명까지 해야 했다. 서명은 손으로 하는 것이
  맞지만 **이름은 이미 위 칸에 적힌 값**이다 — 같은 것을 두 번 쓰게 하지 않는다.
- **법정대리인 2(공동친권자) 칸을 채운다.** 종전에는 통째로 빈칸이었다. 미성년 신청의
  대부분이 부모 두 분인데, 동의서 유의사항이 「공동친권자인 부모 모두의 동의」를 요구하므로
  창구에서 매번 같은 것을 받아 적고 있었다.
  - 새 화면 **「공동친권자」**(미성년 경로, `SCREENS.guardian2`)에서 받는다. **전부 선택이다** —
    「비워 두겠습니다」를 고르면 종전과 똑같이 빈칸으로 인쇄된다(단독친권도 있다).
  - 자리는 **긴급연락처 바로 뒤**다. 방금 적은 분을 그대로 가리켜 「그분인가요?」로 물을 수
    있어서다 — 앞(법정대리인 정보) 자리에서는 아직 긴급연락처를 모른다.
  - 긴급연락처의 관계가 **부·모**면 그분을 카드 하나로 그대로 넣는다.
    ⛔ 두 경우에는 제안하지 않는다: 관계가 부모가 아닐 때(조부모·지인은 공동친권자가 아니다),
    **법정대리인 1 과 이름이 같을 때**(같은 사람을 두 줄에 적으면 동의서가 틀린다).
  - ⛔ **주소는 채우지 않는다.** 법정대리인 1 과 같은 집에 산다고 단정할 수 없다 —
    이 서식은 부모가 따로 사는 경우에 오히려 더 자주 쓰인다. 모르는 것은 비운다.
- **작성예시(미성년)의 긴급연락처를 어머니로 바꿨다.** 종전 예시는 긴급연락처도 아버지(김철수)여서
  법정대리인 1 과 같은 사람이었다 — 「같은 사람을 두 줄에」 막는 판단이 이 예시로는 확인되지
  않았다. 이제 부·모 두 분이 들어간다(`passport-minor-p1` 기준선이 이것 때문에 바뀌었다:
  긴급연락처 성명·관계 두 칸).
- 문구 하나: 경로 불일치 경고에서 카드 문구를 그대로 따오던 것을 풀어 썼다.
  「앞에서 **「제 여권」**을 선택하셨는데」 → 「앞에서 **성인 본인의 여권을 신청**한다고 하셨는데」.
  카드 안에서는 뜻이 통하지만 경고문 안에서는 누구를 가리키는지 흐릿했다. 어떻게 고치면 되는지도
  덧붙였다(「자녀의 여권이라면 처음 화면에서 「미성년 자녀」를 골라 주세요」).
- 확인: `verify-print.py` **19쪽 0px**(갱신한 기준선 기준) · 바뀐 2장의 픽셀 차이를 눈으로 대조해
  **긴급연락처 두 칸과 동의서 법정대리인 2·서명란 말고는 움직인 곳이 없음**을 확인 ·
  `verify-site-config.py` 6개 시나리오 · `check-site-block.py` · `sync-kiosk.sh`

### [수정] 새 화면을 돌려 보고 걸린 것 여섯 가지  · 사용자 시연 확인
- **머리글 아래 선이 제목을 가로질렀다.** 질문면 위쪽 고정 위치(`top:96px`)에 그린 선이라
  제목이 길거나 두 줄이면 글자에 걸쳤다(첫 화면에서 바로 드러났다). 선을 **머리글에 붙여**
  흐름 안에 두었다 — 무슨 글자가 오든 겹칠 수 없다. 두께도 1px → 2px 로 올려
  「흐릿하게 걸쳐 있는 것」이 아니라 「구분선」으로 읽히게 했다.
- **기본으로 열려 있는 선택지에 선택 표시가 없었다.** 주민등록번호 화면은 입력칸이 처음부터
  열려 있는데 카드는 둘 다 흰색이라 「왜 칸이 열려 있지」 싶었고, 번호를 다 적고 [다음]을 눌러도
  **「위에서 하나를 골라 주세요」로 막혔다**(고른 적이 없으니). 카드에 `def:true` 를 두어
  아무것도 고르지 않은 처음부터 골라진 것으로 보이고 또 그렇게 처리되게 했다
  (`cardPicked()` 하나를 그리는 쪽과 막는 쪽이 함께 쓴다).
- ⛔ **주민등록번호로 유효기간을 덮어쓰던 로직을 없앴다.** 성인 경로인데 번호를 미성년으로
  잘못 눌렀을 뿐인데 기본값 10년이 **소리 없이 5년으로** 바뀌어 있었다. 판단은 첫 화면에서
  시민이 고른 경로가 한다. 번호가 경로와 어긋나는 것은 고쳐 줄 일이 아니라 알려 줄 일이다.
  같은 이유로 유효기간 선택지의 10년 차단도 경로 기준으로 바꿨다.
- **노란 안내를 없애고 그 자리에 빨간 경고를 옮겼다.** 「유효기간 5년이 자동 적용되고
  동의서가 함께 출력됩니다」는 경로를 이미 고른 뒤라 다시 설명할 이유가 없고, 성인 경로에서
  번호만 잘못 눌러도 떠서 「내가 미성년으로 처리됐나」 하는 혼란을 만들었다.
  경로 불일치 경고는 종전에 화면 **맨 아래** 경고줄에 있어 번호를 치는 자리에서 눈이 닿지
  않았다 — 이제 입력칸 바로 아래 빨간 판으로 낸다(`warnInline`. 아래에는 적지 않는다 —
  같은 말이 두 군데 뜨면 두 가지 문제로 읽힌다).
- **준비물에 「꼭 필요」를 표시했다.** 목록이 다 같은 굵기면 무엇부터 챙길지 고르지 못한다.
  꼭 필요한 것은 왼쪽에 대표색 띠 + 배지, 「있으면」인 것은 한 톤 내렸다(안내 기둥도 같이).
- **로마자 화면을 시안대로 두 칸으로.** 왼쪽 참고 예시 · 오른쪽 직접 입력. 종전에는 세로로
  이어져 예시에서 칸으로 눈을 옮기는 거리가 멀었고 「이 예시 적용」이 상자 안에 묻혔다.
  「잘 모르겠어요」도 점선 한 줄에서 **눈에 걸리는 도움 판**으로 올렸다 — 한 번 정하면 바꾸기
  어려운 것을 묻는 자리라 직원을 부르는 길이 숨어 있으면 안 된다.
  - 기존 여권이 있어 이 화면에 온 경우(로마자 빈칸 접수를 받지 않는 기관)에는 **예시를 내지
    않고** 그 자리에 「여권을 보고 그대로 적으라」는 경고를 놓는다. 추천 철자를 보고 적으면
    기존 여권과 달라져 접수가 막힌다.
  - ⚠️ 시안의 「사용 가능」 표시는 **그대로 쓰지 않았다.** 그 이름이 실제로 접수되는지는
    이 프로그램이 알 수 없다(기존 여권과 같은지·가족 표기와 맞는지는 창구가 판단한다).
    아는 것만 말한다 — 「✓ 적으셨습니다」와, 한글·숫자가 섞였을 때의 형식 경고.
- 확인: `verify-print.py` 19쪽 0px · `verify-site-config.py` 6개 시나리오 · `check-site-block.py` ·
  `sync-kiosk.sh` · 성인/미성년/기존여권 경로를 화면 갈무리로 확인
  (성인 경로에 미성년 번호를 넣어도 서식의 **10년 체크가 그대로**인 것을 눈으로 확인).

### [개편] 여권 도우미 화면을 **세 영역**으로 — 목업(`passport-prototype-v1.html`)을 기준으로  · 사용자 지시
- 무엇을: **화면 디자인만** 갈아 끼웠다. 업무 로직·데이터 구조·검증·좌표·인쇄는 **한 줄도 고치지 않았다.**
  - **질문면(Writing Desk) · 서식(Paper) · 안내 기둥(Companion Rail)** 세 영역.
    종전의 「배경 위에 뜬 흰 휴대전화 카드」를 버렸다 — 손에 쥐는 화면의 은유라,
    서서 쓰는 큰 키오스크에서는 화면 한복판에 작은 종이가 떠 있는 꼴이 됐다.
  - 질문 글자를 22px → **34px**. 어르신이 서서 보는 거리에서 한눈에 읽히는 크기다.
  - 터치 카드를 세로 목록 → **두 칸 격자**(132px). 선택지가 한 화면에 다 들어와 스크롤이 없다.
    홀수 번째 마지막 카드는 한 줄을 다 쓴다 — 대개 그 자리에 오는 「잘 모르겠습니다」가 눈에 띈다.
  - 글자 칸 높이 64px · 「왜 묻는지」는 상자 대신 **왼쪽 선 한 줄**로. 상자가 많으면 질문이 묻힌다.
  - 대표색 그라데이션 띠(상단) · 제목 옆 짧은 막대 · 질문 아래 가는 선 · 서식 바닥의 모서리 선 —
    **선으로만** 시각적 포인트를 준다.
- 새로 생긴 것 셋:
  - **안내 기둥** — 진행(큰 숫자·막대·묶음 목록) · **준비물** · 안심 안내 · 이용 안내.
    지나온 묶음은 눌러 되돌아갈 수 있다(앞으로 건너뛰기는 종전대로 막는다).
    묶음 목록은 **`flow()` 를 훑어 처음 나온 차례대로** 만든다 — 순서를 손으로 적어 두면
    미성년 경로의 「법정대리인 정보」(여권 정보 뒤)와 대리 경로의 「대신 오신 분」(신청인 정보 뒤)을 놓친다.
  - **「작성 전 준비」 화면**(`SCREENS.ready`) — 종전에는 무엇을 들고 와야 하는지 알려 주는 자리가
    아예 없었다. 다 적고 나서 창구에서 서류가 없어 되돌아가는 일이 그래서 생긴다.
    ⛔ **막지 않는다** — 준비물이 없어도 작성은 끝까지 할 수 있다(검증 항목이 아니다).
    자리는 「개인정보 안내」 **뒤**다. 경로를 고르기 전에는 무엇이 필요한지 알 수 없다(미성년은 서류가 더 든다).
  - **최종 확인의 「수정」** — 그 자리에서 고치러 간다. 종전에는 [이전]을 여러 번 눌러 되짚어야 했다.
    스텝퍼와 같은 규칙으로 **뒤로만** 보낸다(검증 우회 없음).
- 자리를 옮긴 것: **인쇄 버튼**을 요약 본문 끝 → [다음] 자리로. 요약이 길어 스크롤되면
  본문 끝의 버튼이 화면 밖으로 나갔다. 목업의 최종 확인도 [돌아가기][인쇄하기] 두 개다.
- **세로 화면(1080×1920)의 잘림을 고쳤다** — 2026.08.19 디자인 검토 자료에 「아는 문제」로 올렸던 것이다.
  두 단을 그대로 세워 놓아 서식이 오른쪽으로 잘렸다. 이제 질문을 위·서식을 아래로 눕히고
  안내 기둥은 머리맡 가로 띠로 접는다(그때는 준비물을 접는다 — 「작성 전 준비」 화면에서 한 번 안내한다).
- 없앤 것 하나: 패널 머리의 **두 번째 로고**와 그것 때문에 있던 제목 자동축소 스크립트.
  로고 자리를 비우느라 제목을 한 줄에 가두던 장치인데, 로고가 상단 바 하나로 줄면서 쓸모가 없어졌다.
- ⛔ **인쇄물은 손대지 않았다.** 바꾼 것은 화면 전용 CSS 와 화면 렌더 함수뿐이다.
  높이·스크롤·바탕색은 전부 `@media screen` 안에 가뒀다 — 인쇄로 새면 서식이 화면 높이에 잘린다.
  ⚠️ `.cards`·`.card`·`.pbar` 의 되돌림은 스타일시트 **맨 끝**에 두었다. 그 기본값이 파일 뒤쪽에 있어서
  미디어 쿼리라도 앞에 놓으면 나중 규칙에 덮인다(모바일에서 두 칸 격자가 안 풀리는 것으로 드러났다).
- 어디를: `passport-helper-v1.html` 하나. → `kiosk-app/app/` 동기화.
  ⚠️ 나머지 7종과 허브(`index.html`)는 **아직 옛 화면**이다. 여권만 모양이 갈렸다.
- 확인: `verify-print.py` **19쪽 전부 0px**(크롬) · `--electron` 도 통과(용지 210.23×297.35mm,
  minor-p2 246px 는 종전부터 있던 글꼴 렌더 차이·허용 2000) ·
  `verify-site-config.py` 6개 시나리오 전부 통과(새 `ready` 화면이 흐름에 들어간 것도 확인) ·
  `check-site-block.py` 9개 화면 같음 · `sync-kiosk.sh --check` 동기화됨 · `node --check` 3개 블록 통과 ·
  1920×1080 · 1080×1920 · 1366×768 · 좁은 화면과 대표색 교체(초록)를 화면 갈무리로 확인.

---

## 2026.08.19

### [개편] 기관마다 다른 값을 전부 **환경설정**으로 — 배포본 하나로 여러 기관이 쓴다  · 사용자 지시
- 무엇을: 코드에 박혀 있던 **기관 고유값을 환경설정 창(Ctrl+Shift+S)** 으로 뺐다. 창을 세 탭으로 나눴다.
  - **이 PC** — 여권 인쇄 방식·지정 프린터·화면 배치·무동작 초기화 (종전 항목)
  - **기관 설정** — 기관명·부서·창구명·전화·소재지·안내문구 · **로고 PNG** · **대표색**
  - **업무 방식** — **취급 서식 8종 체크** · **여권 접수 기준 5가지** · 입력 장치(키보드 유무)
- 왜: 타 지자체 확산이 예정돼 있는데, 환경이 기관마다 다르다. 종전에는 이름 하나 바꾸려면
  소스를 고쳐 다시 빌드해야 했다(`rebrand.py`). **배포자(군포시)가 아니라 일선 담당자가
  그 자리에서 맞출 수 있어야 한다**는 것이 이번 지시의 요지다.
- 어떻게: 이미 있던 통로(`kiosk.json` → `pageConfig()` → `window.__kioskCfg`)에 키를 늘리고,
  8종+허브에 **똑같은 `SITE-CONFIG` 블록**을 넣어 그 값을 화면에 반영한다.
  - **기본값은 서식이 갖고 설정은 덮어쓰기만 한다.** 기본값을 설정 쪽에도 적으면 두 벌이 되어 갈라진다.
  - 설정이 없는 **웹(GitHub Pages) 배포본**에서는 블록이 아무것도 하지 않고 종전 화면 그대로다.
- **대표색은 하나만 받는다** — 밝은 색·어두운 색·그라데이션을 그 한 색에서 계산한다.
  색을 여러 개 받으면 조합이 어긋나 촌스러워진다. 명도는 30~55 로 좁힌다(너무 밝으면 버튼 위
  흰 글씨가, 너무 어두우면 옅은 배경이 읽히지 않는다). 조정이 걸리면 창이 그 사실과 결과를 보여 준다.
  상단 바 아래선·주 버튼에 그 그라데이션을 쓴다 — **둘 다 인쇄에서 숨겨지는 자리**다.
- **여권 접수 기준을 설정으로 돌렸다.** 2026.08.18 개편 때 「담당자 확인 대기」로 남긴 항목들이다
  (`docs/여권-개편-사전분석.md` §6). 기관마다 답이 다른 것을 하나로 정할 수 없다는 것이 확산 과정에서 분명해졌다.
  - `allowProxy` 대리 신청을 끝까지 받는가 — 켜면 **대리 경로가 생기고 위임장 2쪽**이 붙는다
    (2026.08.18 에 「코드는 보존」해 둔 `renderProxy()` 가 그대로 되살아났다. 인쇄 코드는 한 줄도 고치지 않았다)
  - `allowRomanBlank` 기존 여권이 있을 때 로마자 칸을 비워 접수하는가 — 끄면 그때도 입력받는다
  - `minorPeriodFixed` 미성년 유효기간 5년 고정 — 끄면 물어본다(10년 카드는 법상 나오지 않는다)
  - `requireEmergency` 긴급연락처 필수 — 끄면 비우고 넘어갈 수 있다(창구 전환 대신 「비우고 넘어가기」)
  - `showCounterCode` 창구 사유 코드 표시 — 코드 체계를 쓰지 않는 기관은 사유 문장만 본다
- **취급하지 않는 서식은 첫 화면에서 뺀다.** 여권은 시·군·구청만 취급하는데도 항상 떠 있었다.
  섹션이 통째로 비면 제목도 내지 않는다 — 빈 제목만 남으면 고장으로 보인다.
- **입력 장치**를 「터치 화면만 있음」으로 두면 (1) 글자 칸에서 도움을 청하라는 안내가 상단에 뜨고
  (2) **환경설정 단축키가 PIN 뒤로 들어간다.** 터치 자리에 나중에 키보드를 꽂았을 때 시민이 설정을
  열어 버리는 것을 막는다(`docs` 백로그에 있던 문제). PIN 이 없으면 종전대로 열린다 —
  **잠글 수단만 만들고 열 수단을 없애지는 않는다.**
- ⛔ **인쇄물은 손대지 않았다.** 바꾼 것은 `.topbar`(인쇄 CSS 가 숨김)와 화면 전용 색·로고뿐이다.
  이 경계를 지키는 한 `verify-print.py` 가 계속 안전망으로 작동한다.
- 어디를: `kiosk-app/main.js`(설정 키·로고 파일·PIN 게이트) · `kiosk-app/admin/settings.html`(탭·색·로고·정책) ·
  `settings-preload.js` · `engine/base.html`·`engine/engine.js`(공통 블록·기관 표기) → **엔진 5종 재빌드** ·
  손작성 3종(passport·marriage·divorce)과 `index.html` 에 같은 블록 손수 반영 · `kiosk-app/app/` 동기화.
  `passport-helper-v1.html` 에 정책 분기와 **대리 경로**를 추가.
- 새 도구 둘:
  - **`tools/check-site-block.py`** — 9개 화면의 `SITE-CONFIG` 블록이 같은지 본다. 손작성 3종을
    빠뜨리면 그 서식만 설정을 못 읽는 채로 배포되는데, `sync-kiosk.sh --check` 는 루트↔키오스크만
    보므로 그 어긋남을 못 잡는다. `--fix` 로 맞춘다.
  - **`tools/verify-site-config.py`** — 설정 6가지 조합을 헤드리스 크롬으로 태워, 값이 화면에
    실제로 걸리는지 본다. **설정은 화면 흐름을 통째로 바꾸는데 인쇄물은 그대로**라서
    `verify-print.py` 가 아무 말도 하지 않는다 — 그 사각지대를 메운다.
    `--preview` 로 색·로고를 브라우저에서 눈으로 볼 수도 있다.
- 확인: **`verify-print.py` 19쪽 전부 0px** · **`--electron` 19쪽 전부 0px** ·
  `sync-kiosk.sh --check` 통과 · `check-site-block.py` 9개 화면 동일 · JS 문법 전부 통과 ·
  헤드리스 크롬으로 6가지 설정 조합을 태워 확인(기관명·대표색·서식 숨김·대리 경로 완주와 위임장 판정·
  로마자 재입력·긴급연락처 건너뛰기·코드 숨김·미성년 기간 고정/해제·설정 없을 때 종전 동작).
- 고친 것: 미성년 흐름에 **유효기간 화면 자리가 없어**, 고정을 끈 기관에서 그 화면이 아예 안 나오던 문제
  (화면 조건만 풀고 `FLOW.minor` 에 자리를 안 넣었다). 헤드리스 시험에서 잡혔다.
- 판 올림: **1.0.1 → 1.1.0**. 시험용 포터블 실행본을 만들었다(사용자 요청). 1.0.1 은 시범운영에
  쓰인 것이라 **덮어쓰지 않으려고** 판을 올렸다 — `dist/` 에 두 판이 함께 남아 있다.
  ⚠️ 포터블은 **그 PC 의 실제 설정 파일**(`%ProgramData%\군포민원서식도우미\kiosk.json`)을 읽고 쓴다.
  현장 설정을 건드리지 않고 시험하려면 `GUNPO_KIOSK_CONFIG` 로 다른 파일을 물려라.
- 남은 것: 웹 배포본은 설정 통로가 없어 종전대로 `rebrand.py` 가 필요하다 · 터치 전용 기관을 위한
  화면 키보드는 여전히 만들지 않았다(2026.08.13 결정 유지 — 안내로 대신한다).

### [문서] 디자인 검토를 밖에 맡기기 위한 자료 묶음 — `docs/디자인검토/`  · 사용자 요청
- 무엇을: 화면 갈무리 19장 + 지금 쓰는 화면 CSS + **의뢰서**(`README.md`)를 한 폴더에 모았다.
  코드를 실행하지 않아도 판단할 수 있게 만들었다.
- 왜: 여권만 새 흐름(큰 터치 카드)이고 **나머지 7종은 옛 폼 흐름**이라 같은 키오스크에서
  조작법이 갈린다. 7종을 정비하기 전에 기준을 잡아야 한다.
- 의뢰서에 담은 것: 사용 상황(민원실·어르신·서서 3~10분·터치+물리 키보드) · **⛔ 인쇄물 불가침**
  (절대좌표·`@media print`·서식 이미지) · 대표색 하나에서 계산하는 색 체계(기관마다 달라지므로
  특정 색 전제 불가) · 이미 아는 문제 7가지 · 물어볼 것 8가지 · 답 주는 방법.
- 새 도구: **`tools/design-shots.py`** — 상태별 화면(경로 선택·요약·창구 전환·작성예시 채운 상태)을
  헤드리스 크롬으로 한 번에 찍고, 원본 HTML 에서 화면 CSS 를 뽑아 `현재-스타일.css` 로 낸다.
  `--css-only`·`--only`·`--size` 로 부분 재생성.
- 자료를 만들다 문제를 하나 더 찾았다: **세로 화면(1080×1920)에서 서식이 오른쪽으로 잘린다.**
  두 단 배치를 그대로 세워 놓아서다. 의뢰서 「아는 문제」에 적어 두었다.
- 확인: 갈무리는 **전부 가상 인물**(작성예시)이다 — 개인정보가 들어가지 않는다.
  인쇄물과 무관한 작업이라 `verify-print.py` 는 돌리지 않았다(코드를 고치지 않았다).

---

## 2026.08.18 (2)

### [개편] 여권 도우미를 터치 키오스크 흐름으로 — 10단계 고정 → 경로별 분기  · 담당자 의견 다수
- 무엇을: 첫 화면에서 **큰 아이콘 카드 2개**로 경로를 갈라, 그 경로에 필요한 화면만 지난다.
  키오스크가 확실히 처리하는 둘만 끝까지 안내하고, 나머지는 **개인정보를 더 받지 않고** 창구로 넘긴다.
  - **경로 A** 성인 본인 → 신청서 1쪽 (15화면 중 조건에 따라 12~14개)
  - **경로 B** 미성년 자녀 + 법정대리인 → 신청서 + 법정대리인 동의서 2쪽
  - **창구 전환** 8가지 사유 코드(P-01~P-99). 오류가 아니라 **정상 완료 유형**으로 설계했다.
- 왜: 종전에는 누구나 10단계를 전부 지나고, 분기가 9단계에 몰려 있었으며, 미성년이면 이미 고른
  유효기간을 **뒤에서 5년으로 덮어썼다.** 특수 사례(관용·외교관·대리)를 끝까지 끌고 가다
  창구에서 되돌려보내는 것이 시민에게도 직원에게도 가장 나빴다.
- 새로 생긴 것:
  - **빈칸의 뜻을 6가지로 구분**(입력완료·직접작성·기존자료활용·해당없음·창구확인·미입력).
    「값이 없다」 하나로는 일부러 비운 칸과 빠뜨린 칸을 구별할 수 없었다.
  - **주민등록번호 선택권** — 「인쇄 후 직접 작성」을 고르면 그 칸만 비워 인쇄한다. 오류가 아니다.
  - **경로↔주민번호 교차 검증** — 조용히 보정하지 않고 알려서 고치게 한다. 2020.10 체크섬
    폐지로 오탈자를 잡을 수단이 없어졌는데, **앞단의 경로 선택이 그 역할을 대신한다.**
  - **긴급연락처 중복 차단** + 「적을 번호가 없습니다」 창구 출구(P-06). 막기만 하면 갇힌다.
  - **창구 안내표 인쇄** — 코드와 사유만. 이름·주민번호·전화·주소·로마자 **한 글자도 싣지 않는다.**
- ⛔ **인쇄층은 손대지 않았다.** 좌표맵·`renderForm()`·`renderProxy()` 그대로다. 유일한 변경은
  `needsConsent()` 의 판정 근거를 **주민번호 → 경로 선택**으로 옮긴 것 — 주민번호를 「직접 작성」으로
  비우면 종전 로직으로는 **동의서가 통째로 안 붙었다.**
- 💡 서명란은 기존 한 줄(`applyBy==="대리인" ? agentName : nameKor`)이 그대로 새 요구를 만족한다.
  경로 B 에서 `applyBy="대리인"` + `agentName=법정대리인 성명` 을 채우면 서명란·동의서·위임장 제외가
  전부 의도대로 나온다. 위임장은 `needsProxy` 가 항상 false 가 되어 **저절로 호출되지 않는다**(코드는 보존).
- 어디를: `passport-helper-v1.html`(화면 정의·라우터·카드 UI·창구 전환·안내표·터치 CSS) ·
  `kiosk-app/app/` 동기화. 착수 전 원본은 `docs/archive/passport-helper-v1_2026.08.18_개편전.html`
  과 태그 `여권개편-전-2026.08.18` 에 보존.
- 확인: **`verify-print.py` 19쪽 전부 0px**(화면을 통째로 갈아엎었는데 인쇄물은 그대로 — 질문 흐름과
  데이터 모델을 분리한 설계가 실제로 작동한다) · `sync-kiosk.sh --check` 통과 · JS 문법 통과 ·
  헤드리스 크롬으로 경로 A·B 완주, 창구 전환, 경로↔주민번호 충돌 차단, 긴급연락처 중복 차단,
  안내표 개인정보 없음을 클릭 경로로 확인.
- 남은 것: 겹쳐 찍기 상태에서 **안내표 인쇄 시 서식 용지를 먹는 문제**(화면·사진을 앞에 두고
  인쇄는 보조로 안내하는 것으로 완화). 담당자 확인 대기 6건은 `docs/여권-개편-사전분석.md` 참조.

### [문서] 여권 개편 착수 전 분석 — 지시서 검토와 §28 답변
- 무엇을: `docs/여권-개편-사전분석.md` 신규. 지시서의 P0 3건·P1 6건·P2 2건을 심각도순으로 짚고,
  §28 이 요구한 10항목(필드 목록·위임장 로직·동의서 연결·로마자·미성년 판정·서명란·연락처 검증·
  개인정보 초기화 5지점·충돌 가능 지점·최소변경 대 리팩터링)을 답했다.
- 확인: 문서만 추가 — 코드 변경 없음.

---

## 2026.08.18

### [수정] 인쇄물 날짜가 나흘째 틀리고 있던 것 — PC 시계를 눈에 보이게 한다  · 시범운영 현장 신고
- 무엇을: 민원용 PC 에서 신청일이 설치일(2026.08.14)로 고정돼 인쇄됐다. **원인은 앱이 아니라
  그 PC 의 윈도 시계**였다 — 하드보안관이 재부팅마다 날짜까지 설치일로 되돌리고 있었다.
  PC 쪽은 담당자가 조치했고, 리포에는 **다시는 나흘씩 모르고 지나가지 않도록** 두 곳을 넣었다.
  ① 환경설정 창(`Ctrl+Shift+S`) 맨 위에 **이 PC 의 날짜**를 늘 띄운다.
  ② `--selfcheck` 에 **`[시각]`** 항목 — 현재 날짜·시각과 동기화 원본.
- 왜: 서식의 신청일·동의일은 `APP_TODAY=new Date()` 로 **OS 시계를 그대로 찍는다.** 그런데
  화면 어디에도 날짜가 크게 보이지 않아, 틀린 날짜가 찍힌 신청서·동의서·위임장이 나흘 동안
  창구로 넘어갔다. 프로그램은 그 PC 의 날짜가 맞는지 **스스로 알 수 없다** — 사람이 대조해야
  하고, 그러려면 보여야 한다.
- 어디를: `kiosk-app/main.js`(`nowStamp()`·`timeSource()` 신설, `settings:load` 에 `today`,
  점검 결과에 `[시각]` 절) · `kiosk-app/admin/settings.html`(머리말 + 안내 상자) ·
  `운영문서/붙임3_키오스크_설치운영_안내.md`(일일 점검표에 날짜 항목, 「PC 날짜가 틀릴 때」
  절 신설 — 복원 솔루션 해제 순서·CMOS 배터리·폐쇄망 드리프트 구분).
- ⚠️ **한글 윈도우의 `w32tm` 출력은 cp949 다.** UTF-8 로 읽으면 깨진 글자가 검수지에 박힌다 →
  `latin1` 로 받아 **ASCII 일 때만** 싣고, 아니면 「확인 불가」로 두고 사람이 날짜를 대조하게 했다.
- 확인: `verify-print.py` 19쪽 **전부 0px** · `sync-kiosk.sh --check` 통과 ·
  `node --check main.js` 통과. 관리자 창·점검 모드만 건드려 인쇄 경로는 지나지 않는다.

### [문서] 여권 입력 시나리오 현황 정리 — 개편 논의용  · 담당자 의견 다수
- 무엇을: `docs/여권-입력시나리오-현황.md` 신규. 현재 10단계가 무엇을 어떤 순서로 묻는지,
  분기(미성년·대리·특수여권)가 어디서 갈리는지, 인쇄물이 어떻게 붙는지를 **빠짐없이** 적었다.
  구조적 문제 7가지, 재설계 시 못 넘는 선 7가지, 담당자 확인 질문 12개, 전체 필드 표를 함께 뒀다.
- 왜: 담당자들에게서 수정 의견이 많이 올라와 입력 흐름을 새로 설계하기로 했다. 그 논의를
  **다른 AI·담당자와 함께** 하려면 현재를 한 장으로 볼 자료가 필요하다. 리포 사정을 모르는
  사람이 읽는다는 전제로 썼다.
- ⛔ 문서에 못박은 원칙: **질문 흐름과 데이터 모델을 분리한다.** 인쇄층은 `state.data` + 좌표맵만
  읽으므로, 필드 모양만 유지하면 화면을 크게 뒤집어도 `verify-print.py` 19쪽 0px 이 유지된다.
- 향후 **터치스크린 모니터** 도입을 전제로 초기 화면을 아이콘화하는 방향도 논점으로 적었다.
- 확인: 문서만 추가 — 코드 변경 없음.

---

## 2026.08.15

### [정리] 작업 지침을 `AGENTS.md` 한 벌로 — Codex 도 같은 규칙을 받는다  · 담당자 요청
- 무엇을: `CLAUDE.md` 의 내용을 루트 **`AGENTS.md`** 로 옮겼다. `CLAUDE.md` 는 그것을
  불러오는(`@AGENTS.md`) 한 줄짜리로 남겼다. Codex 는 `AGENTS.md` 를 스스로 읽는다.
- 왜: 도구마다 지침 파일을 따로 두면 **두 벌이 갈라진다.** 여기서 지침이 갈라진다는 것은
  「엔진 5종 루트 HTML 직접 수정 금지」·「verify-print 통과」 같은 **안전망을 한쪽만 모른다**는 뜻이다.
- 무엇을 더했나: **0장 개발 환경** — Git Bash·Python(`Pillow`·`PyMuPDF`)·Node·Chrome 경로·
  Electron(`kiosk-app/npm install`). 앱 자체는 의존성이 없고 이건 빌드·검증용이라는 점을 밝혔다.
  인쇄 검증이 **프로세스를 띄우고 임시 PDF를 쓴다**는 것도 적었다(샌드박스형 에이전트가 막힌다).
- 어디를: `AGENTS.md`(신규) · `CLAUDE.md`(포인터로 축소) · `README.md`(디렉터리 표) ·
  `docs/타지자체-확산.md`(AI 에게 맡길 때) · `tools/rebrand.py`(교체 대상 목록).
- 확인: `rebrand.py --dry-run` 이 `AGENTS.md` 를 1곳 잡는다(기관명) — 확산 시 같이 바뀐다.

### [정리] 「↔ 좌우 바꾸기」 버튼을 없앴다 — 배치는 담당자만 정한다  · 담당자 요청
- 무엇을: 여권 서식 상단의 `btnFlip` 버튼과 그 클릭 처리를 삭제했다. 좌우 배치를 정하는 곳은
  이제 **환경설정 창(Ctrl+Shift+S)** 하나뿐이다.
- 왜: 시민이 눌러 뒤집어 놓으면 **뒷사람이 설명과 다른 화면을 보게 된다**. 안내를 듣고 온
  창구 직원도 헷갈린다. 2026.08.13 에 환경설정 창이 생기면서 버튼은 중복이 됐다.
- 어디를: `passport-helper-v1.html`(버튼 8종 중 **여권에만 있었다**) ·
  `kiosk-app/admin/settings.html`·`kiosk-app/main.js` 의 설명 문구 ·
  `운영문서/붙임2_사용매뉴얼.md`·`붙임2-1_사용매뉴얼(상세).md`.
- ⚠️ **CSS `body.form-left` 규칙은 남겨 뒀다** — 환경설정이 로드 시 이 클래스를 얹는다.
  버튼이 없다고 같이 지우면 배치 설정이 조용히 먹통이 된다.
- 확인: `verify-print.py` 19쪽 **전부 0px**(버튼은 `.topbar` 안이고 인쇄에서 숨겨진다) ·
  `sync-kiosk.sh --check` 통과 · `node --check main.js` 통과.
- 운영문서 **PDF 는 재생성하지 않았다**(시범운영 중 파생물 생성 중지). `.md` 만 갱신했다.

---

## 2026.08.13

### [수정] 「미리보기 → 인쇄」가 백지로 나오던 것  · 시범운영 현장 신고
- 무엇을: 여권 서식의 인쇄 CSS 에서 **미리보기 모달을 인쇄 대상에서 제외**했다
  (`.modal-back{display:none !important}`). 나머지 7종은 원래 이 방식이었다.
- 왜: 여권만 「모달이 열려 있으면 모달 안의 사본을 찍고 좌측 원본은 숨긴다」로 돼 있었는데,
  모달은 **화면용 flex 레이아웃**이라 인쇄에서 `.modal-scroll` 이 `display:flex`·`flex-basis:0%`
  인 채로 남는다. 그 안의 `.paper{width:100%}` 는 기댈 폭이 없어 **0 으로 붕괴**한다.
  숨긴 원본 + 무너진 사본 = **백지**.
- 실측(인쇄 시점): `modal-scroll h=0` · `paper w=0 h=0` · 값(`.ov`) **66개가 전부 0px 안에 갇힘**.
- 왜 성인만 터졌나: **미성년(2쪽) 건은 우연히 멀쩡했다.** 법정대리인 동의서의 글자가
  내재 폭을 만들어 줘서 붕괴를 면했다. 성인 단독은 배경 이미지뿐이라 기댈 폭이 없다.
- 확인(잉크 비율): 미리보기 인쇄 **0.00% → 9.69%** 로 직접 인쇄와 일치.
  겹쳐 찍기를 켠 상태도 0.00% → 0.71% 로 일치. `verify-print` 19쪽 0px.
- ⚠️ 모달의 값은 좌측 서식과 **같은 `renderForm()`** 이 채운다 — 좌측을 찍어도 내용은 동일하다.

### [기능] 환경설정 창 (Ctrl+Shift+S) — 설정을 손으로 고치지 않게 한다  · 담당자 요청
- 무엇을: 키오스크 안에서 설정을 바꾼다. 여권 인쇄 방식 · 지정 프린터 · 화면 좌우 배치 ·
  무동작 초기화 시간(1·3·5·10분) · 인쇄 후 대기 시간(즉시·1·3·5·10초).
- 왜: `kiosk.json` 을 메모장으로 고치는 방식은 **BOM 하나에 설정 전체가 조용히 무시**됐다
  (`JSON.parse` 실패 → 프린터 지정까지 날아가 인쇄 정지). 이제 프로그램이 저장한다.
  모르는 키(`exitPinHash` 등)는 읽어서 병합해 **보존**한다.
- 어디를: `kiosk-app/admin/settings.html`·`settings-preload.js`(신규) ·
  `kiosk-app/main.js`(창·IPC·단축키) · `preload.js`(`__kioskCfg`) · `print-options.js`(방식 매핑).
  `package.json` 의 `build.files` 에 preload 를 추가했다 — 빠뜨리면 **포장본에서만** 창이 빈다.
- ⚠️ **PIN 을 묻지 않는다**(담당자 결정). 시민이 키보드를 만질 수 있는 자리라면 OS 쪽에서
  키보드를 막거나 PIN 을 설정하는 편이 안전하다. `--selfcheck` 가 PIN 미설정을 계속 보고한다.
- ⚠️ 저장하면 **처음 화면으로 되돌린다.** 시간·배치는 서식이 로드될 때 들어가므로
  보고 있던 화면에는 안 걸린다.
- 시간 설정은 서식 8종의 지역 변수였다 → `window.__kioskCfg` 를 먼저 보고 없으면 기존
  기본값을 쓰게 고쳤다(엔진 재빌드 5종 + 손작성 3종). preload 가 **`sendSync`** 로 심는다 —
  비동기로 넘기면 서식이 이미 기본값으로 타이머를 걸어 버린 뒤라 늦다.
  웹(GitHub Pages) 배포본은 preload 가 없어 **종전대로 3분/5초**다.

### [기능] 적색 드롭아웃 — 미리 인쇄된 용지 없이 흑백으로 판독되게  · 컬러 프린터 대안
- 무엇을: 배경 서식에 **빨강 채널만 회색조로 쓰는 색행렬**을 걸어, 흑백 프린터가 빨간 칸선을
  연하게 찍게 한다. 스캐너의 적색 드롭아웃을 **인쇄 단계에서 미리** 흉내내는 것이다.
  `Out = 0.86R + 0.07G + 0.07B` — 빨강(255,0,0)→255(사라짐) · 검정 항목명(0,0,0)→0(남음).
- 왜 '전체를 밝게' 로는 안 되나: 밝기는 색을 구분하지 못한다. 전체를 25% 밝게 하면 빨간 선은
  사라지지만(0%) **「필수 기재란」 같은 검은 항목명도 같이 사라진다(0%)**. 실측값이다.
- 실측(Electron 인쇄 경로 · 이진화 임계 128): 빨간 선이 남는 비율 **29.6% → 1.2%** ·
  검은 항목명 보존 **100% → 83%** · 빨강 자리 밝기 169.7 → **230.6**(사람 눈에는 옅은 안내선).
- 이점: 미리 인쇄된 용지가 필요 없다 → **2쪽 문제와 정합 공차 문제가 함께 사라진다.**
- ⚠️ **기본값은 꺼짐이다.** 흑백 레이저는 밝기 230 을 균일한 회색이 아니라 **띄엄띄엄한 검은
  점**으로 찍는다. 스캐너가 그 점을 주울 수 있고, 이건 시뮬레이션으로 안 풀린다.
  **실물을 뽑아 접수처 스캔으로 확인한 뒤** 켜라. 점이 문제면 `DROPOUT_KEEP` 을 0 으로 낮춘다.
- ⚠️ `color-interpolation-filters="sRGB"` 를 빼지 마라 — 기본값 linearRGB 로 계산되면 값이 달라진다.

### [수정] 하드보안관 대응 — 설정 파일을 보존 영역에서 먼저 찾는다  · 담당자 지적
- 무엇을: `kiosk.json` 을 ① 환경변수 `GUNPO_KIOSK_CONFIG` ② `D:`~`H:` 중
  **`<드라이브>:\군포민원서식도우미\` 폴더가 있는** 첫 드라이브 ③ `%ProgramData%` 순으로 찾는다.
- 왜: 현장 PC 는 하드보안관이 걸려 있어 **C: 가 부팅마다 원복**된다. `%ProgramData%` 에
  저장하면 다음 부팅에 설정이 사라지고, **지정 프린터가 비어 인쇄가 전면 차단**된다
  (지정이 없으면 안 찍는 것이 설계다). 설정 편의가 아니라 운영 중단 문제다.
- 관리자가 할 일은 **폴더 하나 만드는 것**뿐이다. 파일을 미리 만들 필요 없다.
  폴더를 만들어 두는 것 자체가 "여기에 저장하라"는 지시다.
- 확인: 환경변수 지정 ✓ · `E:\군포민원서식도우미` 생성 시 그쪽으로 전환 ✓(한글 폴더명 정상) ·
  폴더 삭제 후 `%ProgramData%` 로 복귀 ✓. `--selfcheck` 에 「재부팅 후 유지」 줄을 넣었다.
- ⚠️ 드라이브 훑기는 **폴더 존재 확인만** 한다(`existsSync`) — 없는 드라이브에서 대화상자가 뜨지 않는다.

### [검증] 겹쳐 찍기 시험 출력물의 정렬 — 합격  · 현장 시험 인쇄본
- 스캔본(300dpi)에서 미리 인쇄된 주황 칸과 우리가 찍은 검은 값을 색으로 분리해 측정.
- 가로 어긋남: 한글성명 −0.75 · 주민번호 −0.57 · 본인연락처 −0.72 mm.
  **세 행이 같은 방향·같은 양**이므로 서식 좌표 문제가 아니라 **프린터 급지 기준의 오프셋**이다.
- 칸 피치 7.80mm vs 글자 피치 7.80mm — 누적 드리프트 11칸에 +0.02mm.
  `CHANGELOG` 2026.08.12 의 「남은 일(격자 간격 누적 오차)」은 이 행들에서는 이미 잡혀 있다.
- 세로는 확대 육안으로 칸 중앙. 자동 측정은 스캔본의 **노란 형광펜**이 칸선 검출을 방해해
  불안정했다(−0.4 ~ +2.5mm 를 오감) — 수치를 쓰지 않았다.
- 판정: 가로 여유가 ±1.9mm(칸 7.8 − 글자 4)이므로 0.7mm 는 넉넉히 안쪽이다.
  ⛔ **좌표를 밀지 마라.** 급지 오프셋은 기기마다 달라, 한 대에 맞추면 다른 기기에서 반대로 틀어진다
  (`docs/GOTCHAS.md ㉝`). 맞추려면 프린터 드라이버의 인쇄 위치 보정으로 그 기기에서만 한다.

### [기타] 키오스크 버전 1.0.0 → **1.0.1**
- 왜: 같은 날 두 번 구우면 파일명·`--selfcheck` 의 「앱 버전」이 같아 현장 PC 에 어느 것이
  깔렸는지 구분되지 않는다.

---

## 2026.08.12

### [수정] 인쇄물이 서식과 어긋나던 것 — Electron 이 A4 를 Letter 로 조판하고 있었다  · 시범운영 현장
- 무엇을: 인쇄 조판 옵션에 **용지(`pageSize:'A4'`)를 명시**했다. 옵션은 `kiosk-app/print-options.js`
  한 곳으로 모아, **실제 인쇄와 회귀 검증이 같은 값**을 쓰게 했다.
- 왜: 키오스크 인쇄물의 값이 **칸을 벗어났다.** 좌상단은 맞는데 우측이 넘치고 아래가 남았다.
  `pageSize` 를 주지 않으면 Electron 은 서식 CSS 의 `@page{size:A4 portrait}` 를 무시하고
  **Letter(215.9×279.4mm)** 로 조판한다. A4 조판을 Letter 에 맞추면 **가로 +2.9%·세로 −5.9%** 로
  **두 축이 반대로** 어긋난다. 좌표·글자 크기가 `cqw` 단위라 균일 배율에는 끄떡없지만,
  비균일이면 그대로 칸 이탈이 된다.
- 실측(Electron 33 / 크로미움 130 · 여권 서식):

  | 조판 옵션 | 나온 용지 |
  |---|---|
  | `pageSize` 미지정 (종전) | 215.90 × 279.40mm (Letter) |
  | `pageSize:'A4'` (반영) | 210.23 × 297.35mm |
  | `preferCSSPageSize:true` | 209.89 × 297.01mm |
  | 데스크톱 크롬 `--print-to-pdf`(기준선) | 209.89 × 297.01mm |

- 왜 프린터 문제로 보였나: **드라이버 여백을 0.0 으로 맞춰도 안 고쳐지고**, 개발PC와 키오스크가
  **같은 방향으로** 어긋났다(크기만 1mm/2mm 차이 — 그건 프린터별 정합 공차다). 상류에서 이미
  왜곡돼 나가므로 프린터 설정으로는 손댈 수 없었다. `GOTCHAS ⑯` 의 "좌표를 재정합하고 싶어지는" 함정과
  같은 자리다 — **좌표는 한 줄도 건드리지 않았다.**
- 어디를: `kiosk-app/print-options.js`(신설) · `kiosk-app/main.js` · `kiosk-app/package.json`
  (`build.files` 에 새 모듈 추가 — 빠뜨리면 포장된 실행본에서만 죽는다) · `docs/GOTCHAS.md` ㉝ 신설.
- 확인: `verify-print.py` 19쪽 **전부 0px**(크롬 기준선 불변 — 조판 옵션은 인쇄 경로에만 영향) ·
  `sync-kiosk.sh --check` ✓. 아래 검증 도구로 용지 A4 확인.
- 파생물: **사용자 요청으로 설치본·포터블을 재빌드**했다(시범운영 중 재생성 중지의 예외).
  시범운영이 이 사고로 막혀 있어 선반영이 불가피했다. 포장 후 확인 —
  `app.asar` 안에 `print-options.js` 포함 확인(`build.files` 누락이면 여기서만 죽는다) ·
  포장된 실행본 `--selfcheck` 전 항목 [양호].

### [도구] 인쇄물 검증에 Electron 경로 추가 — 안전망이 못 보던 구간  · 위 사고의 재발 방지
- 무엇을: `python tools/verify-print.py --electron` 을 추가했다. **키오스크와 같은 Electron 조판**으로
  19쪽을 뽑아 **같은 기준선**과 픽셀 비교하고, 실제 인쇄 옵션의 **용지가 A4 인지 따로 검사**한다.
  렌더러는 `tools/print-electron.js`(신설)이고 `print-options.js` 를 그대로 읽는다.
- 왜: 종전 안전망은 **데스크톱 크롬으로만** 검증해서, 실제 인쇄가 지나가는 Electron 경로를 아예 보지
  못했다. 위 사고가 정확히 그 틈으로 빠져나갔다 — 크롬 기준선은 19쪽 전부 0px 인데 인쇄물만 어긋나 있었다.
  `2026.08.07` 항목에 "Electron `printToPDF` 기반 회귀 검증을 먼저 만들자"고 적고 미뤄 둔 그 도구다.
- 판정 근거: 19쪽 중 **18쪽이 0px**, 1쪽이 246px(주민번호 숫자 몇 글자의 안티에일리어싱).
  두 크로미움 빌드의 글자 렌더 차이라 `ELECTRON_PX_TOL`(2000px)을 두되 **숫자는 그대로 표시**한다.
  조판이 어긋나면 자릿수가 다르다 — 기준선을 **1px(0.17mm)만 밀어도 66,000~300,000px** 가 달라지는 것을
  실측해 허용치를 그 사이에 놓았다.
- 자체 검증: `pageSize` 를 일부러 빼고 돌려 **`✗ 용지 215.90 × 279.40mm`** 로 잡히는 것을 확인했다.
- `--electron --update` 는 막았다. 기준선은 하나이고, **두 경로가 같은 결과를 내야 한다**는 것이 요지다.
- 곁다리: 이 도구는 **Electron 44 상향**의 전제 조건이기도 하다(`2026.08.07` 남은 일).
  크로미움이 바뀌어 인쇄 조판이 달라지면 이제 여기서 잡힌다.

### [수정] 여권 「관계」 칸 글자가 오른쪽으로 삐져나오던 것  · 겹쳐 찍기 시험
- 무엇을: `emgRel`(긴급연락처 관계) 격자를 `cellW:30.5·gap:0` → **`cellW:26·gap:6`** 으로 고쳤다.
  `x` 는 그대로 629 다.
- 왜: 종전 값은 **칸 폭과 칸 사이 여백을 cellW 하나에 합쳐** 두어 칸 간격이 8.07mm 였다.
  원본 실측은 **8.47mm** 라 첫 칸 글자가 오른쪽으로 **0.61mm** 밀렸다. 겹쳐 찍기로 실물 서식에
  얹으니 칸을 벗어나 보였다(서식까지 함께 인쇄할 때는 배경도 같이 밀려 덜 드러났다).
- 확인 (원본 별지서식 PDF 400dpi 실측 대비):

  | | 칸1 | 칸2 | 칸3 | 칸4 | 글자 크기 |
  |---|---|---|---|---|---|
  | 종전 | 170.39 | 178.46 | 186.53 | 194.59 | 5.00mm |
  | 보정 | 169.80 | 178.26 | 186.73 | 195.19 | 4.26mm |
  | 원본 | 169.79 | 178.27 | 186.75 | 195.20 | (칸 폭 6.75mm) |

  최대 오차 **0.61mm → 0.03mm**. `cellW` 가 줄면서 글자 크기(`min(cellW,h)*0.62`)도 함께 줄어
  6.75mm 칸 안에 여유가 생겼다.
- 기준선: `passport-adult-p1`·`passport-minor-p1` **2쪽 갱신**(353px, **관계 칸 한 곳만**).
  나머지 17쪽은 0px.
- ⚠️ **남은 일 — 같은 문제가 다른 격자에도 있다.** 원본과 대조한 결과(우리 간격 → 원본 간격):
  한글성명 10.58→10.88(최대 **−1.74mm**) · 긴급 성명 8.28→8.48(−0.88) · 전화번호 2종 7.72→7.84(−0.65) ·
  로마자 성·이름 8.46→8.48(−0.39). 주민번호(0.19)·배우자 로마자(0.07)는 양호.
  오차가 **뒤 칸으로 갈수록 누적**되므로 3글자 이름은 티가 안 나지만 5~6글자면 1mm 넘게 밀린다.
  최소제곱 보정값은 산출해 두었다(잔차 0.08mm 이하). 시행 결과를 보고 정리한다.

### [원인] 스캔 판독 실패는 위치가 아니라 **색**이었다  · 접수처 회신
- 무엇을: 여권발급신청서는 **드롭아웃 컬러 서식**이다. 기재칸 테두리가 **빨강(255,0,0)** 으로 인쇄돼
  있고, 접수처는 스캔할 때 빨강을 걸러내 적힌 글자만 읽는다.
- 왜 흑백으로는 안 되나: 빨강은 흑백 변환에서 **밝기 76** 이 되어 **검은 선**으로 인쇄된다.
  드롭아웃이 안 되니 테두리와 글자가 엉키고, 판독이 실패한다. 원본의 유채색 픽셀은 **17.2%**
  (살구색 기재란 배경 6.2% · 빨강 0.6%)이고, 서식 CSS 에도 원래 `(A4 규격 · 컬러 인쇄)` 라고 적혀 있었다.
- 위치는 아니었다: 우리 서식은 원본 별지서식과 **0.09mm** 안에서 일치한다.
  실물(미리 인쇄된 서식) 189mm · 법정 PDF 189.04mm · 우리 189.09mm — 자 분해능(±0.5mm) 아래다.
  "우리는 190" 으로 읽힌 것은 배경이 143dpi 래스터라 **선이 굵어** 바깥 가장자리를 재게 된 탓이다.
- 하드웨어 함의: 키오스크의 `M332x 382x 402x Series` 는 **모노 전용**이라 빨강을 낼 수 없다.
  **근본 해결은 컬러 프린터**다. 이 건은 담당자 판단 사항으로 올린다.

### [기능] 겹쳐 찍기 — 미리 인쇄된 서식 용지에 값만 인쇄  · 컬러 프린터 전까지의 임시 방편
- 무엇을: `kiosk.json` 에 `"overlayPrintForms": ["passport"]` 를 넣으면 그 서식은 **값만** 인쇄된다.
  실물 컬러 서식을 용지로 쓰므로 드롭아웃이 원래대로 동작한다.
- 어디를: `kiosk-app/print-options.js`(`OVERLAY_PRINT_CSS`·`isOverlayForm`) · `kiosk-app/main.js`
  (`did-finish-load` 에서 `insertCSS` 주입 · `--selfcheck` 에 표시).
  **서식 HTML 은 한 줄도 안 고쳤다** — 그래서 엔진 재빌드·`sync-kiosk`·인쇄물 기준선이 전부 그대로다.
  웹(GitHub Pages) 배포본도 영향 없다. 설정에 없으면 종전대로 서식까지 통째로 인쇄한다.
- 숨기는 것: `.bg`(배경 서식) · `.cover`(흰 마스크) · `.guide`(다시 그린 회색 안내문) ·
  `.ovhi`(서명란 노란 형광펜 — 드롭아웃 대상 색이 아니라 스캔을 방해할 수 있다).
  `.bg` 는 **`visibility:hidden`** 이다 — `display:none` 으로 지우면 `.stage` 높이가 그 이미지에서
  나오므로 좌표 기준이 통째로 무너진다.
- 확인: 겹쳐 찍기 출력을 원본 서식과 합성해 **값이 전부 칸 정중앙에 떨어지는 것**을 눈으로 확인.
  잉크 비율 **0.585%**(서식까지 찍으면 5% 안팎) — 값만 나온다는 뜻. 1쪽 A4 유지.
  `verify-print.py --electron` 19쪽 0px · `sync-kiosk --check` ✓ (서식을 안 건드렸으니 당연하다).
- ⚠️ **바뀌는 것 — 프린터 공차가 그때부터 진짜 어긋남이 된다.** 서식과 값을 함께 찍을 때는 둘이 같이
  밀려 무해했지만, 여기서는 칸이 종이에 고정되고 값만 움직인다. 기재칸 높이 **약 7mm** · 글자 **약 4mm**
  → 여유 **±1.5mm**, 프린터 정합 공차 ±1~2mm. **기기별 실물 확인이 필수**다.
- ⚠️ **2쪽 문제**: 한 인쇄 작업의 모든 쪽에 적용된다. 여권 **미성년자** 신청은 2쪽(신청서+법정대리인
  동의서)인데 트레이에는 한 종류 용지만 들어간다. 용지 운영을 정하기 전에는 미성년자 건을
  창구에서 처리해야 한다. `docs/GOTCHAS.md ㉞-1`.

### [확인] 가장자리 손실은 감수한다 — 실척 유지 결정 + 근거 측정  · 담당자 판단
- 무엇을: 용지를 고친 뒤에도 **가로에 1mm 안팎이 남았다**(개발PC 1mm · 키오스크 2mm).
  이것은 프린터의 **인쇄 불가 영역·정합 공차**로, 소프트웨어로는 없앨 수 없다.
  균일 축소로 피할 수도 있었으나(좌표가 `cqw` 단위라 균일 배율에는 정합이 깨지지 않는다),
  **실척(100%) 유지**로 결정했다. 대신 손실 구간에 내용이 없음을 확인하고, 기기별 손실량을
  검수로 기록하는 방향으로 간다.
- 근거 — 기준선 19쪽의 인쇄 내용이 가장자리에서 얼마나 떨어져 있는지 실측:

  | | 좌 | 우 | 상 | 하 |
  |---|---|---|---|---|
  | 19쪽 중 가장 좁은 여백 | **9.8mm** | **9.8mm** | 11.5mm | 10.5mm |

  → **1~2mm 가 잘려도 사라지는 내용이 없다.** 가장 빠듯한 것은 여권 서식(약 10mm)이다.
  ※ 단 하나의 예외: 여권 **법정대리인 동의서(2쪽)의 회색 점선 절취선**이 상단 4.4mm 위치에서
  좌우 끝까지 닿는다. 잘려도 무방한 절취선이다.
- 측정 방법 메모: 기준선은 **16단계로 양자화**돼 있어 **순백이 240** 이다. 임계값을 240 으로 잡으면
  종이 전체가 '자국'으로 잡힌다 — 처음에 그렇게 재서 "여백 0mm" 라는 잘못된 값이 나왔다.
  진한 잉크는 ≤128, 옅은 회색 안내문(`#c4c4c4`→192)까지 보려면 ≤208 로 잡아야 한다.
- 확인: `python tools/verify-print.py --electron` 19쪽 0px · 용지 A4.
- **실기 검수 결과** (`tools/calibration-sheet.html` 로 측정):

  | 기기 | 프린터 | 인쇄 불가 영역 | 190×277mm 실측 | 판정 |
  |---|---|---|---|---|
  | 개발PC | 삼성 흑백 프린터 (105.16.1.113) | **5mm** | **190 × 277mm** | **합격** (여유 4.8mm · 배율 100%) |
  | 키오스크 | M332x 382x 402x Series | (미측정) | (미측정) | — |

  막대 1~4 가 통째로 사라지고 5부터 보였다는 것은 프린터가 **축소하지 않고 잘라낸다**는 뜻이다
  (축소했다면 1~4 도 안쪽으로 밀려 들어와 전부 보인다). 실척이 유지되고 좌표 정합도 그대로다.

### [도구] 프린터 검수지 — 기기별 인쇄 불가 영역·배율을 재서 기록  · 위 결정의 실행 수단
- 무엇을: `tools/calibration-sheet.html` 을 만들었다. A4 한 장으로 **자 없이** 인쇄 불가 영역을 읽는다.
  네 변에 **막대 눈금**(막대 N 은 종이 끝에서 N mm 까지만 그려짐)을 두어, **처음으로 보이는 막대의
  숫자**가 그 변의 인쇄 불가 영역이 된다. 숫자는 안쪽에 있어 막대가 사라져도 읽힌다.
  가운데 **190 × 277mm** 사각형은 배율 검사용이다(자로 재서 그대로면 실척).
- 왜: 손실량이 기기마다 다르므로(1mm/2mm) **좌표로 흡수하면 안 된다.** 대신 설치할 때 기기를 검수하고
  그 값을 기록으로 남긴다. 종이에 기록란(프린터 이름·손실량·배율·판정·점검자)을 함께 두었다.
- 합격 기준을 종이에 박아 두었다 — **인쇄 불가 영역 9mm 이하 + 배율 100%**. 위 실측에서 나온 값이다.
- 확인: 크롬 렌더 1쪽 A4 · 사각형이 실제로 **10.08~200.00mm × 10.07~287.01mm** 에 그려지는 것을
  300dpi 픽셀 스캔으로 검증.
- 타 지자체 확산 때도 그대로 쓸 수 있다(서식 좌표맵이 전국 공통이므로 판정 기준도 같다).

---

## 2026.08.08

### [화면] 세부 서식 페이지에도 상단 왼쪽에 시 로고 — 허브와 같은 자리·크기  · 담당자 요청
- 무엇을: 서식 도우미 8종의 **상단바 제목 왼쪽**에 군포시 로고를 넣었다. 허브(`index.html`)와
  **같은 규칙**(높이 38px · 좁은 화면 30px · 원본 민트색)을 쓴다. 코치 패널 파란 머리의 흰색 로고는
  그대로 두었으므로, 서식 페이지에는 로고가 **두 군데**(상단바 원본색 · 파란 머리 흰색) 있다.
- 왜: 허브에서 서식으로 들어가면 상단 왼쪽 로고가 사라져 다른 사이트로 넘어간 것처럼 보였다.
- 어디를: **`tools/embed-logo.py`**(원본). 허브 전용이던 `HUB_CSS`·삽입 함수를 `TOPBAR_CSS`·
  `patch_topbar()` 로 바꿔 **허브와 서식이 같은 규칙을 공유**하게 했다 — 한쪽만 바뀌는 사고를 막는다.
  이후 `python tools/embed-logo.py` → 엔진 5종 재빌드 → `sync-kiosk.sh`.
  ⛔ 서식 HTML 을 직접 고치지 마라. 로고 구간은 `<!--GUNPO-LOGO-->` 마커 안이라 이 도구가 덮어쓴다.
- 곁다리로 고친 것: `embed-logo.py` 의 서식 목록이 낡아 **`birth` 를 손작성으로 분류**하고 있었다.
  엔진으로 옮겨진 뒤에도 목록이 안 바뀌어, 도구가 `birth-helper-v1.html` 을 직접 고치고 재빌드
  안내에서는 빠뜨렸다(다음 재빌드에 조용히 덮이는 상태). `HAND_WRITTEN` 3종 / `ENGINE_FORMS` 5종으로
  바로잡았다.
- 확인: 8종 전부 로고 2개 · 허브와 여권(손작성)·출생(엔진)의 상단바를 나란히 렌더해 **위치·크기 동일** 확인 ·
  `verify-print.py` 19쪽 **전부 0px**(상단바는 인쇄에서 숨김, 로고에도 `@media print` 규칙 추가) ·
  `sync-kiosk.sh --check` ✓.

### [수정] 「초기화」를 누르면 키오스크가 먹통이 되던 것 — 네이티브 확인창 폐지  · 포터블 시험
- 무엇을: 8종 서식의 「초기화」가 쓰던 네이티브 `confirm()` 을 **페이지 안 확인 창**(`askConfirm`)으로
  바꿨다. 아울러 `kiosk-app/main.js` 의 화면 이탈 방지 처리가 **네이티브 대화상자를 이탈로 오인하지
  않도록** 판별을 넣었다.
- 왜: 포터블로 시험하는 중 **확인 창이 뜨면 화면이 완전히 먹통**이 되었다. 원인은 두 보안 조치가
  서로 부딪친 것이다 —
  네이티브 대화상자는 Win32 모달이라 부모 창을 **비활성 상태**로 만드는데, 보안성 검토 반영으로 넣은
  이탈 방지 처리(`blur` → 첫 화면 초기화 + `focus()`)가 그 포커스 이동을 **시민이 Alt+Tab 으로 나간 것**
  으로 보고 포커스를 도로 가져갔다. 그 결과 **포커스는 있는데 입력은 막힌** 상태가 되고,
  대화상자는 전체화면(alwaysOnTop) 뒤로 밀려 눌러 닫을 수도 없었다.
- 실측(임시 검증 프로그램, 확인 후 삭제):
  `대화상자 전 isEnabled=true` → `대화상자 중 isEnabled=false` → `focus() 후 isEnabled=false·isFocused=true`.
  마지막 줄이 먹통 상태 그대로다. 덤으로 **`win.isEnabled()` 가 대화상자 판별에 쓸 수 있음**을 확인해
  그것을 방어 코드의 근거로 삼았다.
- 어디를:
  - `engine/engine.js`(`askConfirm` 신설 · `resetAll`) → **엔진 5종 재빌드**(birth·death·naming·cert·realestate)
  - 손작성 3종(`marriage`·`divorce`·`passport`)에 같은 함수를 손수 반영
  - `kiosk-app/main.js` — `blur` 핸들러에 `isEnabled()` 판별 + 짧은 지연 후 재확인
  - `docs/GOTCHAS.md` ㉜ 신설
- 참고: **`Ctrl`+`Shift`+`Q` 는 멀쩡했던 이유**는 PIN 경로가 `adminMode` 를 먼저 세워 이 핸들러를
  건너뛰기 때문이다. 이탈 방지 핸들러 하나가 원인이라는 증거였다.
- 참고: **창 닫기가 안 되는 것은 버그가 아니라 설계**다(`main.js` 의 close 차단 · `Alt`+`F4` 차단).
  시험 중에도 종료는 `Ctrl`+`Shift`+`Q` 로 한다. 이번에 손대지 않았다.
- 확인: 8종 전부 페이지 안 확인 창으로 동작(예/아니요 · 창 활성 유지 · 닫힘)을 **Electron 으로 실행 검증** ·
  `verify-print.py` 19쪽 **전부 0px**(화면 전용 변경이라 인쇄물 무변경) · `sync-kiosk.sh --check` ✓ ·
  `node --check` ✓.
- 배포: **설치본·포터블 재빌드함**(사용자 요청 — 재생성 중지의 예외). NSIS `1.0.0-x64` · portable 각 약 80MB,
  Electron 33.4.11. 포장된 `app.asar` 안에 `askConfirm` 이 들어갔고 **네이티브 `confirm(` 호출은 0건**임을
  확인했다. 빌드된 exe 로 `--selfcheck` 재실행 — 렌더러 보안·인쇄 보호 전부 `[양호]`.
  ⚠️ **빌드 중 "output file is locked" 로 멈출 수 있다** — 이전 포터블이 실행 중이면 exe 를 물고 있다.
  빌드 전에 실행 중인 프로그램을 먼저 종료할 것.

---

## 2026.08.07

### [보안] 보안성 검토 지적 반영 — 인쇄 통제·키오스크 잠금·종료 통제·잔존 방지  · 보안부서
- 무엇을: 키오스크 프로그램(`kiosk-app/`)에 아래를 넣었다. **서식·좌표·앱 화면은 무변경.**
  - **인쇄** — 설정 파일(`%ProgramData%\군포민원서식도우미\kiosk.json`)에 적힌 **실물 프린터로만** 출력
    (`deviceName` 고정). 프린터 미지정·가상 프린터·비정상 상태(일시중지·오프라인·용지없음)이면 **인쇄하지 않고**
    사유를 안내한다. `Ctrl+P` 차단. 인쇄 후 **1분이 지나도 남은 작업은 회수**하고, 시작·종료 시에도 정리한다.
  - **화면** — `sandbox: true`·`devTools: false` 명시, `app/`·`admin/` 밖으로 이동 차단, 권한 요청 전면 거부,
    개발자도구 단축키 차단. 창이 가려지면 첫 화면으로 되돌린다.
  - **실행·종료** — 중복 실행 차단(`requestSingleInstanceLock`), `Ctrl+Shift+Q`·`Alt+F4` 는 **관리자 PIN** 확인 후에만.
    단축키를 `globalShortcut`(OS 전역)에서 창 로컬로 되돌렸다 — 다른 프로그램에까지 걸리던 문제.
  - **잔존 방지** — 저장소·크래시덤프를 임시 경로로 옮기고 **시작·종료 때 비운다**(강제 종료 대비). 로깅 비활성.
  - **점검 모드** — `군포민원서식도우미.exe --selfcheck` 가 위 상태를 점검해 파일로 남긴다(보안팀 회신 근거).
  - 관리자 스크립트에 `/pin`(종료 PIN)·프린터 지정 기록·`KeepPrintedJobs` 끄기를 추가했다.
- 왜: **가장 중요한 것은 인쇄 보호가 "실패하면 열린 채로" 였다는 점이다.** `window.print` 를 무음 인쇄로
  바꿔 끼우는 방식이라, 주입이 실패하면 네이티브 인쇄 대화상자가 그대로 남아 거기서 `Hancom PDF` 를
  고를 수 있었다. 이제 주입 결과를 되읽어 확인하고, 안 걸렸으면 **안내 화면으로 멈춘다**(fail-closed).
  대기열 잔존도 같은 성격이다 — 프린터를 멈춰 두면 스풀 파일에 개인정보가 그대로 쌓였다.
- 어디를: `kiosk-app/main.js`(전면 보강) · `preload.js` · `admin/`(신설 — 설정 스크립트·안내 화면 3종) ·
  `package.json`. **`app/` 안의 서식 HTML 은 건드리지 않았다.**
  회신 문서는 `운영문서/보안성검토_조치결과서.md` 로 신설했다(항목별 조치·미조치 사유·협조 요청).

### [문서] 운영 환경 기반 보완 통제 반영 — 전용 오프라인 PC · 디스크 복원 · 도우미 상주  · 담당자
- 무엇을: 운영 조건이 확정되어(인터넷 미연결 전용 PC, 문서 편집기 미설치, PC보안관류 디스크 복원,
  민원실 상시 도우미, 셸 교체, USB·BIOS·보호필름) 이를 **보완 통제로 문서화**했다.
  - 답변서 — 제10항 「운영 환경 및 보완 통제」 신설, 제7항에 **표시 엔진 대안 검토**(WebView2·Tauri·직접 생성)
    와 **오프라인이라 취약점 실행 경로가 없다는 근거** 추가, 제12항 **위험 수용 결재란** 신설.
    Alt+Tab 항목을 '협조 요청'에서 **'셸 교체로 차단'** 으로 확정.
  - `운영문서/붙임4_키오스크_운영점검표.md` **신설** — 일일·주간·반기 점검(서명란) · 사고 대응 · 도우미 교육 8항 ·
    관리 책임자 지정 · 기록 1년 보관.
  - 붙임3 — 제Ⅱ항을 **설치 순서표**로 재작성, 제Ⅺ항을 「운영 환경 통제」로 확대(셸 교체·물리 통제·디스크 복원),
    제Ⅹ항에 복원 보호 해제→재저장 절차 추가.
- 왜: **인적·환경 통제는 기록이 남아야 통제로 인정된다.** "도우미가 보고 있다"는 통제가 아니고
  "점검표에 서명한다"가 통제다. 그리고 대안 검토를 적어 두지 않으면 "왜 Electron 을 계속 쓰는가"가 다시 지적된다.
  - ⚠️ **자체 발견한 위험**: 디스크 복원과 fail-closed 인쇄가 만나면 **재부팅마다 설정이 날아가 인쇄가 안 된다.**
    설정(`kiosk.json`)이 복원 대상에 들어간 채로 기준 이미지를 뜨면 그렇게 된다.
    **설정 → 점검 → 기준 이미지 저장** 순서를 붙임3 제Ⅱ항에 경고와 함께 못박고, 재부팅 후 `--selfcheck`
    재확인을 설치 마지막 단계로 두었다.
- 어디를: `운영문서/보안성검토_조치결과서.md` · `운영문서/붙임4_키오스크_운영점검표.md`(신설) ·
  `운영문서/붙임3_키오스크_설치운영_안내.md` · `README.md`. **코드 무변경.**
- 파생물: **사용자 요청으로 운영문서 PDF 4종을 생성·재생성**했다(시범운영 중 재생성 중지의 예외).
  `운영점검표.pdf`(5쪽, 신규 — 현장에서 인쇄해 서명하는 서식) · `보안성검토_조치결과서.pdf`(13쪽, 신규) ·
  `사용매뉴얼.pdf`(9쪽) · `사용매뉴얼_상세.pdf`(27쪽) — 뒤 두 개는 이번 보안 조치로 **동작 설명이 달라져** 재생성했다.

### [정비] 보안성 검토 답변서·운영 점검표를 비공개로 전환  · 담당자 결정
- 무엇을: 아래 4개 파일을 **추적에서 제외**했다(`.gitignore`). 파일은 로컬에 그대로 있다.
  `보안성검토_조치결과서.md`·`.pdf` / `붙임4_키오스크_운영점검표.md` / `운영점검표.pdf`
- 왜: 지적사항과 조치 내용, 관리자 단축키·PIN 운영 구조, 담당자 연락처가 담겨 있어
  공개 저장소에 둘 성격이 아니다. 담당 부서 내부에서만 보관·전달한다.
- ⚠️ **이미 푸시된 뒤의 결정이라 커밋 `dec449d` 의 히스토리에는 남아 있다.** 최신 상태에서만 사라진다.
  완전히 지우려면 히스토리를 다시 쓰고 강제 푸시해야 하는데, 공개 히스토리를 되쓰는 일이라 보류했다.
- 참고: 다른 문서(`붙임3`·`CHANGELOG`)가 붙임4를 참조하는 부분은 그대로 두었다 — 문서를 받는 쪽은
  네 파일을 함께 전달받는다.

### [문서] 검토 결과와 조치를 한 문서로 합침 — 「보안성 검토 보고서」 신설  · 담당자 요청
- 무엇을: 종전의 「보안성 검토 결과 통보」와 「조치결과서」를 합쳐
  `운영문서/보안성검토_보고서.md`(제Ⅰ~Ⅻ장 + 붙임)를 신설했다. 검토 개요·시스템 개요·**항목별 지적→조치→확인**·
  운영 환경 보완 통제·점검 기능·시험 결과·잔여 위험과 위험 수용·개인정보 처리 관점·향후 관리 계획을 담았다.
  제출용 문서라 PDF 15쪽을 함께 생성했다(시범운영 중 재생성 중지의 예외). 종전 조치결과서는 **지우지 않고**
  로컬에 남겨 두었고, 대체 관계는 보고서 머리에 적었다.
- 왜: 받는 쪽이 지적과 조치를 **두 문서를 번갈아 보며 대조**해야 했다. 결재·보관되는 형태는 항목별로
  지적·조치·확인이 한 자리에 있는 하나의 보고서다. 또 개요·잔여 위험·위험 수용·관리 계획이 없으면
  '답변'이지 '보고서'가 아니어서 결재선에 올릴 문서로 성립하지 않았다.
- 어디를: `운영문서/보안성검토_보고서.md`(신설) · `운영문서/보안성검토_보고서.pdf`(15쪽, 신규) · `.gitignore`.
  **코드·서식·좌표·화면 무변경.**
- 비공개: 무시 패턴을 파일별 나열에서 `운영문서/보안성검토*` 로 **넓혔다** — 문서 이름이 늘 때마다 한 줄씩
  더하다 빠뜨리는 사고를 막는다. 보고서도 조치결과서와 같은 성격(지적사항·관리자 단축키·확인 번호 운영
  구조·담당자)이라 추적하지 않는다. ⚠️ **파일은 로컬에 그대로 있다. 지우지 말 것.**
- 표기: 하이픈으로 끊은 날짜·전화번호 형태 숫자열을 문서에 넣지 않고 연락처 자리는 〔 〕 로 비워 두었다.
  DRM 이 개인정보로 오인해 자동 암호화하면 **받는 쪽에서 열지 못한다**(`docs/GOTCHAS.md`).
  생성된 PDF 에 `/Encrypt` 가 없음을 확인했다.
- 확인: PDF 15쪽·암호화 없음 · `verify-print.py` 19쪽 전부 0px · `sync-kiosk.sh --check` ✓
  (코드 무변경이라 회귀는 예상되지 않았지만 관행대로 돌렸다).

### [문서] 추가 확인 요청 3건 회신 — 셸 변경 방식·이동식 저장장치·펌웨어 암호  · 보안부서
- 무엇을: 보안부서가 제2항(전용 계정·셸 교체·USB·BIOS)에 대해 되물은 세 가지를 문서에 확정해 적었다.
  - **셸 방식** — 우리 방식은 **레지스트리 기반 셸 변경**(사용자별 `HKCU\…\Winlogon\Shell`)이고
    **Shell Launcher 가 아니다.** Shell Launcher 는 Enterprise·Education·IoT Enterprise 판 전용이라
    이 키오스크(**Windows 11 Pro**)에서는 기능 자체가 없다. 할당된 액세스도 스토어 앱·브라우저 전용이라
    일반 exe 인 이 프로그램에는 못 쓴다. 상위 판 상향 시 전환 가능하다는 것까지 적었다.
  - **이동식 저장장치** — 「모든 이동식 저장소 클래스: 모든 액세스 거부」(읽기·쓰기 동시)를 주 통제로,
    `USBSTOR\Start=4` 를 보조로, **미사용 포트 포트락 + 사용 포트 잠금 커버 + 케이스 잠금**을 물리 통제로.
    정책 위치·레지스트리 값·`gpupdate` 까지 명시했다.
  - **펌웨어 암호** — 관리자 암호 설정 · 부팅 순서 내장 디스크 고정 · 외부 매체·네트워크 부팅 해제 · 보안 부팅 사용.
- 왜: “확인이 필요하다”는 되물음은 **어떤 기능인지 이름으로 특정해 주지 않으면 끝나지 않는다.** 그리고
  Pro 판에서 Shell Launcher 를 쓰겠다고 답하면 적용 단계에서 막힌다(판 요구사항 미충족).
- ⚠️ **적용 순서와 확인 계정을 함께 못박았다** — 이 둘이 이번 회신의 실질이다.
  - 셸·정책은 **레지스트리에 기록**되므로 **디스크 복원 기준 이미지 저장 전에** 적용해야 한다.
    먼저 이미지를 뜨면 재부팅마다 셸과 차단 정책이 함께 사라진다(`kiosk.json` 과 같은 함정).
  - 저장장치 차단 확인은 **반드시 시민용 계정에서** 해야 한다 — 관리자 계정은 정책에서 제외되는 경우가 있어
    “관리자에서 열렸으니 차단 실패”로 오판하기 쉽다. 윈도우 갱신 후 정책이 풀린 사례도 있어 재확인 항목을 뒀다.
  - **부팅 암호(전원 투입 시)는 설정하지 않기로** 하고 이유를 적었다 — 정전 복구 등 무인 재기동에서
    암호 화면에 멈추면 프로그램이 떠 있지 않고, 결국 일일 재부팅을 건너뛰어 **복원 통제가 무력화**된다.
    외부 매체 부팅 차단은 부팅 순서 고정 + 관리자 암호로 동일하게 달성된다.
  - 셸 변경은 **`HKCU` 로만** — `HKLM` 에 걸면 관리자 계정에서도 탐색기가 안 떠 복구가 어려워진다.
- 어디를: `운영문서/보안성검토_보고서.md`(제Ⅳ장 2 마목 신설 · Ⅲ·Ⅴ장 반영) ·
  `운영문서/붙임3_키오스크_설치운영_안내.md`(제Ⅺ항 가·나목 재작성, 설치 순서 6번 구체화) ·
  `운영문서/붙임4_키오스크_운영점검표.md`(반기 ㈓㈔ 신설 · 갱신 후 임시 점검 행 · 암호 봉인 보관).
  **코드·서식·좌표 무변경.** 종전 조치결과서는 보고서가 대체하므로 손대지 않았다.
- 근거: Shell Launcher 판 요구사항·이동식 저장소 정책 값은 Microsoft 문서로 확인했다(Pro 미지원 확인).
- 남은 일: **PDF 재생성 미실시**(요청 시). 정책·포트락·펌웨어 설정은 정보통신과 협조 사항이라
  **실제 적용과 실측 확인은 키오스크 PC 설치 시** 수행하고 결과를 회신에 첨부한다.

### [배포] 보안 조치 반영 설치본 빌드 — 시범운영 전 선조치  · 담당자 판단
- 무엇을: `npm run dist` 로 NSIS 설치본·portable 을 다시 만들었다(각 약 80MB, Electron 33.4.11).
  보안 조치는 시범운영에 앞서 적용해야 한다는 판단에 따라 재생성 중지의 예외로 진행했다.
- ⚠️ **빌드하면서 잡은 것** — 관리자 스크립트(`.bat`·`.ps1`)가 `app.asar` **안에** 포장됐다.
  asar 는 압축 아카이브라 그 안의 파일은 **실행할 수 없다.** 그대로 배포했으면 담당자가 프린터 지정을 못 해
  **인쇄가 아예 안 되는 키오스크**가 될 뻔했다(미지정이면 인쇄를 막도록 만들었으므로).
  → `files` 를 `admin/*.html`·`admin/pin-preload.js` 로 좁히고, 스크립트는 `extraResources` 로 **실파일로** 내보낸다
  (`<설치 폴더>\resources\admin\`). 붙임3 제Ⅰ·Ⅴ항에 이 경로를 적었다.
- 확인: **포장된 실행본**으로 `--selfcheck` 실행 — sandbox·contextIsolation·nodeIntegration·devTools 차단·
  인쇄 보호 **전부 [양호]**. asar 목록에 HTML 4개만, `resources/admin/` 에 스크립트 2개 실파일 확인.
- 남은 일: **화면을 띄워야 확인되는 것들은 미검증**이다 — PIN 종료·Alt+F4 차단·중복 실행·창 이탈 초기화·
  인쇄 실패 안내 화면, 그리고 **실물 프린터 무음 인쇄**. 키오스크 PC 설치 후 시험하고 결과를 회신에 첨부한다.

### [수정] 운영문서 변환기가 여러 줄 목록을 목록 밖으로 떨어뜨리던 것 · 자체 발견
- 무엇을: `tools/make-manual.py` 가 한 항목이 여러 줄인 목록의 **이어짐 줄을 같은 `<li>` 안으로 이어 붙이도록** 고쳤다.
- 왜: 종전에는 들여쓴 이어짐 줄이 **목록 밖 문단으로 떨어져 나가** 조판이 어긋났다. 오류가 나지 않고
  모양만 이상해지는 종류라 눈으로 보지 않으면 모른다. 현재 운영문서에 24곳 있었다.
- 어디를: `tools/make-manual.py` · `docs/GOTCHAS.md` ㉑-1(지원하는 마크다운 범위와 함께 신설).
- 확인: 4종 재생성 후 쪽수·판독 정상(암호화 없음). 답변서의 항 번호 오기(제11항 → **제10항**)도 함께 고쳤다.
- 확인: `--selfcheck` 실행 — sandbox·contextIsolation·nodeIntegration·devTools 차단·인쇄 보호 **전부 [양호]**,
  이 PC의 가상 프린터 3종(`Hancom PDF`·`Microsoft Print to PDF`·`OneNote`) 탐지 확인.
  프린터 상태·대기열 조회(PowerShell) 동작 확인. `verify-print.py` 19쪽 0px · `sync-kiosk.sh --check` ✓.
- 남은 일(앱 코드로 해결 불가 — 문서에 명시):
  - **`Alt+Tab`·`Windows` 키 차단은 OS 조치가 필요하다**(전용 계정 + 셸 교체 + 정책). 붙임3 제Ⅺ항 신설.
  - **Electron 33 은 지원 종료**(현재 지원: 42·43·44). 44 로 올려야 하는데, 크로미움이 바뀌면 **인쇄 조판이
    달라질 수 있고 현재 안전망은 데스크톱 크롬으로 검증하므로 그걸 못 잡는다.** Electron 자체의 `printToPDF`
    기반 회귀 검증을 먼저 만든 뒤 올리기로 하고 이번에는 착수하지 않았다.
  - 인쇄 직전 안내 창 문구가 아직 예전 기준이다(`[인쇄 창 열기]`). 엔진 수정 → 5종 재빌드 → 손작성 3종 반영 필요.

### [정비] 크롬 키오스크 방식 폐지 — 배포는 설치본 하나로  · 보안성 검토 대응
- 무엇을: `키오스크배포/`(크롬 실행 세트)와 `tools/pack-kiosk.sh` 를 삭제했다. 관리자 스크립트 2종은
  `kiosk-app/admin/` 으로 옮겨 exe 기준으로 다시 썼다(크롬 정책 설정은 제거, `/undo` 에서 정리만 한다).
  `sync-kiosk.sh` 동기화 대상이 3곳 → **2곳(루트·`kiosk-app/app/`)** 으로 줄었다.
- 왜: 크롬 방식은 **인쇄 대화상자가 구조적으로 열려** 가상 프린터를 고를 수 있고, `deviceName` 고정·
  `Ctrl+P` 차단·`Alt+F4` 차단이 모두 불가능하다. 보안 요구를 맞출 수 없는 경로를 남겨 두면 지적이 반복되고
  문서도 두 갈래로 관리해야 한다.
- 어디를: `키오스크배포/`(삭제) · `tools/pack-kiosk.sh`(삭제) · `tools/sync-kiosk.sh` · `tools/embed-logo.py` ·
  `README.md` · `CLAUDE.md` · `engine/README.md` · `docs/GOTCHAS.md`(㉑·㉓·㉖ 갱신, ㉖-1·㉖-2 신설) ·
  `docs/타지자체-확산.md` · `운영문서/붙임1~3`.
- 확인: `sync-kiosk.sh --check` ✓ · 리포 전체에서 `키오스크배포`·`pack-kiosk` 잔존 참조 없음(archive 제외).

### [수정] 인쇄물 회귀 검증이 날짜 때문에 매일 실패하던 것 · 자체 발견
- 무엇을: `verify-print.py` 가 렌더할 때 **시계를 `FIXED_DATE`(2026.08.04)로 고정**한다.
  고정 스크립트를 `<head>` 첫머리에 주입한다 — 서식이 로드 시점에 `APP_TODAY=new Date()` 로
  오늘을 붙잡으므로 `</body>` 뒤 주입으로는 늦다.
- 왜: 서식에 **신청일·동의일이 오늘 날짜로 찍힌다.** 기준선을 만든 2026.08.04 이후로는
  코드를 한 줄도 안 고쳐도 19쪽 전부 '회귀'로 잡혔다(diff 이미지에서 빨간 곳은 '일' 한 글자뿐).
  **이 프로젝트의 유일한 안전망이 매일 거짓 경보를 내는 상태**였고, 그대로 두면 `--update` 로
  기준선을 오염시켜 진짜 좌표 회귀까지 놓치게 된다.
- 어디를: `tools/verify-print.py`(`FIXED_DATE`·`CLOCK_FREEZE` 신설), `docs/GOTCHAS.md` 항목 ㉛.
  **앱 코드·서식·좌표·기준선 이미지는 무변경.**
- 확인: **기준선을 갱신하지 않은 채** `verify-print.py` 19쪽 전부 0px — 고정한 날짜가 기존
  기준선과 정확히 맞는다는 증거다. `sync-kiosk.sh --check` ✓.

### [정리] `preload.js` 주석의 문서 참조 오기
- `붙임4 제Ⅴ항` → `운영문서/붙임3 제Ⅵ항`. 붙임4는 존재하지 않고, 아래 항목에서 장 번호가 밀렸다. 동작 무변경.

## 2026.08.06

### [정비] 운영문서를 키오스크 전용 프로그램(exe) 기준으로 전환  · 담당자 요청
- 무엇을: 정식 운영 기준을 **크롬 실행 파일 → Electron 설치본(`.exe`)** 으로 바꾸고 문서를 그에 맞게 고쳤다.
  `붙임3` 전면 재작성(설치·관리자 단축키·기본 프린터 지정·일일 점검·업데이트 절차 신설, 크롬 방식은 제Ⅺ항 보조로 강등),
  `붙임2`·`붙임2-1` 의 인쇄 절차를 “인쇄 창이 열린다” → “대화상자 없이 바로 출력”으로 정정,
  `붙임1` 의 구동·배포·개인정보 항목 정합화, `README.md` 배포 절 A/B 순서 교체.
- 왜: exe 방식은 인쇄 대화상자를 열지 않고 여백·배율·배경 인쇄를 코드로 고정하므로(`kiosk-app/main.js`),
  크롬 기준으로 쓰인 기존 문서가 실제 동작과 어긋났다. 특히 **① 인쇄 창이 뜨지 않는 것이 정상**,
  **② 기본 프린터가 가상 프린터면 그대로 파일이 남는다**, **③ 인쇄 실패에도 화면은 초기화된다** 는
  운영상 중요한 사실이 어느 문서에도 없었다.
- 어디를: `운영문서/붙임1~3`, `README.md`, `CHANGELOG.md`. **앱 코드·서식·좌표는 무변경.**
- 확인: `verify-print.py` 19쪽 전부 0px · `sync-kiosk.sh --check` ✓ · 붙임3 내부 장 번호 참조 대조.
- 남은 일: 인쇄 직전 안내 창 문구가 아직 크롬 기준이다(단추 이름 `[인쇄 창 열기]`, “인쇄 창에서 여백·배율을
  맞추세요”). exe 에서는 사실과 다르므로 **문서에 ‘화면 문구와의 차이’로 명시해 두었다.** 엔진 문구를 고치려면
  `engine/base.html` 수정 → 5종 재빌드 → 손작성 3종 반영 → 인쇄 회귀 재확인이 필요하다(미착수).

## 2026.08.04

### [정비] 공유·인수인계 준비 — 회귀 안전망, 작업 지침, 확산 도구
- 무엇을: ① `tools/verify-print.py` + `tests/baseline/`(19쪽) — 8종×예시2 인쇄물을 픽셀 비교하는
  정식 회귀 검증. ② `CLAUDE.md`·`docs/GOTCHAS.md` — 원본/생성물 구분·필수 절차·함정 30여 건을
  리포 안으로 이관. ③ `tools/rebrand.py`·`docs/타지자체-확산.md` — 기관명·부서·연락처·로고 일괄 교체.
  ④ `LICENSE`(공공누리 제1유형).
- 왜: 프로젝트를 타 기관과 공유하거나 유지보수를 다른 담당자·AI에게 맡길 상황에 대비. 그때까지
  인쇄물 검증이 매번 임시 스크립트로만 이뤄져 **회귀 안전망이 인수인계되지 않는 상태**였다.
- 어디를: 도구·문서만 추가. 서식·엔진·앱 코드는 무변경(로고 파일명 `assets/gunpo-logo.png` →
  `assets/logo.png` 로 일반화하고 두 도구가 예전 이름도 받도록 함).
- 확인: `verify-print.py` 19쪽 전부 0px(연속 2회 재현) · 회귀 탐지·종료코드 1·diff 이미지 동작 확인 ·
  `rebrand.py` 는 실제 실행 후 되돌려 검증(193곳/28파일, base64·로고 마커 보존) · `sync-kiosk.sh --check` ✓

## 기준선 — 시범운영 시작 시점 (2026.08.03, 커밋 3bfece5)

서식 8종 프로토타입 완성. 이후 변경은 이 기준선과 비교한다.

**서식 추가 이력**

| 날짜 | 서식 | 커밋 |
|---|---|---|
| 2026.07.10 | 여권발급신청서 (프로젝트 초기 커밋) | `1e4ada9` |
| 2026.07.14 | 혼인신고서 | `8530ac8` |
| 2026.07.15 | 이혼(친권자 지정)신고서 | `60f297f` |
| 2026.07.22 | 출생신고서 · 사망신고서 (공통 엔진 도입) | `02fad6b` · `d0705bc` |
| 2026.07.23 | 개명신고서 | `d1ef992` |
| 2026.07.24 | 증명서 교부 등 신청서 · 키오스크 배포 세트 | `0db61b6` · `79d07cd` |
| 2026.07.28 | 부동산거래계약 신고서 (+별지) | `ba22681` · `d44905a` |

**민원실 의견을 이미 반영한 것** (시범운영 전)

- 인쇄창에서 'PDF로 저장'이 되면 개인정보 파일이 PC에 남는다 → 앱의 'PDF 저장' 문구 제거 + `관리자_개인정보보호_설정.bat` 으로 가상 프린터·크롬 정책 차단 (`809119e`)
- 안내 팝업이 작고 눈에 띄지 않는다 → 네이티브 `alert` 을 크고 붉은 화면 모달로 교체 (`46f8ab7`)
- 공용 PC라 앞사람 입력이 남는 것이 걱정된다 → 3분 무동작 초기화 + 인쇄 직후 초기화 + 개인정보 안내 띠 (`8569dfc`, `9f74ffe`)
- 인쇄 폭이 달라 판독기가 못 읽는다 → 서식이 아니라 프린터 설정 문제(배율 100%·여백 없음)로 확인, 인쇄 전 안내로 처리 (`e9ef1f7`, `76efc44`)
- 긴급여권인데 사증 면수 58면이 찍힌다 → 긴급은 12면 고정, 면수 체크 안 함 (`20e3086`)

**시범운영 기간 규칙**

- 파생 산출물(운영문서 PDF · `키오스크배포.zip` · Electron 설치본)은 **재생성하지 않는다.** 피드백을 모아 한 번에 반영한 뒤 다시 만든다.
- 서식 내용을 고쳤으면 `bash tools/sync-kiosk.sh` 로 배포 3곳을 맞추고, 인쇄물은 기준선과 픽셀 비교한다.
