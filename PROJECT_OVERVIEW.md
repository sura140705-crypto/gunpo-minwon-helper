# 무엇을 만들고 있으며, 왜 만드는가

> 군포시 민원실 **신청서 작성 도우미**의 개요 문서입니다.
> 처음 오신 분·발표 자료를 만드는 분·다른 기관에서 검토하는 분이 **이것 하나만 읽어도**
> 이 프로젝트가 무엇인지 알 수 있게 쓴 것이 목적입니다.

| 이 문서 | 프로젝트의 목적·경험·현황 |
|---|---|
| [`docs/디자인검토/DESIGN_PHILOSOPHY.md`](docs/디자인검토/DESIGN_PHILOSOPHY.md) | **어떤 원칙으로** 디자인하는가 (기준 문서) |
| [`docs/UX_DECISIONS.md`](docs/UX_DECISIONS.md) | **무엇을 고민했고 왜 그렇게 정했는가** |
| [`docs/EXPANSION_PLAYBOOK.md`](docs/EXPANSION_PLAYBOOK.md) | 새 서식·다른 기관으로 **어떻게 넓히는가** |
| [`docs/history/`](docs/history/) | 화면이 **어떻게 여기까지 왔는가** |
| [`README.md`](README.md) | 리포 구조·도구·배포·검증 (개발자용) |
| [`CHANGELOG.md`](CHANGELOG.md) | 무엇을 언제 왜 바꿨는가 (일자별 원장) |

> ⚠️ **이 문서는 홍보문안이 아니라 프로젝트 기록입니다.** 아직 하지 않은 것은 「설계 방향」으로
> 적고, 재지 않은 효과는 적지 않습니다. 무엇을 재지 않았는지는 §6 에 따로 적어 두었습니다.

---

## 1. 한 줄로

> **어려운 행정서식을 시민이 이해할 수 있는 질문으로 바꾸고,
> 그 답을 실제 법정서식의 정확한 위치에 얹어 인쇄해 준다.**

기본 철학은 하나입니다.

> **서식을 새로 만드는 것이 아니라, 기존 행정서식을 시민이 쉽게 작성하도록 돕는다.**

### 이것이 **아닌** 것

| | |
|---|---|
| ⛔ 전자민원 시스템이 아닙니다 | 접수·전송을 하지 않습니다. 종이를 뽑아 창구에 내는 것으로 끝납니다 |
| ⛔ 민원서식 안내 웹페이지가 아닙니다 | 읽어 주는 것이 아니라 **실제 서식이 채워집니다** |
| ⛔ 서식을 새로 디자인하는 일이 아닙니다 | 법정 별지서식 원본 위에 값을 얹습니다. 서식 자체는 손대지 않습니다 |
| ⛔ 전용 키오스크 장비 사업이 아닙니다 | 일반 PC·모니터·프린터에서 도는 프로그램입니다 |

---

## 2. 왜 만드는가

민원실에서 시민이 겪는 어려움은 「서류를 낼 줄 몰라서」가 아니라 **「종이를 채울 줄 몰라서」**
생깁니다.

- 행정용어를 시민이 해석해야 합니다 — `신청인 구분`, `등록기준지`, `공동친권자`
- 어느 칸에 무엇을 적는지 종이만 봐서는 알 수 없습니다
- 틀리면 다시 받아 다시 씁니다. 뒤에 사람이 기다립니다
- 창구 직원은 같은 설명을 하루에 수십 번 반복합니다

이 프로그램은 그 사이에 들어갑니다. **서식을 없애지 않고, 서식을 채우는 일을 돕습니다.**

### 쓰는 사람과 상황

| | |
|---|---|
| **누가** | 민원실에 온 시민. **고령자 비중이 높습니다.** 대부분 처음 쓰고, 다시 오지 않습니다 |
| **어디서** | 창구 옆. **서서**, 뒤에 사람이 기다리는 채로 3~10분 |
| **무엇으로** | 터치 · 마우스 · 물리 키보드를 **모두** 씁니다 |
| **환경** | 오프라인 Electron(Chromium) 설치본. 인터넷·웹폰트·외부 CDN 없음 |
| **끝나는 곳** | 화면이 아니라 **프린터**입니다 |

---

## 3. 핵심 경험

```
   시민의 말로 민원 선택
        ↓
   필요한 경우 사전 준비사항 확인
        ↓
   쉬운 질문에 답변
        ↓
   실제 법정·행정서식의 해당 위치에 반영
        ↓
   검토
        ↓
   출력
        ↓
   개인정보 Clear
        ↓
   Main 복귀
```

지금 코드에서 이 흐름이 놓이는 자리는 이렇습니다.

| 단계 | 어디에서 | 지금 상태 |
|---|---|---|
| 민원 선택 | `index.html` — 시민의 말로 된 카드 8장 | 구현됨 |
| 사전 준비 | 카드를 누르면 **그 자리에서 펼쳐지는 안내판**(별도 페이지가 아닙니다) | 구현됨 · 내용은 여권만 채워져 있음 |
| 질문·답변 | 각 서식 HTML | 구현됨 |
| 서식 반영 | 서식 이미지 위 **절대좌표**에 값을 얹음 | 구현됨 · 인쇄물 픽셀 검증 중 |
| 검토 | 인쇄 직전 요약 화면 | 구현됨 |
| 출력 | 지정 프린터로만 · 여백 없음 · 배율 100% | 구현됨 |
| 개인정보 Clear | 인쇄 직후 화면을 덮고 → 첫 화면으로 | 구현됨 |
| Main 복귀 | 첫 화면으로 이동하며 입력값 소멸 | 구현됨 |

> 흐름의 **구조와 전환 규칙**은 [`docs/디자인검토/2026.08.24-Main-Idle-Start-구조동결-v1.md`](docs/디자인검토/2026.08.24-Main-Idle-Start-구조동결-v1.md) 에서 동결돼 있습니다.

### 화면의 문법 — 세 영역

어떤 화면에서도 구역이 생기거나 사라지지 않습니다. **내용만** 바뀝니다.

```
┌─ BAR ─ 로고·기관명 ──────────── 오늘 날짜 ── 크게 보기 ──┐
├──── ASK ──────────┬──── PAPER ────┬──── RAIL ─────────┤
│ 지금 시민이        │ 실제 작성되고  │ 작성하는 동안       │
│ 해야 할 것         │ 있는 행정서식  │ 계속 참고할 정보    │
└───────────────────┴───────────────┴────────────────────┘
```

**세 영역의 역할을 섞지 않습니다.** 특히 RAIL 에는 「지금 해야 할 행동」을 넣지 않습니다 —
행동은 ASK 가 담당합니다. → [UX_DECISIONS ②](docs/UX_DECISIONS.md#d2)

---

## 4. 제품의 특징

아래는 전부 **지금 코드에서 확인된 것**입니다. 계획은 §5 에 따로 적었습니다.

### 시민 쪽

- **복잡한 서식의 항목 위치와 행정용어를 직접 해석하지 않아도 됩니다.**
  `신청인 구분`이 아니라 「누구의 여권을 신청하시나요?」로 묻습니다.
- **실제 신청서(PAPER)를 화면 가운데 두어 작성 결과를 계속 확인합니다.**
  결과 화면이 아니라 작성 내내 보이는 **주인공**입니다.
- **질문(ASK)과 실제 서식(PAPER)이 연결돼 있습니다.**
  답하면 서식의 그 칸에 값이 얹히고, 지금 적는 자리를 은은하게 짚어 줍니다.
- **작성 중 참고정보(RAIL)를 같은 화면에서 제공합니다.** 화면을 옮기지 않습니다.
- **막히면 직원에게 가는 길이 있습니다.** 오류 처리가 아니라 정상 경로의 하나입니다.
- **일반 보기 / 크게 보기** 두 가지. 글자만 키우는 것이 아니라 조작 영역·간격도 함께 키웁니다.

### 개인정보

- **입력값은 메모리에만 있습니다.** `localStorage`·`sessionStorage`·`indexedDB` 를 쓰지 않고,
  파일에 쓰지 않으며, 외부로 보내지 않습니다(외부 통신 자체가 없습니다).
- **무동작 3분**(설정 가능) → 마지막 **30초 카운트다운 경고** → 첫 화면으로 이동, 입력값 소멸.
- **인쇄 직후** 화면을 즉시 덮고 5초 뒤 첫 화면으로.
- 첫 화면에는 지울 개인정보가 없으므로 경고 없이 조용히 시연으로 돌아갑니다.

> ⚠️ **못 하는 것도 적어 둡니다.** 브라우저가 「인쇄」와 「취소」를 구분해 주지 않아
> **취소해도 초기화됩니다**(인쇄 전 안내에 그렇게 적어 두었습니다).
> 「PDF로 저장」 차단은 앱이 할 수 없어 **PC 설정의 몫**입니다.
> → [UX_DECISIONS ⑭](docs/UX_DECISIONS.md#d14)

### 운영·확산

- **일반 PC 환경에서 운영할 수 있는 방향입니다.** 별도 서버·네트워크·전용 장비가 필요 없습니다.
- **특정 고가 키오스크 장비에 UX를 종속시키지 않습니다.** 터치·마우스·키보드에서 모두 됩니다.
  hover 로만 알 수 있는 정보나 기능을 만들지 않습니다.
- **기관명·대표색·로고·취급 서식·업무 기준을 교체할 수 있습니다.** 소스를 고치지 않고
  설치본의 환경설정 창에서 바꿉니다. 화면 색은 **대표색 하나에서 계산**합니다.
- **취급하지 않는 민원은 카드가 아예 뜨지 않습니다.** 8장인 기관도, 4장인 기관도 있습니다.
- **현재 8종이지만 서식을 늘릴 수 있는 구조입니다.** 좌표맵은 법정 별지서식 기준이라
  전국 공통으로 재사용됩니다.

### 서식 8종

| 시민의 말 | 행정서식명 |
|---|---|
| 여권 만들기 | 여권발급신청서 (미성년 법정대리인 동의서·위임장 포함) |
| 아이 출생 신고하기 | 출생신고서 |
| 혼인 신고하기 | 혼인신고서 |
| 사망 신고하기 | 사망신고서 |
| 개명 신고하기 | 개명신고서 |
| 이혼·친권자 신고하기 | 이혼(친권자 지정)신고서 |
| 가족관계증명서 발급받기 | 증명서 발급신청 (가족관계·기본·혼인관계·입양·친양자 5종) |
| 부동산 거래 신고하기 | 부동산거래계약 신고서 (+ 당사자·부동산 별지) |

> ⚠️ 증명서에 **주민등록 등·초본은 포함되지 않습니다.** 「서류 떼기」처럼 넓은 표현을
> 쓰지 않는 이유입니다.

---

## 5. 지금 어디까지 왔나 (2026.08.27)

**2026년 9월 정식 서비스**를 목표로 개발 중이며, 그때까지는 시범운영 상태입니다.

| | 상태 |
|---|---|
| 서식 8종 작성·인쇄 | **구현됨** · 인쇄물 19쪽 픽셀 회귀 검증 중 |
| 첫 화면(Main) 재설계 — 세 영역·카드 펼침·미선택 시작 | **구현됨** (2026.08.25) |
| 대기 시연 — 별도 대기 화면 없이 첫 화면이 스스로 시연 | **구현됨** (2026.08.25 도입 · 08.27 개편) |
| 개인정보 Clear (무동작·인쇄 후) | **구현됨** |
| 일반 / 크게 보기 | **구현됨** |
| 기관별 설정(기관명·대표색·로고·취급 서식·업무 기준) | **구현됨** · 화면 회귀 검증 중 |
| 여권 — 세 영역 흐름(Reference UI) | **구현됨** (2026.08.21) |
| 나머지 7종 — **자리**(상단 바·좌우 배치·단계 표시) | **구현됨** (2026.08.25) |
| 나머지 7종 — **흐름**(세 영역 상태기계) | **설계 방향** · 미착수 → [UX_DECISIONS ⑩](docs/UX_DECISIONS.md#d10) |
| 세로 화면(1080×1920) **전용 배치** | **설계 방향** · 지금은 세 영역을 위아래로 쌓는 수준 |
| 민원 검색 UI | **설계 방향** · 검색용 데이터(`keywords`·`aliases`)는 이미 카탈로그에 있음 |
| Main → Writing 전환 덮개 | **미구현** · 흰 깜빡임이 남아 있음 |
| 모션·시각 토큰의 **값** 확정 | 외부 검토 진행 중 → `docs/디자인검토/2026.08.25-의뢰서-모션과-시각.md` |

남은 행정 절차는 [`README.md`](README.md) 에 있습니다.

---

## 6. 확인한 것과 **확인하지 않은 것**

이 문서를 발표 자료의 근거로 쓸 수 있게 하려면, **재지 않은 것을 재었다고 하지 않는 것**이
가장 중요합니다.

### 확인한 것

| | 어떻게 확인했나 |
|---|---|
| 인쇄물이 서식 원본과 어긋나지 않는다 | `tools/verify-print.py` — 8종×작성예시2 = **19쪽을 기준선과 픽셀 비교**. 「전부 0px」이 기본값이고, 달라지면 이유를 적게 돼 있습니다 |
| 키오스크 실제 인쇄 경로도 같은 결과다 | 같은 19쪽을 **Electron 조판**으로 다시 검사(`--electron`) + 용지(A4) 검사 |
| 기관 설정이 화면에 실제로 걸린다 | `tools/verify-site-config.py` — 6가지 조합 |
| 9개 화면이 같은 토큰·같은 설정 블록을 쓴다 | `tools/check-design-tokens.py` · `tools/check-site-block.py` |
| 저장소를 쓰지 않는다 | 코드에 `localStorage`·`sessionStorage`·`indexedDB` 호출이 없음 |
| 별도 서버·네트워크 없이 돈다 | 자체완결 HTML — `file://` 로 열립니다. 외부 통신 없음 |

### 확인하지 **않은** 것

> 아래는 **재지 않았습니다.** 발표 자료에 수치로 쓰지 마십시오.

- 민원 처리 시간 단축 정도
- 창구 반려·재작성 감소 정도
- 시민 만족도·직원 업무부담 변화
- **비용 절감액** — 별도 하드웨어를 요구하지 않는다는 것은 설계상 사실이지만,
  절감액을 산출한 적은 없습니다
- 고령자 사용 성공률
- 다른 기관에서의 실제 운용 결과

### 쓰지 않는 표현

「전국 최초」 · 「국내 유일」 · 「○○% 단축」 · 「완전 자동화」 — **근거가 없습니다.**
확인되지 않은 우위 주장은 이 리포 어디에도 넣지 않습니다.

---

## 7. 용어

| 말 | 뜻 |
|---|---|
| **BAR** | 상단 바. 로고·기관명·서식 이름·도구 |
| **ASK** | 왼쪽. 지금 시민이 해야 할 것 (묻고 입력받는 곳) |
| **PAPER** | 가운데. 실제 작성되고 있는 행정서식 |
| **RAIL** | 오른쪽. 작성하는 동안 계속 참고할 정보. 「INFO」라고 부르기도 합니다 |
| **Main** | 첫 화면. 서식을 고르는 곳 (`index.html`) |
| **Writing** | 질문에 답하는 작성 화면 |
| **Reference UI** | 여권 화면. 새 문법을 처음 구현한 기준안 |
| **Current Focus** | 지금 적는 서식 칸을 짚는 표시. **화면 전용** · 기관 대표색 파생 |
| **Print Marker** | 인쇄 후 직접 서명·기재할 자리 표시. **인쇄물의 일부** · 노랑 |
| **Clear** | 개인정보를 지우고 첫 화면으로 돌아가는 것 |
| **시민의 말 / 행정명칭** | 「여권 만들기」 / 「여권발급신청서」 |

> ⚠️ **Current Focus 와 Print Marker 는 절대 같은 색을 쓰지 않습니다.** 한 색이 두 뜻을 가지면
> 화면의 노랑은 「지금 적는 칸」, 종이의 노랑은 「여기 서명」이 되어 의미가 무너집니다.

---

## 8. 라이선스와 확산

**공공누리 제1유형(출처표시)** — 상업적 이용·변형 모두 가능합니다([`LICENSE`](LICENSE)).
단 **군포시 로고와 서식 원본은 적용 대상이 아닙니다**(로고는 각 기관 것으로 교체).

가져다 쓰는 절차는 [`docs/EXPANSION_PLAYBOOK.md`](docs/EXPANSION_PLAYBOOK.md) 와
[`docs/타지자체-확산.md`](docs/타지자체-확산.md) 에 있습니다.
